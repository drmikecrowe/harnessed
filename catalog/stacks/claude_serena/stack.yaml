# yaml-language-server: $schema=../../../schemas/stack.schema.json
# Stack: claude_serena — Serena semantic code intelligence for Claude.
# Naming: <agent>_<recipe> (claude + serena).
name: claude_serena
harness: claude
recipes: [serena]
services: []
