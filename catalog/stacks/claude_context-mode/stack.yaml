# yaml-language-server: $schema=../../../schemas/stack.schema.json
# Stack: claude_context-mode — minimal stack for the project-scoped persist spike.
# Naming: <agent>_<recipe>… (claude + context-mode).
name: claude_context-mode
harness: claude
recipes: [context-mode]
services: []
