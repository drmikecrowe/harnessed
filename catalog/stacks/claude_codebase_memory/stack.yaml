# yaml-language-server: $schema=../../../schemas/stack.schema.json
# Stack: claude_codebase_memory — Claude + a single stdio MCP server (cbm).
# Naming: <agent>_<recipe>… (claude + codebase-memory-mcp; underscores between fields).
#
# Contingent on the hatago consolidation: the recipe assembles (pin gate + schema pass here), but
# the capability test only proves the MCP connects — full end-to-end indexing (index_repository /
# get_graph_schema / trace_path) needs hatago in the harness container with the project mounted.
# See catalog/recipes/codebase-memory-mcp/PLAN.md.
name: claude_codebase_memory
harness: claude
recipes: [codebase-memory-mcp]
services: []
