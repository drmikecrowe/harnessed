# yaml-language-server: $schema=https://raw.githubusercontent.com/drmikecrowe/harnessed/main/schemas/stack.schema.json
# Stack: claude_headroom — capability stack for the headroom recipe. One stdio MCP child,
# uvx-resolved at first spawn, so a green `harnessed test` proves the whole chain the recipe
# owns: derived image assembly, hatago hub, child spawn, and the compression tools connecting.
name: claude_headroom
instructions: >
  headroom tracer — the compression tools (headroom_compress / headroom_retrieve /
  headroom_stats) reachable as MCP tools through hatago.
recipes: [headroom]
services: []
