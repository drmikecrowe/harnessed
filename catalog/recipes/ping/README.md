# ping

**Tracer recipe — not a real capability.** A network-native ping MCP server, backed by the shared
`ping` service. Exists to exercise the service + `service:` MCP-ref path end to end.

No upstream: this recipe exists only inside harnessed.
