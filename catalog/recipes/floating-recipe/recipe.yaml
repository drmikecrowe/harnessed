# yaml-language-server: $schema=../../../schemas/recipe.schema.json
# Test fixture — triggers the pin-validation rejection (PinValidationError).
#
# Exists only to exercise the assembler's validate_pin() gate: its Dockerfile contains a floating
# ref, so assembling any stack that uses it fails before any image layer is written.
name: floating-recipe
description: Test fixture — contains a floating Dockerfile ref to trigger pin-validation rejection.
