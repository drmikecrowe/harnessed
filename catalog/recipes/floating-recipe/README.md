# floating-recipe

**Test fixture — not a real recipe.** Contains a deliberately floating Dockerfile ref so the
pin-validation lint has something to reject. Do not put it in a stack.

No upstream: this recipe exists only inside harnessed.
