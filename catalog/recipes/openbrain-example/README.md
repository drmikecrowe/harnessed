# openbrain-example

**Template, not a working capability.** The reference example of a remote, `url:`-based
Streamable-HTTP MCP server (modelled on OB1/OpenBrain). Copy it when adding a recipe that points at
a network-native MCP endpoint that harnessed does not itself run.

No upstream: this recipe exists only inside harnessed, as a template.
