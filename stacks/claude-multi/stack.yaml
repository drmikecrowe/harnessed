# Stack: claude-multi — recipe breadth (plan 04-03, success criterion 2).
#
# Two recipes on the claude harness: `time` (one MCP server + one skill) + `greet` (one skill, no
# MCP). Proves the assembler composes MULTIPLE recipes into one profile and the capability test
# asserts BOTH (the `time` MCP server connected AND the `greet-helper` skill present). Only the
# fields this tracer exercises are set; `services`, `permissions`, and `state` use defaults and are
# parsed forward by the assembler (D-14).
name: claude-multi
harness: claude       # claude | omp  (exactly one)
recipes: [time, greet]
