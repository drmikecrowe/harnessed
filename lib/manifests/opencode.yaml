# lib/manifests/opencode.yaml — per-harness mount manifest for the opencode harness.
#
# Source: CONTEXT.md D-01, D-02, D-03.
#
# profile_files: filenames to mount ro from profiles/<stack>/ into the container.
#   opencode reads .claude/ profile natively (SKILL.md, CLAUDE.md/AGENTS.md); .mcp.json
#   and settings.json are mounted into ~/.claude/ per Pitfall 4 in 09-RESEARCH.md.
#
# history_dirs: intentionally empty — opencode history surfacing deferred to Phase 10 / MNT2-07.

profile_files:
  - .mcp.json
  - settings.json

history_dirs: []  # opencode history deferred to Phase 10 / MNT2-07
