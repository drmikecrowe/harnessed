---
phase: 02-isolated-tracer-bullet-stack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - recipes/time/recipe.yaml
  - recipes/time/skills/time-helper/SKILL.md
  - stacks/tracer-time/stack.yaml
  - tools/Dockerfile
  - tools/pyproject.toml
  - tools/harnessed/__init__.py
  - tools/harnessed/cli.py
  - tools/harnessed/schema.py
  - tools/harnessed/assemble.py
  - tools/harnessed/synclinks.py
  - tools/harnessed/emit.py
  - base/Dockerfile.hatago
  - harnessed
  - lib/harnessed-common.sh
autonomous: false
requirements: [RCP-01, RCP-02, RCP-03, RCP-04, MCP-03]
user_setup: []
must_haves:
  truths:
    - "`harnessed build tracer-time` runs the harnessed-tools image (emit-only) then host `podman build`, producing profiles/tracer-time/.claude/ + hatago.config.json + an emitted Dockerfile/launcher — nothing assembled at container start"
    - "The assembler fans the recipe's standalone skill into profiles/tracer-time/.claude/skills/ and FAILS the build (non-zero exit, both names reported) on a name collision"
    - "harnessed-tools never invokes podman/docker — it only reads/writes the mounted build dir"
    - "base/Dockerfile.hatago bakes @himorishige/hatago-mcp-hub + the light stdio MCP server (uvx mcp-server-time) so hatago can run it as a child (stdio→HTTP)"
  artifacts:
    - "recipes/time/recipe.yaml — one MCP server (time, command uvx, transport stdio) + one standalone skill (skills/time-helper)"
    - "stacks/tracer-time/stack.yaml — config: isolated, harness: claude, recipes: [time]"
    - "tools/ — the harnessed-tools Python assembler image (cli/schema/assemble/synclinks/emit) that emits files only"
    - "base/Dockerfile.hatago — hatago hub + baked uvx mcp-server-time"
    - "profiles/tracer-time/ — generated + committed (.claude/skills/time-helper + hatago.config.json)"
  key_links:
    - "harnessed build <stack> → harnessed-tools emit → host podman build; the emitted profile + hatago image are consumed by the isolated launcher in plan 02-02"
    - "recipe MCP entry transport=stdio → hatago.config.json child-server entry → baked into base/Dockerfile.hatago (MCP-03)"
---

<objective>
Define the recipe + stack schema and build the file-emitting assembler that turns them into a committed
profile + a hatago image — the "assemble" leg of the tracer bullet.

Purpose: This is the reproducible build pipeline (RCP-01..04, MCP-03). Plans 02-02 (run) and 02-03 (assert)
consume its output: the committed `profiles/tracer-time/` tree, `hatago.config.json`, and the hatago image.
Output: `recipes/time/`, `stacks/tracer-time/`, the `tools/` assembler image, `base/Dockerfile.hatago`,
the generated `profiles/tracer-time/`, and a `harnessed build` subcommand.

Decisions covered (02-CONTEXT.md): D-01, D-02, D-03 (tracer-bullet composition: claude + uvx network-free stdio server + standalone no-dep skill), D-04 (emit `.mcp.json` → single hatago endpoint), D-06 (one base hatago image + generated `hatago.config.json`), D-12 (emit-only assembler, host builds), D-13 (fail-fast collision), D-14 (schema scope: §11/§12, tracer-bullet fields required, rest parsed forward).
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-isolated-tracer-bullet-stack/02-CONTEXT.md
@.planning/phases/02-isolated-tracer-bullet-stack/02-RESEARCH.md
@.planning/phases/02-isolated-tracer-bullet-stack/02-PATTERNS.md
@docs/harnessed-design.md
@stacks/transparent/stack.yaml
@base/Dockerfile.harnessed-claude
@harnessed
@lib/harnessed-common.sh
</context>

<threat_model>
**ASVS L1 — block on high.**
- **T-02-01 (Tampering/Supply-chain, hatago image):** `base/Dockerfile.hatago` installs JS (`@himorishige/hatago-mcp-hub`) and Python (`uvx mcp-server-time`). Mitigation: install via pnpm (`pnpm dlx`) and `uvx` with pinned versions; the managed supply-chain config + scan gate is Phase 3, but pin versions now and do NOT use raw `npm`/`npx`. Disposition: mitigate.
- **T-02-02 (Info disclosure, committed profile):** the assembler commits `profiles/tracer-time/`. A secret written there persists in git history. Mitigation: the assembler writes only skills/config/`hatago.config.json` (URLs/`*_env` refs, never secret values); no credentials are read in this plan. Disposition: mitigate.
- **T-02-03 (Elevation, assembler scope):** `harnessed-tools` runs with a mounted build dir. Mitigation: it only reads/writes that dir and never mounts the daemon socket (emit-only, §15). Disposition: mitigate.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: recipe + stack schema (with the tracer-bullet instance)</name>
  <files>recipes/time/recipe.yaml, recipes/time/skills/time-helper/SKILL.md, stacks/tracer-time/stack.yaml</files>
  <read_first>docs/harnessed-design.md, stacks/transparent/stack.yaml, .planning/phases/02-isolated-tracer-bullet-stack/02-CONTEXT.md</read_first>
  <action>Author the two YAML schemas per design §11/§12 and the one tracer-bullet instance. `recipes/time/recipe.yaml`: top-level `name: time`, `description`; an `mcp.servers` list with one entry `{ name: time, command: uvx, args: [mcp-server-time], transport: stdio }` (transport explicit per RESEARCH Pitfall B); a `skills` list with one entry `{ path: skills/time-helper }` (a STANDALONE skill shipped by the recipe — no `plugins:` vendoring, no `deps:`, so no dependency install and no scan are needed this phase). `recipes/time/skills/time-helper/SKILL.md`: a minimal valid Claude skill (frontmatter `name: time-helper` + `description`, a one-paragraph body) — content is trivial; its only job is to be a no-dep skill the assembler can fan and the capability test can detect. `stacks/tracer-time/stack.yaml`: `name: tracer-time`, `config: isolated`, `harness: claude`, `recipes: [time]` (omit services/permissions/state — defaults). Keep parsing tolerant: only these fields are required now; unknown fields parse forward (D-14).</action>
  <verify>`python -c "import yaml,sys; [yaml.safe_load(open(f)) for f in ['recipes/time/recipe.yaml','stacks/tracer-time/stack.yaml']]"` exits 0; SKILL.md has a `name:` frontmatter key.</verify>
  <acceptance_criteria>
    - recipes/time/recipe.yaml contains 'transport: stdio' and 'command: uvx' and 'mcp-server-time'
    - recipes/time/recipe.yaml references 'skills/time-helper' and recipes/time/skills/time-helper/SKILL.md exists with 'name: time-helper'
    - stacks/tracer-time/stack.yaml contains 'config: isolated', 'harness: claude', and 'recipes:' listing 'time'
  </acceptance_criteria>
  <done>The recipe + stack schema and the tracer-bullet instance exist and parse as valid YAML.</done>
</task>

<task type="auto">
  <name>Task 2: harnessed-tools assembler image (emit-only) + sync-links fail-fast</name>
  <files>tools/Dockerfile, tools/pyproject.toml, tools/harnessed/__init__.py, tools/harnessed/cli.py, tools/harnessed/schema.py, tools/harnessed/assemble.py, tools/harnessed/synclinks.py, tools/harnessed/emit.py</files>
  <read_first>docs/harnessed-design.md, .planning/phases/02-isolated-tracer-bullet-stack/02-RESEARCH.md, .planning/phases/02-isolated-tracer-bullet-stack/02-PATTERNS.md, base/Dockerfile.harnessed-base</read_first>
  <action>Build the `harnessed-tools` Python image and its package. `tools/Dockerfile`: a Python 3.12/3.13 image (mise or python-slim) with pip-installed `tools/` package; bake `ruamel.yaml`/PyYAML, `rich`, and `jq` available. The package logic (EMIT ONLY — never call podman/docker): `schema.py` parses+validates `recipe.yaml`/`stack.yaml` (dataclasses/pydantic; tolerant of unknown fields, D-14). `synclinks.py` fans each recipe's `skills`/`commands` into harness-native paths under the target profile dir and RAISES on a name collision reporting BOTH colliding source paths (RCP-04, port `sync-plugin-links`' conflict behavior). `assemble.py` orchestrates: read the stack + its recipes → fan skills via synclinks → merge each recipe's `mcp.servers` into a single `hatago.config.json` (child stdio servers keyed by name; exact key shape per hatago docs — read at implementation) → derive the harness `.mcp.json` (exactly ONE entry → `http://localhost:3535/mcp`, never the stdio server directly) → collect the set of light servers that must be baked into the hatago image. `emit.py` writes into the mounted build dir: `profiles/<stack>/.claude/{skills,commands,agents,hooks,rules}/` (the fanned tree), `profiles/<stack>/.claude/.mcp.json` (the single hatago endpoint), `profiles/<stack>/hatago.config.json`, and a manifest of which servers the hatago image must bake (the Dockerfile.hatago itself is Task 3). `cli.py` exposes `assemble <stack> --build-dir <dir>` and prints a summary. The container entrypoint runs `cli.py`. The image must contain NO `podman`/`docker` binary usage and the code must not shell out to them.</action>
  <verify>`python -m harnessed.cli assemble tracer-time --build-dir /tmp/bd` (run inside the tools image, or locally with deps) writes `/tmp/bd/profiles/tracer-time/.claude/skills/time-helper/SKILL.md`, `/tmp/bd/profiles/tracer-time/.claude/.mcp.json` (single entry → `http://localhost:3535/mcp`), and `/tmp/bd/profiles/tracer-time/hatago.config.json`; a crafted second recipe shipping a `time-helper` skill makes the run exit non-zero and print both source paths.</verify>
  <acceptance_criteria>
    - grep: tools/ contains no 'podman' or 'docker' invocation (emit-only)
    - synclinks.py raises/exits non-zero on a duplicate skill/command name and prints both source paths
    - assemble produces profiles/<stack>/.claude/skills/time-helper/, profiles/<stack>/.claude/.mcp.json, and profiles/<stack>/hatago.config.json under the build dir
    - the emitted .claude/.mcp.json has exactly one server entry pointing at 'http://localhost:3535/mcp' (the hatago endpoint), NOT the stdio server directly (MCP-02 wiring)
    - hatago.config.json declares the 'time' server as a child stdio server (MCP-03)
  </acceptance_criteria>
  <done>The assembler emits the profile + hatago config from the schema and fails fast on collisions, without touching the daemon.</done>
</task>

<task type="auto">
  <name>Task 3: base/Dockerfile.hatago + harnessed build subcommand</name>
  <files>base/Dockerfile.hatago, harnessed, lib/harnessed-common.sh</files>
  <read_first>base/Dockerfile.harnessed-claude, harnessed, lib/harnessed-common.sh, .planning/phases/02-isolated-tracer-bullet-stack/02-RESEARCH.md</read_first>
  <action>`base/Dockerfile.hatago`: a hatago image that bakes the hub and the tracer-bullet's light stdio server — install `@himorishige/hatago-mcp-hub` via `pnpm dlx`/`pnpm add` (pinned) and `uvx mcp-server-time` (pinned), set the entrypoint/CMD to `hatago serve --http --port 3535` (exact flags per hatago `--help`; the harness will reach `http://localhost:3535/mcp`). `FROM harnessed-base` or a slim node+uv base — lineage only (§6). In `lib/harnessed-common.sh`: add `HARNESSED_HATAGO_IMAGE="harnessed-hatago:latest"` beside the existing `HARNESSED_*_IMAGE` (line 16-17) and extend `build_images` (line 38) to also `podman build` the hatago image from `base/Dockerfile.hatago`. In `harnessed`: add a `build` subcommand handler in the arg parser (beside `-b|--build|build` at line 57) so `harnessed build <stack>` (a) ensures the `harnessed-tools` image exists (build it on first use from `tools/Dockerfile`), (b) runs it `podman run --rm -v <build-dir>:<build-dir> harnessed-tools assemble <stack> --build-dir <build-dir>` to EMIT files, then (c) runs host `podman build` for `base/Dockerfile.hatago`. Distinguish `harnessed build` (no stack arg → rebuild base/claude/hatago images, current behavior) from `harnessed build <stack>` (assemble a stack). Keep `set -euo pipefail` discipline; fallible probes use `|| true`.</action>
  <verify>`bash -n harnessed lib/harnessed-common.sh` parses; `grep HARNESSED_HATAGO_IMAGE lib/harnessed-common.sh` matches; `harnessed build tracer-time` (on a podman host) emits profiles/tracer-time/ and builds harnessed-hatago.</verify>
  <acceptance_criteria>
    - base/Dockerfile.hatago contains '@himorishige/hatago-mcp-hub' and 'mcp-server-time' and a 'serve' + '3535' invocation
    - lib/harnessed-common.sh contains 'HARNESSED_HATAGO_IMAGE' and build_images builds the hatago image
    - harnessed dispatches 'build <stack>' to run the harnessed-tools image with a mounted build dir then host podman build
    - grep: harnessed contains no 'CONTAINER_HOST'/'DOCKER_HOST'/'.sock' (host runs podman; assembler is emit-only)
  </acceptance_criteria>
  <done>`harnessed build <stack>` assembles a stack into a committed profile and builds the hatago image, host-natively.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The recipe/stack schema, the `harnessed-tools` emit-only assembler, `base/Dockerfile.hatago`, and `harnessed build <stack>`. Building the tools + hatago images and running the assembler needs a working host rootless podman, which only the operator can confirm.</what-built>
  <how-to-verify>Run `./harnessed build tracer-time` on a machine with podman. Confirm: (1) it builds `harnessed-tools` and `harnessed-hatago`; (2) `profiles/tracer-time/.claude/skills/time-helper/SKILL.md` and `profiles/tracer-time/hatago.config.json` exist; (3) `hatago.config.json` lists the `time` server. Then commit the generated `profiles/tracer-time/`.</how-to-verify>
  <resume-signal>Type "approved" once the build emits the profile + hatago config and the hatago image builds, or describe the error.</resume-signal>
</task>

</tasks>

<verification>
- [ ] `bash -n harnessed lib/harnessed-common.sh` parses; YAML schemas parse
- [ ] harnessed-tools emits profiles/tracer-time/.claude/skills/time-helper + hatago.config.json (no podman/docker calls)
- [ ] sync-links fails the build on a duplicate name, reporting both paths
- [ ] base/Dockerfile.hatago builds and bakes hatago + uvx mcp-server-time
- [ ] `harnessed build tracer-time` runs assemble (emit) then host podman build
</verification>

<success_criteria>
- All tasks completed
- Recipe + stack schema (RCP-01, RCP-02) implemented with the tracer-bullet instance
- Assembler emits profile + hatago config, fails fast on collision (RCP-03, RCP-04)
- Light stdio server baked into the hatago image + declared in hatago.config.json (MCP-03)
- No daemon access from the assembler; no raw npm/npx
</success_criteria>

<output>
After completion, create `.planning/phases/02-isolated-tracer-bullet-stack/02-01-SUMMARY.md`
</output>
