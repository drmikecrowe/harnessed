---
phase: 02-isolated-tracer-bullet-stack
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - tools/harnessed/capability.py
  - tools/harnessed/report.py
  - tools/harnessed/cli.py
  - harnessed
autonomous: false
requirements: [TST-01, TST-02]
user_setup: []
must_haves:
  truths:
    - "`harnessed test tracer-time` builds + launches the stack --fresh headless and asserts the manifest's declared capabilities (MCP server 'time' + skill 'time-helper') are present, exiting non-zero if any is missing or disconnected"
    - "Assertion prefers machine-readable introspection — hatago's hatago://servers resource and/or `claude mcp list` for MCP, headless JSON (or mounted-profile filesystem) skill listing diffed against the stack manifest — with an LLM-prompt backstop"
    - "The stack manifest (stacks/tracer-time/stack.yaml + its recipes) is the test oracle — expected capabilities are derived from it, not hardcoded"
    - "The same check renders a rich markdown per-capability report (capability | kind | status table); CI consumes the same structured result as the assertion"
  artifacts:
    - "tools/harnessed/capability.py — per-stack capability test (manifest oracle → introspect live --fresh instance → structured result)"
    - "tools/harnessed/report.py — rich markdown capability report from the structured result"
    - "harnessed test <stack> — the capability-test entrypoint"
  key_links:
    - "capability.py launches the stack via the 02-02 isolated launcher (--fresh headless) and reads the manifest from 02-01's recipes/stacks"
    - "report.py and the CI exit code derive from one structured result object (one mechanism, two audiences — §18)"
---

<objective>
Add the per-stack capability test + markdown report — the "assert green" leg that closes the tracer bullet.

Purpose: Delivers TST-01 (assert a live --fresh headless instance exposes exactly the manifest's declared
MCP servers/skills) and TST-02 (render a markdown capability report). This is the oracle that makes the
whole slice verifiable — and the user-facing "how well the recipe built" artifact.
Output: `tools/harnessed/capability.py`, `tools/harnessed/report.py`, and a `harnessed test <stack>` command.

Decisions covered (02-CONTEXT.md): D-10 (machine-readable introspection first — hatago://servers / claude mcp list / skill JSON diff — LLM prompt as backstop), D-11 (rich markdown capability report; one structured result drives report + CI; no assembler unit tests).
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-isolated-tracer-bullet-stack/02-CONTEXT.md
@.planning/phases/02-isolated-tracer-bullet-stack/02-RESEARCH.md
@.planning/phases/02-isolated-tracer-bullet-stack/02-PATTERNS.md
@docs/harnessed-design.md
@harnessed
@lib/harnessed-isolated.sh
</context>

<threat_model>
**ASVS L1 — block on high.**
- **T-02-07 (Info disclosure, test output):** the capability report could echo MCP server config that contains secrets. Mitigation: the report prints capability names + status only (no config values/tokens); it asserts the slice exposes declared capabilities, not their secrets. Disposition: mitigate.
- **T-02-08 (Tampering, state bleed between test runs):** Mitigation: the test runs `--fresh` and tears down after, so no state persists between runs (inherits 02-02's `--fresh` teardown). Disposition: mitigate.
- The test inherits 02-02's auth handling (ro credential, token-free stub) — no new credential surface is introduced here.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: capability test — manifest oracle vs live introspection</name>
  <files>tools/harnessed/capability.py</files>
  <read_first>docs/harnessed-design.md, .planning/phases/02-isolated-tracer-bullet-stack/02-RESEARCH.md, lib/harnessed-isolated.sh, tools/harnessed/schema.py</read_first>
  <action>Create `tools/harnessed/capability.py` implementing the per-stack capability test (design §18). Derive the EXPECTED capability set from the manifest oracle: parse `stacks/<stack>/stack.yaml` + its `recipes/<r>/recipe.yaml` (reuse `schema.py`) → expected MCP servers (e.g. `time`) and expected skills/commands (e.g. `time-helper`). Launch the stack `--fresh` in HEADLESS mode (via the 02-02 isolated launcher; run claude headless with `claude -p "<introspection prompt>" --output-format json`). Assert ACTUAL capabilities, preferring machine-readable introspection: for MCP, query hatago's `hatago://servers` resource and/or run `claude mcp list` inside the instance and parse connected servers; for skills, prefer a headless JSON listing of visible skills, falling back to a filesystem assertion that `/home/harnessed/.claude/skills/<name>/SKILL.md` exists in the running instance (the mounted profile). Keep an LLM-prompt backstop ("confirm you can use skill time-helper and MCP time") but do NOT make it the primary signal. Build a STRUCTURED result: a list of `{ capability, kind (mcp|skill|command), expected, present/connected (bool), detail }`. Return non-zero overall if any expected capability is missing/disconnected. Tear the instance down (`--fresh` guarantees no bleed). Do NOT hardcode `time`/`time-helper` — read them from the manifest.</action>
  <verify>`python -c "import tools.harnessed.capability"` imports; a dry-run unit (manifest parse → expected set) returns `{mcp: [time], skills: [time-helper]}` for tracer-time; the structured result object has one entry per expected capability with a boolean status.</verify>
  <acceptance_criteria>
    - capability.py derives expected capabilities from stacks/<stack>/stack.yaml + recipes (grep: reads the manifest, no hardcoded 'time'/'time-helper' literal as the source of truth)
    - it queries hatago://servers and/or 'claude mcp list' for MCP and a headless/filesystem skill listing for skills
    - it returns a structured per-capability result and a non-zero overall status when a capability is missing
    - it launches the stack --fresh headless and tears it down
  </acceptance_criteria>
  <done>The capability test compares the manifest oracle against a live --fresh instance via machine-readable introspection.</done>
</task>

<task type="auto">
  <name>Task 2: rich markdown capability report + CI exit code</name>
  <files>tools/harnessed/report.py</files>
  <read_first>tools/harnessed/capability.py, docs/harnessed-design.md, .planning/phases/02-isolated-tracer-bullet-stack/02-RESEARCH.md</read_first>
  <action>Create `tools/harnessed/report.py` that takes the structured result from `capability.py` and renders a `rich` markdown per-capability table with columns `capability | kind | status` (status e.g. `✓ connected`, `✓ present`, `✗ missing (<reason>)`) — matching the design §18 report shape. The SAME structured result drives the report and the process exit code (green when all expected capabilities are present/connected, non-zero otherwise) — one mechanism, two audiences. Print the table to stdout (markdown→terminal via rich) and also emit the structured result as JSON when a `--json` flag is passed (for CI consumption). No secret/config values in the table — capability names + status only.</action>
  <verify>`python -c "import tools.harnessed.report"` imports; feeding a sample result with one missing capability renders a table marking it `✗` and the exit code is non-zero; an all-present result renders all `✓` and exits 0.</verify>
  <acceptance_criteria>
    - report.py renders a markdown table with 'capability', 'kind', 'status' columns via rich
    - the same structured result yields the exit code (0 all-green, non-zero on any missing)
    - --json emits the structured result for CI; no token/config values appear in the output
  </acceptance_criteria>
  <done>The capability report renders as markdown and drives the CI pass/fail from one result.</done>
</task>

<task type="auto">
  <name>Task 3: harnessed test subcommand</name>
  <files>harnessed, tools/harnessed/cli.py</files>
  <read_first>harnessed, tools/harnessed/cli.py, tools/harnessed/capability.py</read_first>
  <action>Wire the entrypoint. In `tools/harnessed/cli.py`: add a `test <stack> [--json]` subcommand that runs `capability.py` then `report.py`. In `harnessed`: add a `test` subcommand to the dispatcher that runs the capability test for `<stack>` — since the test needs to launch a live `--fresh` headless instance (host podman), the `harnessed test <stack>` flow ensures the stack is built (`harnessed build <stack>` if the profile/image is missing) then runs the capability test (which uses the 02-02 launcher headless), prints the rich report, and exits with the test's status. Keep it host-native; the introspection commands run via `podman exec` inside the instance. Preserve `set -euo pipefail`; the test's non-zero exit must propagate (do NOT swallow it).</action>
  <verify>`bash -n harnessed` parses; `harnessed test tracer-time` (on a podman host) builds if needed, runs the capability test, prints the markdown report, and exits 0 only when the time server + time-helper skill are both present.</verify>
  <acceptance_criteria>
    - harnessed dispatches a 'test <stack>' subcommand that runs the capability test and propagates its exit code
    - cli.py exposes 'test <stack>' wiring capability + report
    - the report is printed (markdown) and the process exit reflects green/red
  </acceptance_criteria>
  <done>`harnessed test <stack>` runs the per-stack capability test and renders the report.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The per-stack capability test + markdown report. Running it requires launching a live --fresh headless instance (host rootless podman + Claude auth), which only the operator can do. This is the success-criteria gate for Phase 2 — the tracer bullet is "green end to end".</what-built>
  <how-to-verify>On a machine with podman + Claude auth: run `./harnessed test tracer-time`. Confirm: (1) it builds (if needed), launches the stack --fresh headless, and renders a markdown capability table; (2) the `time` MCP server shows `✓ connected` and the `time-helper` skill shows `✓ present`; (3) the command exits 0. Optionally rename the recipe's skill to force a mismatch and confirm the report marks it `✗` and the command exits non-zero.</how-to-verify>
  <resume-signal>Type "approved" once the capability test passes green (time connected, time-helper present) and the report renders, or describe the failure.</resume-signal>
</task>

</tasks>

<verification>
- [ ] `python -c "import tools.harnessed.capability, tools.harnessed.report"` imports; `bash -n harnessed` parses
- [ ] expected capabilities derived from the manifest (not hardcoded)
- [ ] MCP asserted via hatago://servers / claude mcp list; skills via headless JSON or mounted-profile filesystem
- [ ] markdown report renders; same result drives the exit code; --json for CI
- [ ] `harnessed test tracer-time` passes green on a podman host (operator checkpoint)
</verification>

<success_criteria>
- All tasks completed
- Capability test asserts the live --fresh headless instance exposes exactly the manifest's declared MCP server + skill (TST-01)
- Markdown capability report renders; CI consumes the same structured result (TST-02)
- Manifest is the oracle; machine-readable introspection primary, LLM prompt backstop
- No assembler unit tests; no secret/config values in the report
</success_criteria>

<output>
After completion, create `.planning/phases/02-isolated-tracer-bullet-stack/02-03-SUMMARY.md`
</output>
