---
phase: 11-architecture-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
  - AGENTS.md
autonomous: true
requirements: [DOC2-01]
must_haves:
  truths:
    - "README image lineage section describes all 3 layers: harnessed-base, harnessed-<harness>, harnessed-<stack>"
    - "README quickstart shows harnessed test tracer-time after harnessed build and run"
    - "No narrative use of 'isolated' or 'transparent' remains in README.md, CLAUDE.md, or AGENTS.md"
    - "CLAUDE.md core value statement drops 'isolated' adjective from the description of a launched instance"
  artifacts:
    - path: "README.md"
      provides: "Updated image lineage (3-layer) + quickstart with capability test"
      contains: "harnessed-<stack>"
    - path: "CLAUDE.md"
      provides: "Updated core value without narrative isolated/transparent"
  key_links:
    - from: "README.md"
      to: "docs/harnessed-design.md"
      via: "design references (§2, §3, §15)"
      pattern: "design.*§"
---

<objective>
Update README.md to describe the 3-layer image lineage (base → agent → derived-stack) introduced by Phase 8, extend the quickstart to include the capability test, and remove all narrative uses of "isolated" and "transparent" from README.md, CLAUDE.md, and AGENTS.md.

Purpose: README is the primary entry point; it still describes only 2 image layers and omits the capability test from the quickstart. CLAUDE.md still uses "isolated" to describe what a stack is.

Output: Updated README.md, CLAUDE.md, AGENTS.md with current architecture and no stale mode terminology.
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/mcrowe/Programming/Personal/code-container/.planning/ROADMAP.md
@/home/mcrowe/Programming/Personal/code-container/.planning/phases/11-architecture-documentation/11-01-PLAN.md
@/home/mcrowe/Programming/Personal/code-container/README.md
@/home/mcrowe/Programming/Personal/code-container/CLAUDE.md
@/home/mcrowe/Programming/Personal/code-container/AGENTS.md
@/home/mcrowe/Programming/Personal/code-container/stacks/gstack-time/stack.yaml
@/home/mcrowe/Programming/Personal/code-container/recipes/gstack/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md — 3-layer image lineage + quickstart capability test + de-narrative</name>
  <files>README.md</files>
  <read_first>
    README.md — full current state (169 lines); understand every section before editing
    stacks/gstack-time/stack.yaml — confirms the derived stack image name pattern harnessed-gstack-time
    recipes/gstack/Dockerfile — confirms ARG HARNESS, FROM stripped by assembler, recipe body concatenation pattern
  </read_first>
  <action>
Edit README.md with three targeted changes:

1. **Image lineage section** (currently "First-run build", around line 69): The current text describes 2 layers. Update it to describe 3 layers explicitly:
   - Layer 1 — `harnessed-base`: fat toolchain image (mise, node@24, python, pnpm; no harness CLI)
   - Layer 2 — `harnessed-<harness>`: FROM harnessed-base + harness CLI installed (one image per harness: claude, omp, opencode, gemini, antigravity, codex)
   - Layer 3 — `harnessed-<stack>`: derived stack image built by `harnessed build <stack>` — FROM harnessed-<harness> + recipe Dockerfiles concatenated (e.g. `harnessed-gstack-time` builds FROM `harnessed-claude`)
   Retain `hatago` and `harnessed-tools` as supporting images, but clarify they are not part of the base→agent→stack lineage.

2. **Quickstart section** (around line 85): After the two-command example, add a third step showing the capability test:
   ```
   harnessed test tracer-time
   ```
   Add a brief note that `harnessed test` runs the two-oracle capability check and writes `profiles/tracer-time/capability-report.md`.

3. **Narrative de-terminology**: Replace narrative uses of "isolated" that describe a harnessed concept (not a file or function name):
   - Line 24: "composable isolated stacks" → "composable harness stacks"
   - Line 28: "isolated, composable harness stacks" → "composable harness stacks"
   - Line 31: "isolated, authenticated instance" → "authenticated instance"
   - Line 157 (comparison table, "Fully isolated" refers to Trail of Bits' architecture, NOT harnessed — leave this unchanged as it describes another project)

   Do NOT change any `code spans`, function names, file names, or section headings that use the word as a technical identifier.
  </action>
  <verify>
    <automated>rg -n "3-layer\|harnessed-&lt;stack&gt;\|harnessed-gstack\|Layer 1\|Layer 2\|Layer 3\|base.*agent.*stack\|harnessed-base.*harnessed-.*harnessed-" README.md | head -10</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains the phrase "harnessed-&lt;stack&gt;" in the image lineage description
    - README.md contains "harnessed test tracer-time" in the quickstart section
    - `rg "isolated|transparent" README.md` returns only: the comparison table row with "Fully isolated" (describing Trail of Bits), and any code-span/backtick references to file/function names; NO narrative sentences describing a harnessed stack as "isolated"
    - `rg "harnessed-base.*harnessed-.*harnessed-" README.md` returns at least one match showing the 3-layer progression
  </acceptance_criteria>
  <done>README.md describes 3-layer image lineage, quickstart includes capability test step, no narrative "isolated" describing a harnessed stack or instance.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md + AGENTS.md — remove narrative isolated/transparent</name>
  <files>CLAUDE.md, AGENTS.md</files>
  <read_first>
    CLAUDE.md — full current state (274 lines); locate line 19 (Core Value) and any other narrative uses
    AGENTS.md — full current state (63 lines); check for any narrative isolated/transparent
  </read_first>
  <action>
Edit CLAUDE.md with targeted change:

1. **CLAUDE.md line 19** (Core Value): The sentence "You can compose a named stack (one harness + chosen recipes) and launch an isolated, authenticated instance that exposes **exactly**…" — change "an isolated, authenticated instance" to "an authenticated instance". The core value is about composition and reproducibility, not the internal isolation model.

2. **CLAUDE.md**: Scan for any other narrative uses of "isolated" or "transparent" that describe the harnessed stack model (not file names, function names, or CLI commands). If found, rewrite to use current terminology (e.g. "stack instance" instead of "isolated instance").

3. **AGENTS.md**: Scan for narrative uses of "isolated" or "transparent". AGENTS.md is short (63 lines); no hits were found in the initial scan but confirm by reading the file.

Do NOT change:
- The "transparent" stack command `harnessed transparent` where it appears as a CLI command in code blocks
- Any file path references like `lib/harnessed-isolated.sh` or function names like `harnessed_isolated`
- The phrase "composable isolated stacks" in the Forked-from note if it appears in AGENTS.md (already handled in Task 1 for README)
  </action>
  <verify>
    <automated>rg -v '^#' CLAUDE.md AGENTS.md | rg -c "isolated|transparent" || echo "0 narrative matches"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md line containing "Core Value" no longer contains the word "isolated" as a description of the launched instance
    - `rg "isolated|transparent" CLAUDE.md` returns zero matches, OR all remaining matches are in: (a) code blocks showing CLI commands, (b) file/function name references like `harnessed-isolated.sh`, or (c) the technology stack table where "isolated" is used in a technical configuration context
    - `rg "isolated|transparent" AGENTS.md` returns zero matches
  </acceptance_criteria>
  <done>CLAUDE.md core value no longer uses "isolated" as a narrative term for a harnessed instance; AGENTS.md has no narrative uses of either term.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor→repo | Documentation edits modify committed markdown files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | README.md, CLAUDE.md | accept | Documentation-only phase; no code changes, no auth, no data handling. Git history provides audit trail. Low-value target for tampering. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Not applicable. |

Note: This is a documentation-only phase. Threat surface is minimal — no code execution, no auth, no data handling, no network calls. All changes are to markdown files committed to a local git repo.
</threat_model>

<verification>
Run after both tasks complete:

```bash
# Confirm 3-layer lineage present in README
rg "harnessed-<stack>" README.md

# Confirm capability test in quickstart
rg "harnessed test tracer-time" README.md

# Confirm no narrative isolated/transparent in target files
rg -n "isolated|transparent" README.md CLAUDE.md AGENTS.md
# Review output: only expected technical references should remain (Trail of Bits row, file/function names)
```
</verification>

<success_criteria>
- README.md image lineage section describes 3 distinct layers: harnessed-base, harnessed-&lt;harness&gt;, harnessed-&lt;stack&gt;
- README.md quickstart includes `harnessed test tracer-time`
- CLAUDE.md core value statement does not use "isolated" to describe a stack or instance
- `rg "isolated|transparent" README.md CLAUDE.md AGENTS.md` produces no narrative harnessed-concept descriptions
</success_criteria>

<output>
Create `.planning/phases/11-architecture-documentation/11-01-SUMMARY.md` when done
</output>
