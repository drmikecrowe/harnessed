---
phase: 11-architecture-documentation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/guides/troubleshooting.md
  - docs/guides/service-authoring.md
  - docs/guides/secrets.md
autonomous: true
requirements: [DOC2-01]
must_haves:
  truths:
    - "No narrative use of 'isolated' or 'transparent' remains in troubleshooting.md, service-authoring.md, or secrets.md"
    - "troubleshooting.md still accurately describes stack authentication and --fresh behavior"
    - "service-authoring.md networking note reflects current pasta/rootless networking model without 'isolated stacks'"
  artifacts:
    - path: "docs/guides/troubleshooting.md"
      provides: "Updated troubleshooting guide with current stack terminology"
    - path: "docs/guides/service-authoring.md"
      provides: "Updated service-authoring guide without isolated-stack narrative"
    - path: "docs/guides/secrets.md"
      provides: "Updated secrets guide without isolated-stack narrative"
  key_links:
    - from: "docs/guides/troubleshooting.md"
      to: "lib/harnessed-isolated.sh"
      via: "code references (technical, not narrative)"
      pattern: "harnessed-isolated\\.sh"
---

<objective>
Remove all narrative uses of "isolated" and "transparent" from docs/guides/troubleshooting.md, docs/guides/service-authoring.md, and docs/guides/secrets.md, replacing with current stack-centric terminology ("stack instance", "stack", "harnessed stack"). Per DOC2-01.

Purpose: These three guides use "isolated instance", "isolated stack", and "isolated mode" as narrative descriptions of what a harnessed stack is. The terms "isolated" and "transparent" were v1 mode names that are no longer the user-facing model.

Output: Updated guides with current terminology; technical code references (file paths, function names) preserved unchanged.
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/mcrowe/Programming/Personal/code-container/.planning/ROADMAP.md
@/home/mcrowe/Programming/Personal/code-container/docs/guides/troubleshooting.md
@/home/mcrowe/Programming/Personal/code-container/docs/guides/service-authoring.md
@/home/mcrowe/Programming/Personal/code-container/docs/guides/secrets.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-narrative troubleshooting.md — replace isolated/transparent with stack terminology</name>
  <files>docs/guides/troubleshooting.md</files>
  <read_first>
    docs/guides/troubleshooting.md — full current state; identify every occurrence of "isolated" and "transparent" and classify as narrative vs technical
  </read_first>
  <action>
Edit docs/guides/troubleshooting.md. For each occurrence of "isolated" and "transparent", apply this decision:

**Narrative uses → replace:**
- "Running an unbuilt **isolated** stack" → "Running an unbuilt stack"
- "An **isolated** instance authenticates by mounting…" → "A stack instance authenticates by mounting…"
- "If `claude` prompts for onboarding in an **isolated** instance" → "If `claude` prompts for onboarding in a stack instance"
- "state bleed (design §9, [lib/harnessed-**isolated**.sh]...)" → keep the link but adjust surrounding prose if "isolated" is used narratively; the file path itself stays unchanged
- "By default an **isolated** instance persists…" → "By default a stack instance persists…"
- Any sentence using "**transparent** mode" as a narrative concept (not as a CLI command) → rephrase to describe behavior directly (e.g., "When running with host config mounted live…")

**Technical uses → preserve:**
- `[lib/harnessed-isolated.sh](../../lib/harnessed-isolated.sh)` — file path links, leave unchanged
- Any backtick-fenced CLI command like `harnessed transparent` — this is a command name, leave it; if it's used narratively around it, adjust only the surrounding prose
- Code blocks showing internal function names like `harnessed_isolated` — leave unchanged

The goal: after the edits, a reader of troubleshooting.md encounters "stack", "stack instance", and "harnessed stack" where the old docs said "isolated stack" or "isolated instance." Technical code references (`lib/harnessed-isolated.sh`) are unaffected.

Do NOT rewrite sections that don't contain the terms; surgical edits only.
  </action>
  <verify>
    <automated>rg -n "isolated|transparent" docs/guides/troubleshooting.md | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Review `rg -n "isolated|transparent" docs/guides/troubleshooting.md` output line by line; every remaining match must be either: (a) a code-span or code-block containing a file path like `harnessed-isolated.sh`, (b) a function name like `harnessed_isolated`, or (c) a CLI command in a code block like `harnessed transparent`
    - No plain prose sentence describes "an isolated instance" or "isolated stack" or "isolated mode" as a narrative description of what harnessed does
    - `rg "stack instance\|harnessed stack\|the stack" docs/guides/troubleshooting.md` returns at least 3 matches (confirming replacements were made)
  </acceptance_criteria>
  <done>troubleshooting.md has no narrative uses of isolated/transparent; only technical code references to files and function names remain.</done>
</task>

<task type="auto">
  <name>Task 2: De-narrative service-authoring.md + secrets.md</name>
  <files>docs/guides/service-authoring.md, docs/guides/secrets.md</files>
  <read_first>
    docs/guides/service-authoring.md — full current state; find line 162 "isolated stacks use rootless (pasta) networking" and any other narrative uses
    docs/guides/secrets.md — full current state; find line 29 "Launch any isolated stack" and any other narrative uses
  </read_first>
  <action>
Edit docs/guides/service-authoring.md:

1. **Line 162** (networking note): The sentence currently reads: "by default **isolated** stacks use rootless (pasta) networking, so pod members reach…". Change to: "by default stacks use rootless (pasta) networking, so pod members reach…". The word "isolated" is not needed here — all stacks use rootless pasta by default.

2. Scan the rest of service-authoring.md for any other narrative uses of "isolated" or "transparent" and apply the same principle: replace with "stack", "stack instance", or describe the behavior directly.

Edit docs/guides/secrets.md:

1. **Line 29** (comment in shell example): The comment reads: `# 3. Launch any isolated stack. Resolved secrets reach the pod as env only.`. Change to: `# 3. Launch any stack. Resolved secrets reach the pod as env only.`

2. Scan the rest of secrets.md for any other narrative uses of "isolated" or "transparent" and apply the same principle.

In both files: preserve any file path references (`harnessed-isolated.sh`), code blocks, or CLI command examples that happen to contain these words as identifiers.
  </action>
  <verify>
    <automated>rg -n "isolated|transparent" docs/guides/service-authoring.md docs/guides/secrets.md | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "isolated|transparent" docs/guides/service-authoring.md` returns zero matches, OR all remaining matches are inside code blocks/code-spans referencing file paths or function names
    - `rg -n "isolated|transparent" docs/guides/secrets.md` returns zero matches, OR all remaining matches are inside code blocks/code-spans
    - The networking note in service-authoring.md still accurately describes rootless pasta networking (content preserved, only "isolated" removed from the narrative)
    - The secrets.md launch comment still makes sense (step 3 describes launching a stack)
  </acceptance_criteria>
  <done>service-authoring.md and secrets.md have no narrative uses of isolated/transparent; factual content preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor→repo | Documentation edits modify committed markdown files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | docs/guides/*.md | accept | Documentation-only; no code changes. Git history provides audit trail. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Not applicable. |

Note: Documentation-only phase. Threat surface is minimal.
</threat_model>

<verification>
Run after both tasks complete:

```bash
# Final check: narrative isolated/transparent in all guide docs
rg -n "isolated|transparent" docs/guides/troubleshooting.md docs/guides/service-authoring.md docs/guides/secrets.md
# Review: only technical file/function references should remain

# Full phase success-criteria check
rg -rn "isolated|transparent" docs/ README.md CLAUDE.md AGENTS.md | rg -v "harnessed-isolated\.sh\|harnessed_isolated\|harnessed-transparent\.sh\|harnessed_transparent\|harnessed transparent\|Fully isolated" | head -30
# After filtering known-technical references, should be empty or near-empty
```
</verification>

<success_criteria>
- docs/guides/troubleshooting.md: no narrative "isolated instance/stack/mode" — only code-path references to harnessed-isolated.sh remain
- docs/guides/service-authoring.md: line 162 (networking note) uses "stacks" not "isolated stacks"  
- docs/guides/secrets.md: launch comment uses "any stack" not "any isolated stack"
- Running the full rg check across all target files (per ROADMAP success criterion 4) returns only technical identifiers, not narrative descriptions
</success_criteria>

<output>
Create `.planning/phases/11-architecture-documentation/11-04-SUMMARY.md` when done
</output>
