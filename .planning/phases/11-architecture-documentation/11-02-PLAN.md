---
phase: 11-architecture-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/harnessed-design.md
autonomous: true
requirements: [DOC2-01]
must_haves:
  truths:
    - "harnessed-design.md §7 describes recipe = Dockerfile body (not typed-YAML vendor fields), supply chain = pin sources in Dockerfile + scan the derived image"
    - "harnessed-design.md §18 describes the two-oracle approach: deterministic MCP probe + un-primed agent probe with negative control"
    - "§7 mentions ARG HARNESS, body concatenation, no vendoring at recipe level"
    - "§18 mentions the INVALID banner and the negative control decoy capability"
  artifacts:
    - path: "docs/harnessed-design.md"
      provides: "Updated §7 (Dockerfile recipe model) and §18 (two-oracle capability test)"
      contains: "ARG HARNESS"
  key_links:
    - from: "docs/harnessed-design.md §7"
      to: "recipes/gstack/Dockerfile"
      via: "Dockerfile recipe model reference"
      pattern: "Dockerfile.*recipe|recipe.*Dockerfile"
---

<objective>
Rewrite §7 of docs/harnessed-design.md to describe the Phase 8 Dockerfile recipe model (replacing the stale typed-YAML vendor/sync-links description), and rewrite §18 to describe the Phase 10 two-oracle capability test (replacing the v1 single-oracle description). D-NN: per DOC2-01.

Purpose: §7 still describes the v1 model (vendor-plugin, sync-plugin-links, typed-YAML fields) that was replaced in Phase 8. §18 still describes the v1 single-oracle test that was replaced in Phase 10.

Output: Updated docs/harnessed-design.md with accurate §7 and §18.
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/mcrowe/Programming/Personal/code-container/.planning/ROADMAP.md
@/home/mcrowe/Programming/Personal/code-container/docs/harnessed-design.md
@/home/mcrowe/Programming/Personal/code-container/recipes/gstack/Dockerfile
@/home/mcrowe/Programming/Personal/code-container/recipes/gstack/recipe.yaml
@/home/mcrowe/Programming/Personal/code-container/stacks/gstack-time/stack.yaml
@/home/mcrowe/Programming/Personal/code-container/.planning/phases/08-dockerfile-recipe-model-assembler-supply-chain-gate/08-03-SUMMARY.md
@/home/mcrowe/Programming/Personal/code-container/.planning/phases/10-opencode-codex-investigation-combined-capability-test/10-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite §7 for Dockerfile recipe model</name>
  <files>docs/harnessed-design.md</files>
  <read_first>
    docs/harnessed-design.md — read the full current §7 (lines ~127–198, "## 7. Assembly") to understand exactly what to replace
    recipes/gstack/Dockerfile — the canonical Dockerfile recipe artifact: no FROM line, ARG HARNESS, body concatenated by assembler
    recipes/gstack/recipe.yaml — shows harnesses: and expect: fields alongside the Dockerfile
    .planning/phases/08-dockerfile-recipe-model-assembler-supply-chain-gate/08-01-SUMMARY.md — Phase 8 assembler work (read if it exists, skip if missing)
  </read_first>
  <action>
Replace the current §7 body ("## 7. Assembly — split: build-time vendor/sync (committed) + baked images") with a description of the Phase 8 Dockerfile recipe model. The new §7 must cover:

1. **Recipe = Dockerfile body.** A recipe contributes a `Dockerfile` (no `FROM` line) that the assembler concatenates into the derived stack Dockerfile. `ARG HARNESS` at the top of each recipe Dockerfile lets the body reference `${HARNESS}` for harness-specific installs (e.g. `pnpm dlx @gstack/install --host ${HARNESS}`). The assembler strips `ARG HARNESS` lines and emits `FROM harnessed-${HARNESS}:latest` as the header before concatenating bodies.

2. **recipe.yaml declares metadata, not build steps.** The `recipe.yaml` still carries `name`, `description`, `mcp.servers` (for hatago config), and `harnesses:` (compat list) + `expect:` (smoke-check entries). The build steps live in the `Dockerfile`, not in YAML fields like `deps`, `plugins`, or `hooks`. The old `vendor-plugin`/`sync-plugin-links` approach is superseded.

3. **Supply chain = pin sources + scan the derived image.** Recipes pin their sources inside the Dockerfile body (pinned `git clone --branch <tag>`, pinned `pnpm dlx @pkg@<exact-version>`, or `ARG`-driven version pins). The assembler validates that no floating ref (`--branch main`, unversioned `pnpm dlx @pkg`) passes. After building the derived image `harnessed-<stack>`, osv-scanner V2 scans the image (not vendored source files). The "scan the derived image" approach catches transitive CVEs that a source-only scan misses.

4. **Assembler output.** The assembler emits `profiles/<stack>/Dockerfile.harnessed-<stack>` (the concatenated derived Dockerfile) and `profiles/<stack>/hatago.config.json` (from recipe.yaml `mcp.servers` entries). The host then runs `podman build` on the emitted Dockerfile to produce `harnessed-<stack>`.

5. **pnpm everywhere** — the supply-chain policy sub-section stays largely the same; update any references to "vendored plugins" → "recipe Dockerfiles", "vendor-plugin" → Dockerfile pinning.

Keep the heading "## 7. Assembly" but retitle the sub-heading to match the new model. Retain the Scanner credentials sub-section (snyk/Socket.dev policy is unchanged). Remove the old "Prior-art source" table with vendor-plugin/sync-plugin-links, as those are no longer the mechanism.

Do NOT change any other section of harnessed-design.md in this task.
  </action>
  <verify>
    <automated>rg -n "ARG HARNESS|Dockerfile.*recipe|recipe.*Dockerfile|derived.*image|scan.*derived\|image.*scan" docs/harnessed-design.md | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `rg "ARG HARNESS" docs/harnessed-design.md` returns at least one match in §7
    - `rg "vendor-plugin\|sync-plugin-links\|Prior-art source" docs/harnessed-design.md` returns zero matches (old v1 assembly table removed)
    - `rg "scan.*derived\|image.*scan\|harnessed-.*stack" docs/harnessed-design.md` returns at least one match in §7 describing supply chain as scanning the derived image
    - `rg "harnesses:" docs/harnessed-design.md` returns at least one match in §7 (recipe.yaml metadata fields)
    - Section heading `## 7.` still exists in the file
  </acceptance_criteria>
  <done>§7 describes recipe = Dockerfile body with ARG HARNESS, body concatenation, pin-sources discipline, and supply chain = scan derived image. No references to vendor-plugin or sync-plugin-links remain in §7.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite §18 for two-oracle capability test</name>
  <files>docs/harnessed-design.md</files>
  <read_first>
    docs/harnessed-design.md — read the full current §18 (lines ~560–612, "## 18. Proposed: testing") to understand exactly what to replace
    .planning/phases/10-opencode-codex-investigation-combined-capability-test/10-SUMMARY.md — Phase 10 two-oracle implementation (read if exists); also check profiles/gstack-time/capability-report.md if it exists
    tools/uat/phase-05.sh — check for the harnessed test invocation pattern (if it shows the oracle approach)
  </read_first>
  <action>
Replace the current §18 body (starting "## 18. Proposed: testing — integration only, behavior through the instance") with a description of the Phase 10 two-oracle capability test. The new §18 must cover:

1. **Two-oracle approach.** The capability test uses two complementary checks that together give confidence the stack exposes exactly what its manifest declares:
   - **Oracle 1 — Structured MCP probe (deterministic).** Query hatago's `hatago://servers` resource (a JSON snapshot of connected servers) directly — no model call needed. Assert the manifest's `mcp.servers` entries appear in the response. This is fast and deterministic.
   - **Oracle 2 — Un-primed agent probe (behavioral).** Ask the harness, headless, what capabilities it has — deliberately NOT priming it with the expected list. The probe asks the agent to enumerate available skills and MCP servers. Assert declared capabilities appear and a known-absent "decoy" capability does NOT appear.

2. **Negative control (anti-sycophancy gate).** The decoy capability is a name that exists in neither the recipe nor the image. If the agent claims the decoy is present, the test exits non-zero with status INVALID (distinct from a capability-failure non-zero exit). The INVALID banner appears in `capability-report.md`. This catches sycophantic priming — a model hallucinating agreement with a leading question.

3. **Capability report.** After every `harnessed test <stack>` run, `profiles/<stack>/capability-report.md` is written with a per-capability table showing ✓/✗ for each MCP server and `expect:` entry, plus the INVALID banner when priming is detected.

4. **Philosophy unchanged.** Retain the existing rationale: test behavior through the public interface (the running instance), the stack manifest is the test oracle, no assembler unit tests. Update any language that says "proposed" to "implemented."

5. **Remove stale details.** The v1 description asked the harness to "emit the skills/commands it sees as JSON and diff against the manifest" — this is superseded. Remove the old step-by-step list that mentions "ask the harness…to emit skills/commands…as JSON."

Keep "## 18." heading; retitle from "Proposed: testing" to just "Testing — integration only, behavior through the instance."

Do NOT change any other section of harnessed-design.md in this task.
  </action>
  <verify>
    <automated>rg -n "two-oracle\|negative control\|decoy\|INVALID\|Oracle 1\|Oracle 2\|MCP probe\|agent probe" docs/harnessed-design.md | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `rg "two-oracle\|negative control\|decoy\|INVALID" docs/harnessed-design.md` returns at least 3 matches in §18
    - `rg "Oracle 1\|Oracle 2\|MCP probe\|agent probe\|hatago.*servers\|hatago://servers" docs/harnessed-design.md` returns at least one match in §18
    - `rg "emit.*skills.*JSON\|ask.*harness.*emit\|diff.*manifest" docs/harnessed-design.md` returns zero matches (v1 single-oracle step removed)
    - `rg "Proposed: testing" docs/harnessed-design.md` returns zero matches (heading updated)
    - `rg "capability-report" docs/harnessed-design.md` returns at least one match in §18
  </acceptance_criteria>
  <done>§18 describes the two-oracle capability test (structured MCP probe + un-primed agent probe), negative control/anti-sycophancy gate, INVALID status, and capability-report.md output. No v1 single-oracle language remains in §18.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor→repo | Documentation edits modify committed markdown files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Tampering | docs/harnessed-design.md | accept | Documentation-only; no code changes. Git history provides audit trail. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Not applicable. |

Note: Documentation-only phase. Threat surface is minimal.
</threat_model>

<verification>
Run after both tasks complete:

```bash
# Verify §7 Dockerfile model
rg -n "ARG HARNESS|vendor-plugin|sync-plugin-links" docs/harnessed-design.md
# Expected: ARG HARNESS present, vendor-plugin and sync-plugin-links absent

# Verify §18 two-oracle test
rg -n "two-oracle|negative control|INVALID|Proposed: testing" docs/harnessed-design.md
# Expected: two-oracle/negative control/INVALID present, "Proposed: testing" absent

# Check no unintended sections changed
grep -c "^## " docs/harnessed-design.md
# Should match pre-edit count (18 sections)
```
</verification>

<success_criteria>
- §7 describes recipe = Dockerfile body, ARG HARNESS, body concatenation, pin sources, scan derived image — no vendor-plugin/sync-plugin-links
- §18 describes two-oracle test (MCP probe + agent probe with negative control), INVALID status, capability-report.md
- No other sections of harnessed-design.md changed
</success_criteria>

<output>
Create `.planning/phases/11-architecture-documentation/11-02-SUMMARY.md` when done
</output>
