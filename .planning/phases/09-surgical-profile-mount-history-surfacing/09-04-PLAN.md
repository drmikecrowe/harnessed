---
phase: 09-surgical-profile-mount-history-surfacing
plan: 04
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03
files_modified:
  - tools/uat/phase-09.sh
autonomous: true
requirements:
  - MNT2-01
  - MNT2-02
  - MNT2-03
  - MNT2-04
  - MNT2-05
  - MNT2-06

must_haves:
  truths:
    - "tools/uat/phase-09.sh exists and uat_run_phase function is defined"
    - "Smoke tests pass without a container runtime (UAT_QUICK=true path)"
    - "Manifest existence tests cover all 6 harness files"
    - "Profile shape tests verify .mcp.json at profile root and no .claude/ tree after build"
    - "History surfacing integration tests are defined and skip gracefully under --quick"
  artifacts:
    - path: "tools/uat/phase-09.sh"
      provides: "Phase 9 UAT suite"
      contains: "uat_run_phase"
  key_links:
    - from: "tools/uat/run-uat.sh"
      to: "tools/uat/phase-09.sh"
      via: "source"
      pattern: "phase-09\\.sh"
---

<objective>
Write the Phase 9 UAT suite (tools/uat/phase-09.sh) with smoke tests (manifest existence,
profile shape) that pass without a container runtime, and integration test stubs for
history surfacing and path mirroring that skip under --quick / UAT_QUICK=true.

Purpose: Provides automated verification for all Phase 9 requirements (MNT2-01 through
MNT2-06) in the same UAT pattern as prior phases. Smoke tests confirm the structural
invariants without needing a live container; integration tests confirm the runtime
behavior when a container is available.

Output: tools/uat/phase-09.sh with uat_run_phase() entrypoint and 7+ test functions.
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-surgical-profile-mount-history-surfacing/09-CONTEXT.md
@.planning/phases/09-surgical-profile-mount-history-surfacing/09-RESEARCH.md
@.planning/phases/09-surgical-profile-mount-history-surfacing/09-PATTERNS.md
@.planning/phases/09-surgical-profile-mount-history-surfacing/09-01-SUMMARY.md
@.planning/phases/09-surgical-profile-mount-history-surfacing/09-02-SUMMARY.md
@.planning/phases/09-surgical-profile-mount-history-surfacing/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 9 smoke tests (no container required)</name>
  <files>tools/uat/phase-09.sh</files>
  <read_first>
    - tools/uat/phase-04.sh (exact analog: shebang, header, test_ function naming, AAA pattern, run_test calls, uat_run_phase entrypoint, needs_container guard)
    - tools/uat/uat-common.sh (available helpers: assert_exists, assert_not_exists, assert_file_contains, assert_exit_zero, uat_run, run_test, uat_suite, skip_test)
    - tools/uat/phase-08.sh (most recent phase — verify any newer patterns added after phase-04)
    - .planning/phases/09-surgical-profile-mount-history-surfacing/09-RESEARCH.md (Validation Architecture: Phase Requirements → Test Map; Wave 0 Gaps list; test IDs and assertions)
    - .planning/phases/09-surgical-profile-mount-history-surfacing/09-PATTERNS.md (phase-09.sh Pattern: shebang, header, test function list, available uat-common.sh helpers)
  </read_first>
  <action>
    Create tools/uat/phase-09.sh following the exact pattern of tools/uat/phase-04.sh.

    Shebang and header comment: cite phase 9, goal (surgical profile mount + history surfacing),
    note Arrange → Act → Assert pattern.

    Define a `needs_container` guard function (same as phase-04.sh) for skipping integration tests
    when UAT_QUICK=true.

    Implement these SMOKE tests (no container; run even under --quick):

    test_manifests_exist():
      Assert assert_exists for each of the 6 manifest files:
      lib/manifests/claude.yaml, omp.yaml, antigravity.yaml, opencode.yaml, gemini.yaml, codex.yaml.
      Assert assert_file_contains lib/manifests/claude.yaml "profile_files" "claude manifest has profile_files key".
      Assert assert_file_contains lib/manifests/claude.yaml "history_dirs" "claude manifest has history_dirs key".
      Assert assert_file_contains lib/manifests/antigravity.yaml "antigravity-cli" "antigravity manifest references antigravity-cli subdir".

    test_manifest_no_credentials():
      Assert that no manifest file contains credential path patterns.
      For each manifest: assert_not_contains "agent.db" "$(cat lib/manifests/*.yaml)" or use
      individual checks. Must verify: "agent.db", "antigravity-oauth-token", ".credentials.json"
      do not appear in any manifest. Use rg --count pattern check returning 0.

    test_manifest_mounts_helper_exists():
      Assert assert_exists lib/harnessed-manifest-mounts.sh.
      Assert that file contains "harnessed_manifest_mounts" function name:
      assert_file_contains lib/harnessed-manifest-mounts.sh "harnessed_manifest_mounts" "helper defines the function".
      Assert bash -n lib/harnessed-manifest-mounts.sh exits 0 via assert_exit_zero.

    test_launcher_uses_manifest_mounts():
      Assert assert_file_contains lib/harnessed-isolated.sh "harnessed-manifest-mounts.sh" "launcher sources manifest mounts helper".
      Assert assert_file_contains lib/harnessed-isolated.sh "harnessed_manifest_mounts" "launcher calls manifest mounts".
      Assert assert_file_contains lib/harnessed-isolated.sh "profile_dir/.mcp.json" "launcher guard uses .mcp.json".

    test_assembler_no_fanout():
      Assert rg returns 0 matches for "syncer|LinkSyncer|harness_dir|ensure_profile_tree" in
      tools/harnessed/assemble.py and tools/harnessed/emit.py (use assert_exit_zero on
      `rg -c 'syncer|LinkSyncer|harness_dir|ensure_profile_tree' tools/harnessed/assemble.py tools/harnessed/emit.py` with expected exit 1
      — or use `! rg -q` pattern matching the existing codebase style).

    test_profile_shape_after_build():
      Arrange: skip if profiles/gstack-time directory does not exist.
      Assert assert_not_exists profiles/gstack-time/.claude "gstack-time has no .claude/ tree".
      Assert assert_exists profiles/gstack-time/.mcp.json "gstack-time has .mcp.json at profile root".
      Assert assert_exists profiles/gstack-time/settings.json "gstack-time has settings.json at profile root".

    Wire all smoke tests into uat_run_phase. The `uat_run_phase` entrypoint uses `uat_suite`
    to name the suite and calls `run_test` for each test function.
  </action>
  <verify>
    <automated>bash -n tools/uat/phase-09.sh</automated>
  </verify>
  <done>
    `bash -n tools/uat/phase-09.sh` exits 0. File defines `uat_run_phase`. File contains
    test_manifests_exist, test_manifest_no_credentials, test_manifest_mounts_helper_exists,
    test_launcher_uses_manifest_mounts, test_assembler_no_fanout, test_profile_shape_after_build.
    `UAT_QUICK=true bash tools/uat/phase-09.sh 2>&1 | grep -c SKIP` shows 0 skips for smoke tests
    (smoke tests don't call needs_container).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests for history surfacing and path mirroring</name>
  <files>tools/uat/phase-09.sh</files>
  <read_first>
    - tools/uat/phase-09.sh (already written in Task 1 — append integration tests)
    - tools/uat/phase-04.sh (needs_container usage pattern, headless container test pattern, skip_test call)
    - tools/uat/phase-08.sh (most recent integration test patterns — how headless container tests are structured)
    - .planning/phases/09-surgical-profile-mount-history-surfacing/09-RESEARCH.md (Validation Architecture: MNT2-02/03/04/05 integration test descriptions; Pitfall 2/3 for slug assertions)
  </read_first>
  <action>
    Append integration test functions to tools/uat/phase-09.sh and add them to uat_run_phase.
    All integration tests must call `needs_container && { skip_test "skipped (--quick)"; return; }` at the start.

    test_path_mirroring():
      needs_container guard. Arrange: determine HOST_PWD=$(pwd).
      Act: launch a gstack-time instance in headless mode and exec `pwd` inside the container.
      Assert: container pwd output equals $HOST_PWD (path mirroring active).
      Teardown: stop/rm the instance.
      Note: if HARNESSED_HEADLESS or equivalent headless flag is not available, use a brief
      `podman exec <container> pwd` after a background launch. Follow the same pattern as
      phase-08.sh integration tests for headless launch + assertion + teardown.

    test_claude_history_surfaced():
      needs_container guard. Arrange: clear or snapshot $HOME/.claude/projects/ state.
      Act: run a brief headless claude session via gstack-time (enough to create a project entry).
      Assert: assert_exists $HOME/.claude/projects/ "host projects dir exists after session".
      Assert: at least one entry appears inside that dir (use ls or find with count assertion).
      Teardown: stop/rm instance.

    test_omp_history_surfaced():
      needs_container guard. Arrange: determine expected omp slug from HOST relpath
      (omp_slug="-${relpath//\//'-'}"; relpath=$(realpath --relative-to="$HOME" "$(pwd)")).
      Act: run a brief headless omp session.
      Assert: assert_exists "$HOME/.omp/agent/sessions/$omp_slug" "host omp session dir exists".
      Teardown: stop/rm instance.

    test_antigravity_history_surfaced():
      needs_container guard.
      Act: run a brief headless antigravity session.
      Assert: assert_exists "$HOME/.gemini/antigravity-cli/conversations/" "host agy conversations dir exists".
      Teardown: stop/rm instance.

    For all integration tests: if the relevant stack (omp-time, antigravity-time, gstack-time) is
    not built (profile .mcp.json absent), skip_test with a message like
    "skipped — omp-time not built (run: harnessed build omp-time)".

    Add all integration tests to uat_run_phase after the smoke tests.

    Update the uat_run_phase suite header to include a note:
    "Integration tests (history surfacing, path mirroring) require --no-quick and relevant stacks built".
  </action>
  <verify>
    <automated>UAT_QUICK=true bash tools/uat/phase-09.sh 2>&1 | tail -20</automated>
  </verify>
  <done>
    `bash -n tools/uat/phase-09.sh` exits 0. `UAT_QUICK=true bash tools/uat/phase-09.sh`
    runs to completion with all integration tests showing SKIP (not FAIL).
    Smoke tests (test_manifests_exist, test_manifest_no_credentials, test_manifest_mounts_helper_exists,
    test_launcher_uses_manifest_mounts, test_assembler_no_fanout, test_profile_shape_after_build)
    all pass when Phase 9 implementation is in place.
    Integration tests (test_path_mirroring, test_claude_history_surfaced, test_omp_history_surfaced,
    test_antigravity_history_surfaced) are present and skip under UAT_QUICK=true.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT test → host filesystem | Tests read host dirs under ~/.claude/, ~/.omp/, ~/.gemini/ to assert history surfacing; test cleanup must not delete real user data |
| UAT test → container runtime | Integration tests launch/stop containers; failed cleanup could leave orphan containers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-C01 | Tampering | UAT teardown deleting real history | mitigate | Integration tests assert existence of dirs/files; they do NOT delete history dirs; teardown only stops/rm the container instance, not host dirs |
| T-09-C02 | Information Disclosure | omp slug assertion reads real session paths | accept | Test only asserts dir existence at computed slug path; no file contents are read or logged |
| T-09-C03 | Tampering | test_manifest_no_credentials false negative | mitigate | Test uses rg with explicit credential path patterns (agent.db, antigravity-oauth-token, .credentials.json); pattern list is explicit and reviewed during task authoring |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `bash -n tools/uat/phase-09.sh` exits 0
- `UAT_QUICK=true bash tools/uat/phase-09.sh` completes; smoke tests PASS; integration tests SKIP
- Smoke tests pass when Phase 9 implementation is in place:
  `bash tools/uat/phase-09.sh` (with UAT_QUICK unset, no container) runs smoke tests and they PASS
- rg 'uat_run_phase' tools/uat/phase-09.sh returns 1+ match
- All 6 manifest smoke test assertions reference real file paths in lib/manifests/
</verification>

<success_criteria>
tools/uat/phase-09.sh defines uat_run_phase() with 6 smoke tests (no container required)
and 4 integration tests (skip under UAT_QUICK=true). Smoke tests verify: manifests exist,
no credential paths in manifests, manifest-mounts helper exists and passes bash -n,
launcher sources the helper and uses .mcp.json guard, assembler has no fan-out traces,
profile shape after build is correct. Integration tests verify runtime history surfacing
for claude, omp, and antigravity, plus path mirroring. bash -n exits 0.
</success_criteria>

<output>
Create `.planning/phases/09-surgical-profile-mount-history-surfacing/09-04-SUMMARY.md` when done
</output>
