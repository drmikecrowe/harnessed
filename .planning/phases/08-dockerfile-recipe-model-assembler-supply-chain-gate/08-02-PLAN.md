---
phase: 08-dockerfile-recipe-model-assembler-supply-chain-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - recipes/gstack/recipe.yaml
  - recipes/gstack/Dockerfile
  - stacks/gstack-time/stack.yaml
  - recipes/floating-recipe/recipe.yaml
  - recipes/floating-recipe/Dockerfile
  - stacks/floating-test/stack.yaml
  - stacks/omp-gstack-test/stack.yaml
autonomous: true
requirements: [RCP2-01, RCP2-02, RCP2-03, ASM-01, ASM-02]

must_haves:
  truths:
    - "recipes/gstack/recipe.yaml declares harnesses: [claude] and expect: [gstack-skill]"
    - "recipes/gstack/Dockerfile uses pnpm dlx with a pinned version, has no --branch or :latest"
    - "stacks/gstack-time/stack.yaml declares harness: claude and recipes: [gstack, time]"
    - "stacks/floating-test/stack.yaml triggers ASM-02 pin rejection when assembled (references floating-recipe)"
    - "stacks/omp-gstack-test/stack.yaml triggers ASM-01 harness compat rejection when assembled (omp + gstack)"
    - "recipes/floating-recipe/Dockerfile contains --branch main to exercise the pin validation gate"
  artifacts:
    - path: "recipes/gstack/recipe.yaml"
      provides: "claude-only recipe with harnesses/expect fields"
      contains: "harnesses:"
    - path: "recipes/gstack/Dockerfile"
      provides: "pinned recipe Dockerfile (Phase 8 test artifact)"
      contains: "ARG HARNESS"
    - path: "stacks/gstack-time/stack.yaml"
      provides: "test stack for Phase 8 success criteria"
      contains: "gstack"
    - path: "recipes/floating-recipe/Dockerfile"
      provides: "floating-ref rejection fixture"
      contains: "--branch main"
    - path: "stacks/omp-gstack-test/stack.yaml"
      provides: "harness-compat rejection fixture"
      contains: "omp"
  key_links:
    - from: "stacks/gstack-time/stack.yaml"
      to: "recipes/gstack"
      via: "recipes: [gstack, time] — assembler resolves recipes/gstack/ + recipes/time/"
      pattern: "gstack"
    - from: "stacks/floating-test/stack.yaml"
      to: "recipes/floating-recipe/Dockerfile"
      via: "assembler reads floating-recipe/Dockerfile and triggers validate_pin failure"
      pattern: "--branch main"
    - from: "stacks/omp-gstack-test/stack.yaml"
      to: "recipes/gstack/recipe.yaml"
      via: "harness: omp + harnesses: [claude] → validate_harness_compat raises HarnessCompatError"
      pattern: "claude"
---

<objective>
Create the recipe artifacts and test fixtures that Phase 8 uses for both the success criteria
verification (`gstack-time` stack) and the rejection scenario tests (floating-ref and harness-compat
UAT heavy tests).

Purpose: These are pure data files (YAML + Dockerfile) with no code dependencies; they can be
authored in parallel with Plan 08-01. The `gstack` recipe is a minimal test artifact that exercises
the new Dockerfile recipe model. The rejection fixtures prove the pre-emission validation gates work.

Output: Seven new files — one recipe pair (gstack), one stack (gstack-time), two rejection
test fixtures (floating-recipe + floating-test stack, omp-gstack-test stack).
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-dockerfile-recipe-model-assembler-supply-chain-gate/08-RESEARCH.md
@.planning/phases/08-dockerfile-recipe-model-assembler-supply-chain-gate/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: gstack recipe + gstack-time stack</name>
  <files>recipes/gstack/recipe.yaml, recipes/gstack/Dockerfile, stacks/gstack-time/stack.yaml</files>
  <read_first>
    recipes/time/recipe.yaml (analog for field ordering: name, description, then mcp/skills/commands)
    tools/test-fixtures/stacks/tracer-time/stack.yaml (analog for stack.yaml field ordering)
    .planning/phases/08-dockerfile-recipe-model-assembler-supply-chain-gate/08-PATTERNS.md
    (gstack recipe.yaml and Dockerfile sections — exact field content and Dockerfile format)
    .planning/phases/08-dockerfile-recipe-model-assembler-supply-chain-gate/08-RESEARCH.md
    (Sample gstack recipe.yaml and Dockerfile code examples — confirms field values)
  </read_first>
  <action>
    Create three files:

    recipes/gstack/recipe.yaml:
    - Header comment marking it as Phase 8 test artifact (claude-only)
    - Fields: name (gstack), description, harnesses ([claude]), expect ([gstack-skill])
    - No mcp: section — this recipe only installs via its Dockerfile, no MCP server
    - Follow field ordering from recipes/time/recipe.yaml for non-new fields

    recipes/gstack/Dockerfile:
    - Header comment: "Recipe Dockerfile for: gstack (Phase 8 test artifact)"
    - Comment explaining ARG HARNESS is stripped by assembler but needed for standalone builds
    - `ARG HARNESS=claude` as first instruction (assembler strips this line during concatenation)
    - One RUN instruction using `pnpm dlx @gstack/install --host ${HARNESS} --version 1.2.3`
    - CRITICAL: use `--version 1.2.3` (pinned) — NOT `--branch main` or `:latest` (would trip ASM-02)
    - CRITICAL: use `pnpm dlx` — NOT `npm` or `npx` (would trip BLD-03)
    - No FROM line (assembler provides the canonical `FROM harnessed-${HARNESS}:latest` in the header)

    stacks/gstack-time/stack.yaml:
    - Fields: name (gstack-time), harness (claude), recipes ([gstack, time])
    - Follow field ordering from the tracer-time analog
  </action>
  <verify>
    <automated>rg "harnesses:" /home/mcrowe/Programming/Personal/code-container/recipes/gstack/recipe.yaml && rg "expect:" /home/mcrowe/Programming/Personal/code-container/recipes/gstack/recipe.yaml && rg "pnpm dlx" /home/mcrowe/Programming/Personal/code-container/recipes/gstack/Dockerfile && rg "gstack" /home/mcrowe/Programming/Personal/code-container/stacks/gstack-time/stack.yaml && echo "gstack artifacts: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `recipes/gstack/recipe.yaml` exists and contains `harnesses:` with value `[claude]`
    - `recipes/gstack/recipe.yaml` exists and contains `expect:` with value `[gstack-skill]`
    - `recipes/gstack/Dockerfile` exists and contains `pnpm dlx` (not npm/npx)
    - `recipes/gstack/Dockerfile` does NOT contain `--branch` or `:latest`
    - `recipes/gstack/Dockerfile` does NOT contain a `FROM` line (assembler provides it)
    - `stacks/gstack-time/stack.yaml` exists and contains `gstack` in the recipes list
    - `stacks/gstack-time/stack.yaml` declares `harness: claude`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rejection test fixtures — floating-recipe + omp-gstack-test</name>
  <files>
    recipes/floating-recipe/recipe.yaml,
    recipes/floating-recipe/Dockerfile,
    stacks/floating-test/stack.yaml,
    stacks/omp-gstack-test/stack.yaml
  </files>
  <read_first>
    recipes/gstack/recipe.yaml (just created — use same field ordering for floating-recipe.yaml)
    stacks/gstack-time/stack.yaml (just created — use same field ordering for test stacks)
    .planning/phases/08-dockerfile-recipe-model-assembler-supply-chain-gate/08-PATTERNS.md
    (Phase-08 UAT section — describes test_pin_validation_rejects_floating and test_harness_compat_rejected
    tests; these fixtures are what those tests use)
    .planning/phases/08-dockerfile-recipe-model-assembler-supply-chain-gate/08-RESEARCH.md
    (ASM-02 Pin-Validation Error example; ASM-01 Harness-Compat Error example)
  </read_first>
  <action>
    Create four files:

    recipes/floating-recipe/recipe.yaml:
    - Header comment: "Phase 8 test fixture — triggers ASM-02 pin validation rejection"
    - Fields: name (floating-recipe), description, harnesses ([claude])
    - No expect, no mcp — this recipe exists only to trigger the pin validation gate

    recipes/floating-recipe/Dockerfile:
    - Header comment: "Phase 8 test fixture — contains a floating --branch ref (triggers ASM-02 PinValidationError)"
    - `ARG HARNESS=claude` (first line)
    - One RUN instruction using `git clone --branch main https://example.com/fake-repo.git /opt/fake`
    - The `--branch main` fragment is the floating ref that `validate_pin()` must catch
    - IMPORTANT: use a clearly fake URL so the Dockerfile cannot accidentally be used in a real build

    stacks/floating-test/stack.yaml:
    - Header comment: "Phase 8 test fixture — references floating-recipe to trigger ASM-02 at build time"
    - Fields: name (floating-test), harness (claude), recipes ([floating-recipe])

    stacks/omp-gstack-test/stack.yaml:
    - Header comment: "Phase 8 test fixture — harness: omp + claude-only gstack recipe → ASM-01 rejection"
    - Fields: name (omp-gstack-test), harness (omp), recipes ([gstack])
    - When assembled, gstack.harnesses = [claude] but stack.harness = omp → HarnessCompatError
  </action>
  <verify>
    <automated>rg "\-\-branch main" /home/mcrowe/Programming/Personal/code-container/recipes/floating-recipe/Dockerfile && rg "omp" /home/mcrowe/Programming/Personal/code-container/stacks/omp-gstack-test/stack.yaml && rg "floating-recipe" /home/mcrowe/Programming/Personal/code-container/stacks/floating-test/stack.yaml && echo "rejection fixtures: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `recipes/floating-recipe/Dockerfile` exists and contains `--branch main` (the floating ref)
    - `recipes/floating-recipe/recipe.yaml` exists with `name: floating-recipe`
    - `stacks/floating-test/stack.yaml` exists with `recipes: [floating-recipe]` and `harness: claude`
    - `stacks/omp-gstack-test/stack.yaml` exists with `harness: omp` and `recipes: [gstack]`
    - Neither `recipes/floating-recipe/Dockerfile` nor `stacks/*.yaml` files contain real package names
      or real registry URLs that could be accidentally executed in a production build
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed recipe Dockerfiles → assembler | Recipe Dockerfiles are read by the assembler; test fixtures are intentionally malformed to exercise gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-F1 | Tampering | floating-recipe/Dockerfile accidentally executed in production | accept | File uses `https://example.com/fake-repo.git` (unreachable); header comment marks it as Phase 8 test fixture; assembler gate (ASM-02) would reject it anyway |
| T-08-F2 | Spoofing | omp-gstack-test accidentally launched as a real stack | accept | Stack name ends in `-test`; harness-compat gate (ASM-01) rejects it before any image is built; serves only as a rejection fixture |
| T-08-SC | Tampering | npm/pip/cargo installs in fixture Dockerfiles | accept | floating-recipe uses `git clone` only (no package manager); gstack uses `pnpm dlx` (compliant); no new packages enter the store |
</threat_model>

<verification>
```bash
rg "harnesses: \[claude\]" /home/mcrowe/Programming/Personal/code-container/recipes/gstack/recipe.yaml && \
rg "expect: \[gstack-skill\]" /home/mcrowe/Programming/Personal/code-container/recipes/gstack/recipe.yaml && \
rg "pnpm dlx" /home/mcrowe/Programming/Personal/code-container/recipes/gstack/Dockerfile && \
test ! -f /home/mcrowe/Programming/Personal/code-container/recipes/gstack/Dockerfile && echo "no FROM in gstack Dockerfile" || rg "^FROM" /home/mcrowe/Programming/Personal/code-container/recipes/gstack/Dockerfile && echo "ERROR: FROM found" || true && \
rg "\-\-branch main" /home/mcrowe/Programming/Personal/code-container/recipes/floating-recipe/Dockerfile && \
rg "harness: omp" /home/mcrowe/Programming/Personal/code-container/stacks/omp-gstack-test/stack.yaml && \
echo "all fixtures verified"
```
</verification>

<success_criteria>
- gstack recipe correctly declares `harnesses: [claude]` and `expect: [gstack-skill]`
- gstack Dockerfile uses pnpm dlx with a pinned version; no FROM, no floating refs
- gstack-time stack references both gstack and time recipes with harness claude
- floating-recipe Dockerfile contains `--branch main` (exercises ASM-02 gate in UAT)
- floating-test stack references floating-recipe (UAT can call `harnessed build floating-test` to assert rejection)
- omp-gstack-test stack uses harness omp with recipes [gstack] (UAT can assert HarnessCompatError)
- All files are clearly commented as Phase 8 test artifacts
</success_criteria>

<output>
Create `.planning/phases/08-dockerfile-recipe-model-assembler-supply-chain-gate/08-02-SUMMARY.md` when done
</output>
