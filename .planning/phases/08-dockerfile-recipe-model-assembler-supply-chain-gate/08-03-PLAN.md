---
phase: 08-dockerfile-recipe-model-assembler-supply-chain-gate
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - lib/harnessed-common.sh
  - tools/uat/phase-08.sh
autonomous: true
requirements: [IMG-03, SC-01, SC-02, SC-03, SC-04]

must_haves:
  truths:
    - "`harnessed build gstack-time` emits profiles/gstack-time/Dockerfile.harnessed-gstack-time (ASM-03 artifact)"
    - "`harnessed build gstack-time` builds harnessed-gstack-time:latest and runs osv-scanner on it (SC-01)"
    - "`harnessed build gstack-time` runs scan-snyk-container in the tools container (warns-and-skips without SNYK_TOKEN)"
    - "`harnessed build floating-test` exits nonzero before any Dockerfile is emitted (ASM-02 gate)"
    - "`harnessed build omp-gstack-test` exits nonzero before any Dockerfile is emitted (ASM-01 gate)"
    - "`podman images --filter reference='harnessed-*'` lists harnessed-gstack-time:latest after a successful build"
    - "`tools/uat/run-uat.sh 08 --quick` passes all fast manifest-assertion tests"
  artifacts:
    - path: "lib/harnessed-common.sh"
      provides: "IMG-03 derived image build + SC-01 osv scan + SC-03 snyk container test in build_stack()"
      contains: "scan-snyk-container"
    - path: "tools/uat/phase-08.sh"
      provides: "Phase 8 UAT suite with fast + heavy tests"
      contains: "uat_run_phase"
  key_links:
    - from: "lib/harnessed-common.sh build_stack()"
      to: "profiles/<stack>/Dockerfile.harnessed-<stack>"
      via: "`podman build -t harnessed-<stack> -f <derived_dockerfile> <ROOT>` after assembler emits it"
      pattern: "derived_dockerfile"
    - from: "lib/harnessed-common.sh build_stack()"
      to: "tools/harnessed/cli.py scan-snyk-container"
      via: "`$CONTAINER_RUNTIME run $HARNESSED_TOOLS_IMAGE scan-snyk-container $derived_image`"
      pattern: "scan-snyk-container"
    - from: "lib/harnessed-rescan.sh"
      to: "harnessed-gstack-time:latest"
      via: "`podman images --filter reference='harnessed-*'` — naming convention matches existing filter"
      pattern: "harnessed-*"
---

<objective>
Wire the derived image build pipeline into `build_stack()` in `harnessed-common.sh` (IMG-03 + SC-01
+ SC-03 + SC-04) and author the Phase 8 UAT suite that validates all eleven requirements.

Purpose: Plan 08-01 delivers the Python building blocks; Plan 08-02 delivers the test artifacts;
this plan wires them into the host bash pipeline and adds observability through the UAT suite.
After this plan, `harnessed build gstack-time` runs the full new pipeline end-to-end.

Output: `lib/harnessed-common.sh` extended with the derived image block; `tools/uat/phase-08.sh`
with fast file-assertion tests and heavy build/scan tests.
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-dockerfile-recipe-model-assembler-supply-chain-gate/08-RESEARCH.md
@.planning/phases/08-dockerfile-recipe-model-assembler-supply-chain-gate/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: build_stack() extension — IMG-03 + SC-01 + SC-03 + SC-04</name>
  <files>lib/harnessed-common.sh</files>
  <read_first>
    lib/harnessed-common.sh (full build_stack() function — lines ~115–191; identify:
      the final `print_success "Stack '$stack' assembled..."` line at the END of build_stack(),
      the `img_tar / img_rc` hatago image scan block above it,
      the existing `TOKEN_ARGS` array construction,
      the `$(rt_userns_args)` pattern used in run --rm calls,
      the `$HARNESSED_TOOLS_IMAGE` image name variable,
      the `$ROOT` variable holding the repo root)
    .planning/phases/08-dockerfile-recipe-model-assembler-supply-chain-gate/08-PATTERNS.md
    (harnessed-common.sh section — full IMG-03 + SC-01 + SC-03 bash block with variable names,
    the print_success update, and the SC-04 comment)
    .planning/phases/08-dockerfile-recipe-model-assembler-supply-chain-gate/08-RESEARCH.md
    (Pattern 5 + Pitfalls 4, 5, 6, 7 — ARG HARNESS extraction, set -euo pipefail safe exit, naming)
  </read_first>
  <action>
    Replace the final `print_success "Stack '$stack' assembled..."` line at the bottom of
    `build_stack()` with the new derived image block plus an updated print_success. Insert
    AFTER the `rm -f "$img_tar"` + `if [ "$img_rc" -ne 0 ]` block that closes the hatago scan.

    The new block (in order):

    1. [IMG-03] Derive local variables:
       `local derived_image derived_dockerfile stack_harness`
       `derived_image="harnessed-${stack}:latest"`
       `derived_dockerfile="$ROOT/profiles/$stack/Dockerfile.harnessed-${stack}"`
       Extract stack_harness by reading the emitted Dockerfile (no yq on host — pure bash):
       `stack_harness="$(sed -n 's/^ARG HARNESS=//p' "$derived_dockerfile" | head -1)"`
       `stack_harness="${stack_harness:-claude}"` (claude fallback if grep returns empty)
       Guard the whole derived-image block with `if [ -f "$derived_dockerfile" ]; then ... fi`
       so stacks without a recipe Dockerfile (backward-compat) skip the derived image build.

    2. Inside the guard — build the derived image:
       `print_info "Building $derived_image for stack '$stack' ..."`
       `"$CONTAINER_RUNTIME" build --build-arg "HARNESS=${stack_harness}" -t "$derived_image" -f "$derived_dockerfile" "$ROOT"`
       CRITICAL: use `--build-arg HARNESS=...` to override the ARG default at build time (per RESEARCH
       Pitfall 4). The `$ROOT` context dir must be the repo root so COPY instructions can reach recipe files.

    3. [SC-01] osv-scanner image scan — mirrors the existing hatago scan block exactly:
       `print_info "Running supply-chain image scan for $derived_image ..."`
       `local derived_tar derived_img_rc=0`
       `derived_tar="$(mktemp --suffix=.tar)"`
       `"$CONTAINER_RUNTIME" save "$derived_image" -o "$derived_tar"`
       `"$CONTAINER_RUNTIME" run --rm -v "$derived_tar":"$derived_tar":ro "$HARNESSED_TOOLS_IMAGE" scan-image "$derived_tar" || derived_img_rc=$?`
       `rm -f "$derived_tar"`
       `if [ "$derived_img_rc" -ne 0 ]; then print_error "supply-chain image scan failed for $derived_image (HIGH+ finding)"; return 1; fi`
       Use `|| derived_img_rc=$?` (safe-exit-capture) — NOT a bare pipeline — to avoid aborting under
       `set -euo pipefail` on osv-scanner exit 1 (per RESEARCH Pitfall 5).

    4. [SC-03] Snyk container test — token-gated, warn-and-skip:
       `local snyk_rc=0`
       `"$CONTAINER_RUNTIME" run --rm $(rt_userns_args) "${TOKEN_ARGS[@]}" "$HARNESSED_TOOLS_IMAGE" scan-snyk-container "$derived_image" || snyk_rc=$?`
       `if [ "$snyk_rc" -ne 0 ]; then print_error "snyk container test found HIGH+ finding in $derived_image"; return 1; fi`
       Note: `SNYK_TOKEN` absent → `scan-snyk-container` exits 0 (warned internally); absent daemon
       socket inside tools container → snyk exits 2/3 → `scan-snyk-container` exits 0 (warned internally).
       Either way `snyk_rc` stays 0 unless a real HIGH+ finding is found (SC-03 contract).

    5. [SC-04] Comment only — no new code:
       `# [SC-04] Socket source scan of recipe dirs (socket CLI has no container mode — Pitfall 7).`
       `# SC-04 is satisfied by the existing BLD-02a source scan above, which already covers recipe`
       `# directories that contributed to the derived image. socket scan create does not support`
       `# container image layers; source-level analysis is equivalent for pre-build manifests.`

    6. Close the `fi` guard.

    7. Replace (do NOT keep) the old `print_success "Stack '$stack' assembled → profiles/$stack/ + $HARNESSED_HATAGO_IMAGE (scans clean)"` with:
       `print_success "Stack '$stack' assembled → profiles/$stack/ + $HARNESSED_HATAGO_IMAGE${derived_image:+ + $derived_image} (scans clean)"`
       The `${derived_image:+ + $derived_image}` expansion appends the derived image name only when
       the variable is set (i.e., only when a Dockerfile was found and the image was built).
       Place this print_success OUTSIDE and AFTER the closing `fi` of the derived-image guard.
  </action>
  <verify>
    <automated>rg "scan-snyk-container" /home/mcrowe/Programming/Personal/code-container/lib/harnessed-common.sh && rg "derived_dockerfile" /home/mcrowe/Programming/Personal/code-container/lib/harnessed-common.sh && rg "SC-01\|SC-03\|SC-04\|IMG-03" /home/mcrowe/Programming/Personal/code-container/lib/harnessed-common.sh && echo "build_stack extension: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/harnessed-common.sh` contains `scan-snyk-container` (SC-03 invocation)
    - `lib/harnessed-common.sh` contains `derived_dockerfile` variable
    - `lib/harnessed-common.sh` contains `derived_img_rc=0` (safe exit capture for SC-01)
    - `lib/harnessed-common.sh` contains `SC-04` comment documenting the existing source scan coverage
    - `lib/harnessed-common.sh` contains `--build-arg "HARNESS=` in the derived image build command
    - The derived image block is inside a `if [ -f "$derived_dockerfile" ]; then` guard (backward-compat)
    - The old `print_success` line referencing only `$HARNESSED_HATAGO_IMAGE` is replaced with the
      version that conditionally appends `$derived_image`
    - `bash -n lib/harnessed-common.sh` exits 0 (no syntax errors)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Phase 8 UAT suite</name>
  <files>tools/uat/phase-08.sh</files>
  <read_first>
    tools/uat/phase-06.sh (full file — absorb the file header pattern, RT/needs_container helpers,
    fast leg test structure, heavy leg test structure, uat_run_phase entrypoint, skip_test usage)
    tools/uat/uat-common.sh (lines 1–120 — absorb assert_exists, assert_file_contains,
    assert_exit_zero, assert_exit_nonzero, assert_contains, skip_test, arrange/act/assert markers,
    uat_run call signature)
    tools/uat/run-uat.sh (lines 1–55 — confirm that `tools/uat/phase-08.sh` is auto-discovered by
    the `SUITE="$HERE/phase-$PHASE.sh"` pattern; no registration change needed)
    .planning/phases/08-dockerfile-recipe-model-assembler-supply-chain-gate/08-PATTERNS.md
    (UAT section — test function skeletons for all eight tests)
  </read_first>
  <action>
    Create tools/uat/phase-08.sh as a new file. Follow the phase-06.sh structure exactly.

    File header (bash shebang + multiline comment):
    - Script description: Phase 8 UAT — Dockerfile Recipe Model + Assembler + Supply-Chain Gate
    - Note: fast tests (manifest / validation) always run; heavy tests (container build + scan) self-skip under --quick
    - Note: heavy rejection tests (floating-test, omp-gstack-test) require tools image to be built

    Preamble:
    - `RT="$(uat_runtime)"` — podman|docker|""
    - `needs_container() { [ "$UAT_QUICK" = "true" ]; }` — true ⇒ heavy test should skip

    Fast tests (ALWAYS run, no container):

    test_recipe_structure():
    - Checks RCP2-01/02/03: assert_exists on gstack/recipe.yaml, gstack/Dockerfile
    - assert_file_contains gstack/recipe.yaml "harnesses:" "RCP2-02: harnesses field present"
    - assert_file_contains gstack/recipe.yaml "expect:" "RCP2-03: expect field present"
    - assert_file_contains gstack/recipe.yaml "claude" "RCP2-02: gstack declares claude harness"
    - assert_file_contains gstack/Dockerfile "pnpm dlx" "RCP2-01: recipe Dockerfile uses pnpm dlx"
    - assert_file_contains stacks/gstack-time/stack.yaml "gstack" "RCP2-01: stack references gstack recipe"

    test_fixtures_exist():
    - Checks fixtures from Plan 08-02 are present:
    - assert_exists recipes/floating-recipe/Dockerfile "ASM-02 fixture: floating-recipe Dockerfile ships"
    - assert_file_contains recipes/floating-recipe/Dockerfile "--branch main" "ASM-02 fixture: contains floating ref"
    - assert_exists stacks/floating-test/stack.yaml "ASM-02 fixture: floating-test stack ships"
    - assert_file_contains stacks/omp-gstack-test/stack.yaml "omp" "ASM-01 fixture: omp harness declared"

    test_rescan_filter_coverage():
    - Checks SC-02: the naming convention `harnessed-gstack-time:latest` matches `harnessed-*`
    - Arrange: define the derived image name
    - Act: match it against the filter pattern using bash [[ =~ ]]
    - Assert: `[[ "harnessed-gstack-time:latest" =~ ^harnessed- ]]` and assert_exit_zero $? "SC-02: harnessed-gstack-time matches harnessed-* filter"
    - Also assert the rescan script exists: assert_exists lib/harnessed-rescan.sh "SC-02: rescan script ships"
    - Also assert the filter is present: assert_file_contains lib/harnessed-rescan.sh "harnessed-\*" "SC-02: rescan uses harnessed-* filter"

    test_socket_source_scan_coverage():
    - Checks SC-04: socket source scan covers recipe dirs via existing BLD-02a invocation (fast file assertions)
    - Assert the SC-04 comment is present in harnessed-common.sh (documents coverage rationale):
      assert_file_contains lib/harnessed-common.sh "SC-04" "SC-04: coverage comment present in build_stack()"
    - Assert that scan.py is token-gated for SOCKET_SECURITY_API_KEY (Python module warn-skip behavior):
      assert_file_contains tools/harnessed/scan.py "SOCKET_SECURITY_API_KEY" "SC-04: socket scan is token-gated warn-skip in scan.py"
    - Assert that BLD-02a source scan references recipe paths or socket invocation is present:
      assert_file_contains lib/harnessed-common.sh "socket" "SC-04: BLD-02a socket scan wired in build_stack()"
    Note: SC-04 is satisfied by BLD-02a's existing source scan covering recipe dirs (socket CLI has
    no container-image mode). This test verifies the wiring and documentation are correct without
    needing a live socket token or container.

    Heavy tests (self-skip under --quick):

    test_derived_image_build():
    - needs_container && { skip_test "skipped (--quick) — builds harnessed-gstack-time derived image"; return; }
    - [ -z "$RT" ] && { skip_test "no container runtime found"; return; }
    - Arrange: clean up stale image if exists (`$RT rmi harnessed-gstack-time:latest 2>/dev/null || true`)
    - Act: `uat_run "$HARNESSED" build gstack-time`
    - Assert: assert_exit_zero "$UAT_RC" "IMG-03: harnessed build gstack-time exits 0"
    - assert_true "$RT" image inspect harnessed-gstack-time:latest "IMG-03: harnessed-gstack-time:latest image exists"
    - assert_exists "$HARNESSED_DIR/profiles/gstack-time/Dockerfile.harnessed-gstack-time" "ASM-03: derived Dockerfile emitted"
    - assert_file_contains "$HARNESSED_DIR/profiles/gstack-time/Dockerfile.harnessed-gstack-time" "ARG HARNESS" "ASM-03: ARG HARNESS in derived Dockerfile"

    test_snyk_container_skip():
    - needs_container && { skip_test "skipped (--quick) — requires built image"; return; }
    - [ -z "$RT" ] && { skip_test "no container runtime found"; return; }
    - Arrange: ensure gstack-time is built (run `"$HARNESSED" build gstack-time` silently first as a precondition)
    - Act: `uat_run env -u SNYK_TOKEN "$HARNESSED" build gstack-time`
    - Assert: assert_exit_zero "$UAT_RC" "SC-03: build succeeds (snyk warns-and-skips without SNYK_TOKEN)"
    - assert_contains "SNYK_TOKEN" "$UAT_OUT" "SC-03: output mentions SNYK_TOKEN when skipping"

    test_pin_validation_rejection():
    - needs_container && { skip_test "skipped (--quick) — requires tools image for assembler"; return; }
    - [ -z "$RT" ] && { skip_test "no container runtime found"; return; }
    - Act: `uat_run "$HARNESSED" build floating-test`
    - Assert: assert_exit_nonzero "$UAT_RC" "ASM-02: floating ref rejected (build exits nonzero)"
    - assert_contains "floating ref" "$UAT_OUT" "ASM-02: error message names the floating ref"
    - Also assert the derived Dockerfile was NOT emitted (rejection is pre-emission):
      `assert_not_exists "$HARNESSED_DIR/profiles/floating-test/Dockerfile.harnessed-floating-test" "ASM-02: Dockerfile NOT emitted on rejection"`

    test_harness_compat_rejection():
    - needs_container && { skip_test "skipped (--quick) — requires tools image for assembler"; return; }
    - [ -z "$RT" ] && { skip_test "no container runtime found"; return; }
    - Act: `uat_run "$HARNESSED" build omp-gstack-test`
    - Assert: assert_exit_nonzero "$UAT_RC" "ASM-01: incompatible harness rejected (build exits nonzero)"
    - assert_contains "gstack" "$UAT_OUT" "ASM-01: error message names the recipe"
    - assert_not_exists "$HARNESSED_DIR/profiles/omp-gstack-test/Dockerfile.harnessed-omp-gstack-test" "ASM-01: Dockerfile NOT emitted on rejection"

    uat_run_phase() entrypoint:
    - `uat_suite "Phase 8 — Dockerfile Recipe Model + Assembler + Supply-Chain Gate"`
    - Print launcher + runtime + quick-mode status
    - run_test recipe_structure "RCP2-01/02/03: gstack recipe structure (fast, no container)"
    - run_test fixtures_exist "ASM-01/02 fixtures: rejection test files ship (fast, no container)"
    - run_test rescan_filter_coverage "SC-02: harnessed-* filter covers harnessed-gstack-time (fast)"
    - run_test socket_source_scan_coverage "SC-04: socket source scan covers recipe dirs via BLD-02a (fast)"
    - run_test derived_image_build "IMG-03+ASM-03+SC-01: harnessed build gstack-time → image + Dockerfile (heavy)"
    - run_test snyk_container_skip "SC-03: snyk warns-and-skips without SNYK_TOKEN (heavy)"
    - run_test pin_validation_rejection "ASM-02: floating ref rejected before emission (heavy)"
    - run_test harness_compat_rejection "ASM-01: incompatible harness rejected before emission (heavy)"

    File paths in assertions must use `$HARNESSED_DIR/` prefix (the repo root, set by run-uat.sh).
    All path checks for recipes, stacks, and profiles use `$HARNESSED_DIR/` prefix.
  </action>
  <verify>
    <automated>cd /home/mcrowe/Programming/Personal/code-container && tools/uat/run-uat.sh 08 --quick</automated>
  </verify>
  <acceptance_criteria>
    - `tools/uat/run-uat.sh 08 --quick` exits 0 with all fast tests passing
    - Fast test `recipe_structure` passes: asserts harnesses/expect fields present in gstack recipe.yaml
    - Fast test `fixtures_exist` passes: asserts floating-recipe Dockerfile contains `--branch main`
    - Fast test `rescan_filter_coverage` passes: asserts harnessed-gstack-time matches harnessed-* pattern
    - Fast test `socket_source_scan_coverage` passes: asserts SC-04 comment in harnessed-common.sh, SOCKET_SECURITY_API_KEY in scan.py, and socket wiring present (SC-04 satisfaction via BLD-02a source scan)
    - Heavy tests are skipped (not failed) under --quick
    - `uat_run_phase` function exists and is the file's entry point
    - `bash -n tools/uat/phase-08.sh` exits 0 (no syntax errors)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host bash → tools container (scan-snyk-container) | Image name passed to scan-snyk-container subcommand; handled as a string argument, not executed |
| tools container → snyk CLI | snyk container test receives image name; snyk daemon access is optional (exit 2/3 → warning) |
| host bash → podman build | Emitted Dockerfile from assembler (machine-generated, not user-supplied) used as build file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering / EoP | HIGH-severity CVE in derived image layer reaches running container | mitigate | osv-scanner V2 image scan (SC-01) gates `harnessed build <stack>` on HIGH+ findings; `derived_img_rc` check aborts with non-zero exit |
| T-08-05 | Tampering | supply-chain attack on recipe installer package in derived image undetected | mitigate | snyk container test (SC-03) when SNYK_TOKEN present; osv-scanner baseline always runs; warn-and-skip when no token rather than silent pass |
| T-08-06 | Tampering | osv-scanner exit 1 (finding) causes `set -euo pipefail` abort before print_error | mitigate | `|| derived_img_rc=$?` safe-exit-capture pattern (same as existing `img_rc` for hatago scan) prevents pipeline abort |
| T-08-07 | Information Disclosure | SNYK_TOKEN forwarded to tools container via TOKEN_ARGS for source scan; same TOKEN_ARGS reused for container scan | accept | TOKEN_ARGS already established for source scan (BLD-02a); reuse is intentional and consistent; token is injected as env, never written to disk |
| T-08-SC | Tampering | npm/pip/cargo installs during `podman build` of derived image | mitigate | recipe Dockerfiles gated by pin validation (ASM-02) and pnpm-everywhere lint (BLD-03) before build; osv-scanner scans the built image (SC-01) |
</threat_model>

<verification>
After both tasks complete:

```bash
# Syntax check
bash -n /home/mcrowe/Programming/Personal/code-container/lib/harnessed-common.sh && echo "harnessed-common.sh: syntax ok"
bash -n /home/mcrowe/Programming/Personal/code-container/tools/uat/phase-08.sh && echo "phase-08.sh: syntax ok"

# Fast UAT (no container needed)
cd /home/mcrowe/Programming/Personal/code-container && tools/uat/run-uat.sh 08 --quick

# Verify key anchors
rg "scan-snyk-container\|derived_dockerfile\|IMG-03\|SC-01\|SC-03\|SC-04" lib/harnessed-common.sh
```
</verification>

<success_criteria>
- `bash -n lib/harnessed-common.sh` exits 0
- `bash -n tools/uat/phase-08.sh` exits 0
- `tools/uat/run-uat.sh 08 --quick` exits 0 with all four fast tests passing
- `lib/harnessed-common.sh` contains the IMG-03 derived image build block guarded by `if [ -f "$derived_dockerfile" ]`
- `lib/harnessed-common.sh` contains the SC-01 `derived_img_rc` safe-exit-capture pattern
- `lib/harnessed-common.sh` contains the SC-03 `scan-snyk-container` invocation with `TOKEN_ARGS`
- `lib/harnessed-common.sh` contains the SC-04 comment documenting source-scan satisfaction
- `lib/harnessed-common.sh` final print_success conditionally includes derived_image name
- `tools/uat/phase-08.sh` covers RCP2-01/02/03, ASM-01, ASM-02, ASM-03, IMG-03, SC-01, SC-02, SC-03, SC-04 in labeled run_test calls
- SC-04: fast UAT test `socket_source_scan_coverage` asserts SC-04 comment in harnessed-common.sh and SOCKET_SECURITY_API_KEY token-gate in scan.py (proving BLD-02a socket warn-skip wiring is in place)
</success_criteria>

<output>
Create `.planning/phases/08-dockerfile-recipe-model-assembler-supply-chain-gate/08-03-SUMMARY.md` when done
</output>
