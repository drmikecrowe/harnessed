---
phase: 01-containerized-engine-transparent-stack
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - stacks/transparent/stack.yaml
  - tools/harnessed/stack.py
  - tools/harnessed/transparent.py
  - tools/harnessed/claude_config.py
  - tools/harnessed/cli.py
  - container
autonomous: false
requirements: [MODE-01, MODE-02, AUTH-01, MNT-03]
must_haves:
  truths:
    - "`harnessed transparent [path]` launches a harness with host ~/.claude (+ .codex/.config/opencode/.gemini) mounted live and the project mounted"
    - "`container [path]` produces identical behavior to `harnessed transparent [path]` (thin alias)"
    - "The instance is authenticated from the live host config (no re-login)"
    - "Host ~/.claude.json is never rw-bind-mounted; the instance uses a copy-on-start per-instance copy and host ~/.claude.json is unchanged after a run"
  artifacts:
    - "stacks/transparent/stack.yaml — the built-in transparent stack manifest"
    - "tools/harnessed/transparent.py — transparent-mode mount assembly + run"
    - "tools/harnessed/claude_config.py — copy-on-start .claude.json (+ CLAUDE_CONFIG_DIR scope probe)"
    - "container — thin alias → harnessed transparent"
  key_links:
    - "transparent.py composes host_integration (01-02) + claude_config copy-on-start + live host config mounts"
    - "container alias forwards all existing flags to `harnessed transparent`"
---

<objective>
Deliver the `transparent` stack — today's `container` behavior through the harnessed engine — with the
`~/.claude.json` safety fix, and retain `container` as a thin alias.

Purpose: Zero-regression continuity for existing muscle memory plus the §4b corruption fix; transparent is the
degenerate stack that proves the engine before Phase 2's isolated mode reuses it.
Output: the transparent stack manifest, transparent-mode logic, the copy-on-start `.claude.json` handler, and the `container` alias.
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-containerized-engine-transparent-stack/01-CONTEXT.md
@.planning/phases/01-containerized-engine-transparent-stack/01-RESEARCH.md
@.planning/phases/01-containerized-engine-transparent-stack/01-02-SUMMARY.md
@container.sh
@docs/harnessed-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: transparent stack manifest + mode logic</name>
  <files>stacks/transparent/stack.yaml, tools/harnessed/stack.py, tools/harnessed/transparent.py, tools/harnessed/cli.py</files>
  <read_first>docs/harnessed-design.md, container.sh, tools/harnessed/host_integration.py</read_first>
  <action>Create `stacks/transparent/stack.yaml` per design §12: `name: transparent`, `config: transparent` (harness omitted — transparent mirrors all host harness configs; the user picks one in the shell). Add `stack.py` to locate+parse a stack manifest (minimal YAML load; transparent has almost no config). Add `transparent.py` implementing the transparent run path: compose the §4a layer from `host_integration.py` (01-02) PLUS the live host config mounts — `~/.claude` rw (per-path dirs, low race risk), `$SCRIPT_DIR/.codex`, `$SCRIPT_DIR/.opencode`→`.config/opencode`, `$SCRIPT_DIR/.gemini` (mirroring `container.sh:136-138`) — all via `mounts.py` (host-absolute). Wire `cli.py` so `harnessed transparent [path]` creates the harness container (`harnessed-claude` image, `sleep infinity`), applies the firewall, prints the instance name for the bootstrap's host-native attach. NO pod/hatago (transparent is the degenerate case).</action>
  <verify>`./harnessed transparent` in a test project starts a harness container; `podman inspect` shows `~/.claude` mounted and the project at `/home/harnessed/<relpath>`; no hatago/pod siblings created.</verify>
  <acceptance_criteria>
    - stacks/transparent/stack.yaml has `config: transparent`
    - transparent.py mounts host ~/.claude rw + .codex/.opencode/.gemini, all via mounts.py
    - no pod/hatago is created in transparent mode
  </acceptance_criteria>
  <done>`harnessed transparent` launches a host-mirror instance through the harnessed engine.</done>
</task>

<task type="auto">
  <name>Task 2: copy-on-start .claude.json safety (the §4b fix)</name>
  <files>tools/harnessed/claude_config.py, tools/harnessed/transparent.py</files>
  <read_first>container.sh, .planning/research/PITFALLS.md</read_first>
  <action>Implement `claude_config.py` with copy-on-start (D-01): at instance start, copy host `~/.claude.json` (if present) into a per-instance writable location the container reads/writes, so host `~/.claude.json` is NEVER rw-bind-mounted (delete/never-add the `container.sh:124-126` rw mount). Implement it as a host-side per-instance file mounted writable, OR (preferred if it works) set `CLAUDE_CONFIG_DIR` in the instance to a per-instance dir seeded from host state. Add a `claude_config.probe_config_dir_scope()` that empirically checks whether `CLAUDE_CONFIG_DIR` relocates the top-level `.claude.json` (open question #1); log the result. Default to copy-on-start; switch to `CLAUDE_CONFIG_DIR` only if the probe confirms clean relocation. Wire into `transparent.py`.</action>
  <verify>Run `./harnessed transparent`, exercise the harness, exit; assert host `~/.claude.json` mtime/content is unchanged and no new `~/.claude/backups/*.backup.*` appeared from the container run.</verify>
  <acceptance_criteria>
    - No code path rw-bind-mounts host ~/.claude.json (grep: no `.claude.json:.*:rw` and no `.claude.json` host bind without copy)
    - claude_config.py copies host state once at start; host file unchanged after a run
  </acceptance_criteria>
  <done>Transparent instances get a writable .claude.json copy; the host file is never raced.</done>
</task>

<task type="auto">
  <name>Task 3: container thin alias</name>
  <files>container</files>
  <read_first>container.sh, install.sh</read_first>
  <action>Create an executable `container` that is a thin alias → `harnessed transparent`, forwarding `[path]` and preserving the existing operational flags (`--build`, `--list`, `--stop`, `--remove`, `--clean`) by mapping them onto the harnessed equivalents (or passing through). It MUST resolve the harnessed bootstrap path the same way `install.sh` resolves the binary. Update `install.sh`'s symlink target note if needed, but do NOT change install.sh behavior beyond pointing `container` at the alias. Zero behavior change for existing users (D-07).</action>
  <verify>`./container` and `./harnessed transparent` produce the same instance; `./container --list` works.</verify>
  <acceptance_criteria>
    - container forwards to `harnessed transparent` and preserves existing flags
    - `bash -n container` parses
  </acceptance_criteria>
  <done>`container` works as a thin alias with no behavior change.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The transparent stack end-to-end (requires the rootless podman socket). `harnessed transparent` and `container` should launch a host-mirror harness with your live config and project, without touching host ~/.claude.json.</what-built>
  <how-to-verify>With the socket enabled, run `./harnessed transparent` (or `./container`) in a scratch project: confirm the harness opens authenticated with your skills/config, the project is at /home/harnessed/<relpath>, and after exiting, `git status` on ~/.claude.json (or its mtime) shows it was not modified by the container.</how-to-verify>
  <resume-signal>Type "approved" if the instance is authenticated and host ~/.claude.json is untouched, or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- [ ] `harnessed transparent` and `container` launch an authenticated host-mirror instance (host config live, project at /home/harnessed/<relpath>)
- [ ] Host ~/.claude.json is byte-unchanged after a run; no rw bind-mount of it anywhere
- [ ] No pod/hatago created in transparent mode
</verification>

<success_criteria>
- All tasks completed
- transparent = old `container` behavior with zero regression, via the harnessed engine
- `.claude.json` corruption risk eliminated (copy-on-start)
- `container` alias preserved
</success_criteria>

<output>
After completion, create `.planning/phases/01-containerized-engine-transparent-stack/01-03-SUMMARY.md`
</output>
