---
phase: 01-containerized-engine-transparent-stack
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - lib/harnessed-transparent.sh
  - lib/harnessed-claude-config.sh
  - stacks/transparent/stack.yaml
  - container
autonomous: false
requirements: [ENG-03, MODE-01, MODE-02, AUTH-01, MNT-03]
must_haves:
  truths:
    - "`harnessed transparent [path]` launches harnessed-claude with host ~/.claude (+ .codex/.config/opencode/.gemini) mounted live and the project mounted"
    - "`container [path]` behaves identically to `harnessed transparent [path]` (thin alias)"
    - "The instance is authenticated from the live host config (no re-login)"
    - "The interactive attach runs host-natively (clean TTY)"
    - "Host ~/.claude.json is never rw-bind-mounted; the instance uses a copy-on-start per-instance copy and host ~/.claude.json is unchanged after a run"
  artifacts:
    - "lib/harnessed-transparent.sh — transparent host launcher (compose mounts, run, firewall, attach)"
    - "lib/harnessed-claude-config.sh — copy-on-start .claude.json (+ CLAUDE_CONFIG_DIR scope probe)"
    - "stacks/transparent/stack.yaml — the built-in transparent stack manifest"
    - "container — thin alias → harnessed transparent"
  key_links:
    - "harnessed-transparent.sh sources harnessed-mounts.sh (01-02) + harnessed-claude-config.sh + harnessed-common.sh"
    - "container forwards all args to `harnessed transparent`"
---

<objective>
Deliver the `transparent` stack — today's `container` behavior through the `harnessed` host launcher —
with the `~/.claude.json` safety fix and host-native attach, and retain `container` as a thin alias.

Purpose: zero-regression continuity plus the §4b corruption fix; transparent is the degenerate stack the
host launcher proves before Phase 2's assembler generates launchers for isolated stacks.
Output: the transparent launcher, the copy-on-start `.claude.json` handler, the stack manifest, and the alias.
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-containerized-engine-transparent-stack/01-CONTEXT.md
@.planning/phases/01-containerized-engine-transparent-stack/01-RESEARCH.md
@.planning/phases/01-containerized-engine-transparent-stack/01-02-SUMMARY.md
@container.sh
@docs/harnessed-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: transparent host launcher + stack manifest</name>
  <files>lib/harnessed-transparent.sh, stacks/transparent/stack.yaml</files>
  <read_first>docs/harnessed-design.md, container.sh, lib/harnessed-mounts.sh</read_first>
  <action>Create `stacks/transparent/stack.yaml` per design §12: `name: transparent`, `config: transparent` (harness omitted — transparent mirrors all host harness configs; the user picks one in the shell). Create `lib/harnessed-transparent.sh`: a host-bash launcher that (1) sources `lib/harnessed-common.sh` + `lib/harnessed-mounts.sh`; (2) builds the run-arg array = §4a layer (01-02) PLUS the live host config mounts — `~/.claude` rw, `$SCRIPT_DIR/.codex`→`.codex`, `$SCRIPT_DIR/.opencode`→`.config/opencode`, `$SCRIPT_DIR/.gemini`→`.gemini` (mirroring `container.sh:136-138`) PLUS the egress-firewall script mount; (3) `podman run -d --name harnessed-transparent-<projhash>` the `harnessed-claude` image with `sleep infinity`; (4) `apply_firewall`; (5) performs the interactive attach host-natively: `exec "$RUNTIME" exec -it <instance> bash` (ENG-03). All paths are host paths ($HOME/$PWD) — no DooD. No pod/hatago (transparent is degenerate).</action>
  <verify>`bash -n lib/harnessed-transparent.sh`; `./harnessed transparent` in a test project starts a harnessed-claude container; `podman inspect` shows `~/.claude` mounted and the project at `/home/harnessed/<relpath>`; no pod/hatago created.</verify>
  <acceptance_criteria>
    - stacks/transparent/stack.yaml has `config: transparent`
    - launcher mounts host ~/.claude rw + .codex/.opencode/.gemini; uses host paths; no pod/hatago
    - final attach is host-native `exec ... exec -it` (ENG-03)
  </acceptance_criteria>
  <done>`harnessed transparent` launches a host-mirror instance through the host launcher.</done>
</task>

<task type="auto">
  <name>Task 2: copy-on-start .claude.json safety (the §4b fix)</name>
  <files>lib/harnessed-claude-config.sh, lib/harnessed-transparent.sh</files>
  <read_first>container.sh, .planning/research/PITFALLS.md</read_first>
  <action>Implement `lib/harnessed-claude-config.sh` with copy-on-start (D-01): before launch, copy host `~/.claude.json` (if present) into a per-instance writable file mounted into the container at `/home/harnessed/.claude.json`, so host `~/.claude.json` is NEVER rw-bind-mounted (do not port `container.sh:124-126`). Add `probe_claude_config_dir_scope()` that empirically checks whether `CLAUDE_CONFIG_DIR` relocates the top-level `.claude.json` (open question); log the result. Default to copy-on-start; switching to `CLAUDE_CONFIG_DIR` is a documented fast-follow only if the probe confirms clean relocation. Wire the copy into `harnessed-transparent.sh`.</action>
  <verify>Run `./harnessed transparent`, exercise the harness, exit; assert host `~/.claude.json` mtime/content is unchanged and no new `~/.claude/backups/*.backup.*` appeared from the container run.</verify>
  <acceptance_criteria>
    - No code path rw-bind-mounts host ~/.claude.json (grep: no `.claude.json:` host bind without a per-instance copy)
    - copy-on-start seeds a per-instance copy; host file unchanged after a run
  </acceptance_criteria>
  <done>Transparent instances get a writable .claude.json copy; the host file is never raced.</done>
</task>

<task type="auto">
  <name>Task 3: container thin alias</name>
  <files>container</files>
  <read_first>container.sh, install.sh</read_first>
  <action>Create an executable `container` that is a thin alias → `harnessed transparent`, forwarding `[path]` and preserving the existing operational flags (`--build`, `--list`, `--stop`, `--remove`, `--clean`) by mapping them to the harnessed equivalents (or passing through). Resolve the `harnessed` bootstrap the same way `install.sh` resolves the binary. Zero behavior change for existing users (D-07).</action>
  <verify>`./container` and `./harnessed transparent` produce the same instance; `./container --list` works; `bash -n container`.</verify>
  <acceptance_criteria>
    - container forwards to `harnessed transparent` and preserves existing flags
    - `bash -n container` parses
  </acceptance_criteria>
  <done>`container` works as a thin alias with no behavior change.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The transparent stack end-to-end (needs host podman). `harnessed transparent` and `container` should launch a host-mirror harness with your live config + project, without touching host ~/.claude.json.</what-built>
  <how-to-verify>Run `./harnessed transparent` (or `./container`) in a scratch project: confirm the harness opens authenticated with your skills/config, the project is at /home/harnessed/<relpath>, and after exiting, host ~/.claude.json mtime/content is unchanged.</how-to-verify>
  <resume-signal>Type "approved" if authenticated and host ~/.claude.json is untouched, or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- [ ] `harnessed transparent` and `container` launch an authenticated host-mirror instance (host config live, project at /home/harnessed/<relpath>)
- [ ] Interactive attach is host-native (clean TTY)
- [ ] Host ~/.claude.json byte-unchanged after a run; no rw bind-mount of it anywhere
- [ ] No pod/hatago created in transparent mode
</verification>

<success_criteria>
- All tasks completed
- transparent = old `container` behavior with zero regression, via the host launcher
- `.claude.json` corruption risk eliminated (copy-on-start)
- `container` alias preserved
</success_criteria>

<output>
After completion, create `.planning/phases/01-containerized-engine-transparent-stack/01-03-SUMMARY.md`
</output>
