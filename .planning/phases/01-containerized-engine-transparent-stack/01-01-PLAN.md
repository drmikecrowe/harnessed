---
phase: 01-containerized-engine-transparent-stack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - harnessed
  - base/Dockerfile.harnessed-base
  - base/Dockerfile.harnessed-claude
  - lib/harnessed-common.sh
autonomous: false
requirements: [ENG-01, ENG-02]
must_haves:
  truths:
    - "`harnessed` (host bash) runs with only podman (or docker) installed and builds the base/claude images on first run; `harnessed --build` forces a rebuild"
    - "Images are built with HOST `podman build` and run with HOST `podman` — no daemon-in-container, no API socket, no CONTAINER_HOST/DOCKER_HOST"
    - "`harnessed-claude` has $HOME=/home/harnessed and `claude` resolves on PATH"
  artifacts:
    - "harnessed — dependency-free host bash bootstrap on PATH (detect runtime, ensure/build images, dispatch)"
    - "base/Dockerfile.harnessed-base — mise/node/pnpm/python toolchain, home /home/harnessed"
    - "base/Dockerfile.harnessed-claude — FROM harnessed-base + claude install"
    - "lib/harnessed-common.sh — shared bash helpers (runtime detection, image_exists/build, logging)"
  key_links:
    - "bootstrap sources lib/harnessed-common.sh for runtime detection + image build, then dispatches to the transparent launcher (plan 01-03)"
---

<objective>
Stand up the `harnessed` host entrypoint and the base/claude image lineage, built and run with host
podman — no Docker-out-of-Docker, no `harnessed-tools` image (that assembler is Phase 2).

Purpose: This is the host-native spine; transparent (01-03) and every later stack ride on these images
and this bootstrap.
Output: the `harnessed` bootstrap, the `harnessed-base`/`harnessed-claude` Dockerfiles, and shared bash helpers.
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-containerized-engine-transparent-stack/01-CONTEXT.md
@.planning/phases/01-containerized-engine-transparent-stack/01-RESEARCH.md
@.planning/phases/01-containerized-engine-transparent-stack/01-PATTERNS.md
@docs/harnessed-design.md
@container.sh
@install.sh
@Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: harnessed bash bootstrap + shared helpers</name>
  <files>harnessed, lib/harnessed-common.sh</files>
  <read_first>container.sh, install.sh, docs/harnessed-design.md</read_first>
  <action>Create an executable host `harnessed` (bash) at repo root and `lib/harnessed-common.sh`. The bootstrap MUST: (1) resolve its own real dir via the symlink-follow loop from `container.sh:16-20` and source `lib/harnessed-common.sh`; (2) detect the runtime preferring podman then docker (port `container.sh:26-34` into the helper); (3) parse the first arg as a subcommand — for this phase, `build` (alias `--build`, force rebuild images) and `transparent` (delegate to the plan 01-03 launcher), defaulting an unknown/absent stack to a usage message; (4) provide `ensure_images` in the helper: if `harnessed-claude:latest` (and its base) are missing, run host `podman build` to build them (auto on first run) with a visible "Building harnessed images (first run)…" message; `harnessed build`/`--build` forces it; (5) NOT mount any podman socket, NOT pass HOST_HOME/HOST_PWD, NOT create a tools container — the host runs podman directly. Put `image_exists`/`build_image`/`print_*` helpers in `lib/harnessed-common.sh` (adapt `container.sh:148-163,223-248`).</action>
  <verify>`bash -n harnessed lib/harnessed-common.sh` parses; `./harnessed` with no args prints usage; `./harnessed build` triggers `podman build` of base+claude.</verify>
  <acceptance_criteria>
    - harnessed sources lib/harnessed-common.sh and detects podman/docker
    - ensure_images builds via host `podman build` (no socket, no tools container)
    - grep: harnessed contains no `CONTAINER_HOST`/`DOCKER_HOST`/`podman.sock`
  </acceptance_criteria>
  <done>The bootstrap detects the runtime, builds images on first run, and dispatches subcommands.</done>
</task>

<task type="auto">
  <name>Task 2: base/claude image lineage with /home/harnessed</name>
  <files>base/Dockerfile.harnessed-base, base/Dockerfile.harnessed-claude</files>
  <read_first>Dockerfile</read_first>
  <action>Split the existing `Dockerfile` into `base/Dockerfile.harnessed-base` (the mise toolchain from `Dockerfile:1-74`, but set the user home to `/home/harnessed` instead of `/container/$USERNAME` per D-06; keep pnpm as-is — Phase 3 hardens it) and `base/Dockerfile.harnessed-claude` (`FROM harnessed-base` + the Claude install from `Dockerfile:76-88`). Ensure `$HOME=/home/harnessed`, the mise shims PATH, and `.bashrc` are correct for the new home. `FROM` is lineage only (§6) — no union.</action>
  <verify>`podman build -t harnessed-base -f base/Dockerfile.harnessed-base .` and `... harnessed-claude -f base/Dockerfile.harnessed-claude .` both succeed; `podman run --rm harnessed-claude bash -lc 'echo $HOME; which claude'` prints `/home/harnessed` and a claude path.</verify>
  <acceptance_criteria>
    - both images build; $HOME is /home/harnessed; claude resolves on PATH
    - harnessed-claude is FROM harnessed-base (lineage only)
  </acceptance_criteria>
  <done>The base/claude image lineage builds with the legible-slug home.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The `harnessed` bootstrap + base/claude image lineage. Building/running images needs a working host rootless podman, which only the operator can confirm on their machine.</what-built>
  <how-to-verify>Run `./harnessed build` on a machine with podman, then `podman run --rm harnessed-claude bash -lc 'echo $HOME; which claude'` and confirm it prints `/home/harnessed` and a claude path.</how-to-verify>
  <resume-signal>Type "approved" once the images build and the run prints the expected home/claude, or describe the error.</resume-signal>
</task>

</tasks>

<verification>
- [ ] `bash -n harnessed lib/harnessed-common.sh` parses
- [ ] `podman build` of harnessed-base and harnessed-claude succeed; $HOME=/home/harnessed, claude on PATH
- [ ] No socket / CONTAINER_HOST / tools-container code anywhere (host runs podman directly)
</verification>

<success_criteria>
- All tasks completed
- Host-native build/run model (no DooD) established
- Bootstrap auto-builds images and dispatches subcommands
- No assembler / harnessed-tools image / recipe logic (out of phase)
</success_criteria>

<output>
After completion, create `.planning/phases/01-containerized-engine-transparent-stack/01-01-SUMMARY.md`
</output>
