---
phase: 01-containerized-engine-transparent-stack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - harnessed
  - tools/Dockerfile
  - tools/pyproject.toml
  - tools/harnessed/__init__.py
  - tools/harnessed/cli.py
  - tools/harnessed/podman.py
  - tools/harnessed/mounts.py
autonomous: false
requirements: [ENG-01, ENG-02, ENG-03]
user_setup:
  - "Enable the rootless podman API socket: `systemctl --user enable --now podman.socket` and `loginctl enable-linger $USER` (one-time; harnessed detects and errors with this hint if absent)."
must_haves:
  truths:
    - "`harnessed` (bash) runs with only podman installed and builds the harnessed-tools image on first invocation"
    - "harnessed-tools drives the HOST podman over the mounted rootless socket (a container created by the tool appears in `podman ps` on the host)"
    - "Every bind mount the tool issues uses a host-absolute source derived from injected HOST_HOME/HOST_PWD"
    - "The interactive harness attach runs host-natively (clean TTY), not tunneled through the tools container"
  artifacts:
    - "harnessed — dependency-free bash bootstrap on PATH"
    - "tools/Dockerfile + tools/pyproject.toml — harnessed-tools image definition"
    - "tools/harnessed/mounts.py — the single host-absolute mount-construction helper"
    - "tools/harnessed/podman.py — DooD podman driver (CONTAINER_HOST)"
    - "tools/harnessed/cli.py — argument parsing + command dispatch"
  key_links:
    - "bootstrap injects HOST_HOME/HOST_PWD + mounts the rootless socket → podman.py uses CONTAINER_HOST"
    - "mounts.py is the ONLY place a `-v` source is constructed; podman.py calls it"
---

<objective>
Stand up the harnessed control plane: a dependency-free bash bootstrap and the `harnessed-tools` Python
image that drives the host rootless podman (Docker-out-of-Docker), with the host-absolute mount helper that
all later work depends on.

Purpose: This is the spine of every harnessed feature; the DooD mount discipline (D-08) is the dominant
Phase-1 correctness risk and must be proven before anything is layered on it.
Output: `harnessed` bootstrap, the `harnessed-tools` image, and the `mounts.py`/`podman.py`/`cli.py` core.
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-containerized-engine-transparent-stack/01-CONTEXT.md
@.planning/phases/01-containerized-engine-transparent-stack/01-RESEARCH.md
@.planning/phases/01-containerized-engine-transparent-stack/01-PATTERNS.md
@docs/harnessed-design.md
@container.sh
@install.sh
@Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: harnessed bash bootstrap (dependency-free)</name>
  <files>harnessed</files>
  <read_first>install.sh, container.sh, docs/harnessed-design.md</read_first>
  <action>Create an executable POSIX/bash `harnessed` at repo root. It MUST: (1) resolve its own real dir via the symlink-follow loop from `container.sh:16-20`; (2) detect runtime preferring podman then docker (`container.sh:26-34`); (3) verify the rootless socket `unix:///run/user/$(id -u)/podman/podman.sock` exists — if absent, print the exact hint `systemctl --user enable --now podman.socket && loginctl enable-linger $USER` and exit non-zero; (4) ensure the `harnessed-tools:latest` image exists, auto-building from `tools/Dockerfile` on first run (D-04) with a visible "Building harnessed-tools (first run)…" message; `harnessed --build` forces rebuild; (5) `podman run --rm` the tools image passing `--env HOST_HOME="$HOME" --env HOST_PWD="$(pwd)" --env CONTAINER_HOST=unix:///run/user/$(id -u)/podman/podman.sock`, mounting the rootless socket and the repo dir at its OWN host-absolute path, forwarding all args; (6) after the tool creates the instance and prints the instance name on stdout, perform the interactive attach HOST-NATIVELY: `exec "$RUNTIME" exec -it "$INSTANCE" "$HARNESS"` (NEVER tunnel the attach through the tools container). Keep the bootstrap free of any non-coreutils dependency.</action>
  <verify>`bash -n harnessed` parses; `./harnessed --help` runs the tool image and prints usage; with the socket disabled, `./harnessed transparent` prints the enable-socket hint and exits non-zero.</verify>
  <acceptance_criteria>
    - harnessed contains the symlink-resolve loop and `CONTAINER_HOST=unix:///run/user/` socket wiring
    - harnessed passes `HOST_HOME` and `HOST_PWD` as env to the tools image
    - harnessed performs the final `exec -it` attach host-natively (grep: `exec .* exec -it`)
  </acceptance_criteria>
  <done>Bootstrap builds the tools image on first run and hands off to it; socket-absent path errors with the documented hint.</done>
</task>

<task type="auto">
  <name>Task 2: harnessed-tools image (Python brain)</name>
  <files>tools/Dockerfile, tools/pyproject.toml, tools/harnessed/__init__.py</files>
  <read_first>Dockerfile, docs/harnessed-design.md, .planning/research/STACK.md</read_first>
  <action>Create `tools/Dockerfile` for `harnessed-tools`: base on a slim python 3.12/3.13 (or reuse the mise base from `Dockerfile:49-74`), install the podman-remote CLI (or rely on `CONTAINER_HOST` with the bundled client), `git`, `yq`, `jq`, and `pip install` the local package. Create `tools/pyproject.toml` declaring package `harnessed` with deps `rich>=14`, a YAML lib (`ruamel.yaml` or `pyyaml`), and a console-script entry `harnessed = harnessed.cli:main`. Create `tools/harnessed/__init__.py`. Do NOT add the pnpm supply-chain config here (Phase 3). Keep the image minimal — its only Phase-1 job is to orchestrate podman.</action>
  <verify>`podman build -t harnessed-tools:latest tools/` succeeds; `podman run --rm harnessed-tools:latest harnessed --help` prints usage.</verify>
  <acceptance_criteria>
    - tools/pyproject.toml defines console_script `harnessed = harnessed.cli:main`
    - tools/Dockerfile installs python + rich + git + yq/jq and the local package
  </acceptance_criteria>
  <done>The tools image builds and runs the `harnessed` CLI entrypoint.</done>
</task>

<task type="auto">
  <name>Task 3: host-absolute mount helper + DooD podman driver + CLI skeleton</name>
  <files>tools/harnessed/mounts.py, tools/harnessed/podman.py, tools/harnessed/cli.py</files>
  <read_first>container.sh, .planning/phases/01-containerized-engine-transparent-stack/01-RESEARCH.md</read_first>
  <action>Implement `mounts.py` as the SINGLE place a bind mount is constructed (D-08, Pitfall 1): a `Mount` dataclass and a `host_path(*parts)` helper that joins against `os.environ["HOST_HOME"]`/`HOST_PWD`, asserts the result is absolute, and raises if any callsite passes a tool-internal path. Implement `podman.py`: a thin wrapper around the podman CLI using `CONTAINER_HOST` (run, exec, ps, image exists/build, rm), returning structured results; all `-v` flags MUST come from `mounts.py`. Implement `cli.py`: argparse dispatch with subcommands stubbed for this phase — `transparent`, `--build` (rebuild tools/base image) — and a `_diag` hidden subcommand that issues one DooD `podman run --rm -v <HOST_PWD>:/probe:ro alpine ls /probe` to PROVE a host-correct mount round-trips. Wire `cli.main()`.</action>
  <verify>`podman run --rm harnessed-tools:latest harnessed _diag` lists the host project files (proves the mount resolved on the host daemon, not the tool's view).</verify>
  <acceptance_criteria>
    - mounts.py raises on a non-absolute or non-host-rooted source (unit-style assert)
    - podman.py constructs no `-v` string except via mounts.py
    - `harnessed _diag` prints the host project's files through a DooD mount
  </acceptance_criteria>
  <done>A host-correct DooD mount is demonstrated end-to-end; the helper rejects internal paths.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The harnessed bootstrap + harnessed-tools image + DooD mount helper. Verification requires the rootless podman socket, which only the operator can enable.</what-built>
  <how-to-verify>Run `systemctl --user enable --now podman.socket && loginctl enable-linger $USER` if not already done, then `./harnessed _diag` and confirm it lists this project's files (proving the host-absolute DooD mount works).</how-to-verify>
  <resume-signal>Type "approved" once `_diag` lists the project files, or describe the error.</resume-signal>
</task>

</tasks>

<verification>
- [ ] `bash -n harnessed` parses; `podman build -t harnessed-tools:latest tools/` succeeds
- [ ] `harnessed _diag` round-trips a host-absolute mount (host files visible inside a DooD container)
- [ ] Socket-absent path prints the documented enable hint and exits non-zero
</verification>

<success_criteria>
- All tasks completed
- DooD mount helper is the single mount-construction point and rejects tool-internal paths
- Bootstrap auto-builds the tools image and attaches host-natively
- No pnpm/supply-chain or recipe logic introduced (out of phase)
</success_criteria>

<output>
After completion, create `.planning/phases/01-containerized-engine-transparent-stack/01-01-SUMMARY.md`
</output>
