---
phase: 01-containerized-engine-transparent-stack
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/harnessed-mounts.sh
  - lib/egress-firewall.sh
  - lib/harnessed-common.sh
autonomous: true
requirements: [MNT-01, MNT-02]
must_haves:
  truths:
    - "The launcher mounts the §4a host-integration layer (1Password SSH agent, GPG/YubiKey, ~/.gnupg ro, ~/.ssh ro, git config ro, /etc/machine-id ro) when those host sources exist"
    - "The project is mounted at /home/harnessed/<relpath> with the working dir set there"
    - "The egress firewall is applied inside the instance (default-DROP allowlist)"
  artifacts:
    - "lib/harnessed-mounts.sh — host-bash §4a mount-arg builder + project mount"
    - "lib/egress-firewall.sh — egress firewall (relocated), mounted into instances"
  key_links:
    - "harnessed-mounts.sh emits a podman run arg array consumed by the transparent launcher (01-03)"
    - "common.sh apply_firewall execs lib/egress-firewall.sh inside the instance, guarded by /run/egress-firewall-active"
---

<objective>
Port the proven §4a host-integration mount layer and egress firewall from `container.sh` into reusable
host-bash building blocks the transparent launcher (01-03) consumes.

Purpose: operational credentials/signing/agents + controlled egress — required for a usable instance and
shared by every stack (including isolated, later).
Output: `lib/harnessed-mounts.sh`, the relocated `lib/egress-firewall.sh`, and the firewall-apply helper.
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-containerized-engine-transparent-stack/01-CONTEXT.md
@.planning/phases/01-containerized-engine-transparent-stack/01-PATTERNS.md
@container.sh
@egress-firewall.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: §4a host-integration mount layer (host bash)</name>
  <files>lib/harnessed-mounts.sh</files>
  <read_first>container.sh</read_first>
  <action>Port `container.sh:start_new_container` (lines 37-146) into `lib/harnessed-mounts.sh` as a function that appends `podman run` args to a bash array (`MOUNT_ARGS+=(...)`), each conditional on the host source existing: 1Password SSH agent socket + `SSH_AUTH_SOCK` env (45-50), GPG SSH socket (52-60), `~/.gnupg` ro (62-65), YubiKey `--device` via `lsusb` vendor 1050 (67-75), `~/.zai.json` ro (77-81), per-tool `~/.config/<tool>` from extra-tools (83-107), git config XDG-or-legacy ro (109-114), `/etc/machine-id` ro (116-119), `~/.ssh` ro (141), plus `--cap-add NET_ADMIN` and `--userns=keep-id` (130-131). Sources use host paths directly (`$HOME`, `$PWD`) — the launcher runs on the host, so this is natural. Add the project mount at `/home/harnessed/<relpath>` with `-w` there (replacing `CONTAINER_HOME/$project_relpath`). Do NOT include `~/.claude*` mounts here — those are mode-specific (plan 01-03).</action>
  <verify>Shellcheck/`bash -n` clean; sourced into a test harness with fake `$HOME` fixtures, the function emits `-v`/`--device`/`-e` args only for existing sources; the project mount targets `/home/harnessed/`.</verify>
  <acceptance_criteria>
    - harnessed-mounts.sh appends the §4a args conditionally; project mounted at /home/harnessed/ with -w
    - no `~/.claude*` mounts in this file
  </acceptance_criteria>
  <done>The §4a layer is reproduced as a reusable host-bash builder.</done>
</task>

<task type="auto">
  <name>Task 2: egress firewall relocation + apply helper</name>
  <files>lib/egress-firewall.sh, lib/harnessed-common.sh</files>
  <read_first>egress-firewall.sh, container.sh</read_first>
  <action>Move `egress-firewall.sh` to `lib/egress-firewall.sh` (verbatim; update the `container` alias/launcher mount path to the new location in 01-03). Add `apply_firewall(instance)` to `lib/harnessed-common.sh` mirroring `container.sh:284-303`: the launcher's run spec mounts the script ro at `/usr/local/sbin/egress-firewall`; `apply_firewall` `podman exec`s it once per session, guarded by the `/run/egress-firewall-active` flag file.</action>
  <verify>`apply_firewall` is idempotent (second call no-ops via the flag); the script mounts read-only at `/usr/local/sbin/egress-firewall`.</verify>
  <acceptance_criteria>
    - lib/egress-firewall.sh exists, byte-identical to the original
    - apply_firewall guarded by /run/egress-firewall-active
  </acceptance_criteria>
  <done>Egress firewall applies inside instances and re-applies safely per session.</done>
</task>

</tasks>

<verification>
- [ ] `bash -n lib/harnessed-mounts.sh lib/harnessed-common.sh` clean
- [ ] §4a args emitted conditionally; project at /home/harnessed/<relpath>; no `~/.claude*` here
- [ ] egress firewall mounts ro and applies idempotently
</verification>

<success_criteria>
- All tasks completed
- §4a layer + egress + project mount reproduced as host-bash building blocks
- Host paths used directly (no DooD mount helper needed)
</success_criteria>

<output>
After completion, create `.planning/phases/01-containerized-engine-transparent-stack/01-02-SUMMARY.md`
</output>
