---
phase: 01-containerized-engine-transparent-stack
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tools/harnessed/host_integration.py
  - tools/harnessed/podman.py
  - lib/egress-firewall.sh
  - base/Dockerfile.harnessed-base
  - base/Dockerfile.harnessed-claude
autonomous: true
requirements: [MNT-01, MNT-02]
must_haves:
  truths:
    - "Every harnessed instance mounts the §4a host-integration layer (1Password SSH agent, GPG/YubiKey, ~/.gnupg ro, ~/.ssh ro, git config ro, /etc/machine-id ro) when those host sources exist"
    - "The project is mounted at /home/harnessed/<relpath> with the working dir set there"
    - "The egress firewall is applied inside the instance (default-DROP allowlist)"
  artifacts:
    - "tools/harnessed/host_integration.py — the §4a mount layer, built via mounts.py"
    - "lib/egress-firewall.sh — egress firewall (moved/kept), mounted into instances"
    - "base/Dockerfile.harnessed-base + base/Dockerfile.harnessed-claude — image lineage with $HOME=/home/harnessed"
  key_links:
    - "host_integration.py builds every mount through mounts.py (host-absolute, D-08)"
    - "the harness image sets $HOME=/home/harnessed so the project relpath yields a legible Claude slug"
---

<objective>
Port the proven §4a host-integration mount layer and egress firewall from `container.sh` into the harnessed
tools, and establish the harness image lineage with the `/home/harnessed` home (D-06).

Purpose: These are operational credentials/signing/agents — they belong in every stack and are required for a
usable transparent instance (signed commits, agent-forwarded SSH, controlled egress).
Output: `host_integration.py`, the relocated `egress-firewall.sh`, and the `harnessed-base`/`harnessed-claude` images.
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-containerized-engine-transparent-stack/01-CONTEXT.md
@.planning/phases/01-containerized-engine-transparent-stack/01-PATTERNS.md
@container.sh
@egress-firewall.sh
@Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: §4a host-integration mount layer</name>
  <files>tools/harnessed/host_integration.py</files>
  <read_first>container.sh, tools/harnessed/mounts.py</read_first>
  <action>Port `container.sh:start_new_container` (lines 37-146) into `host_integration.py` as a function returning a list of `Mount`/flag specs, each conditional on the host source existing (use `HOST_HOME`-rooted paths via `mounts.py`, NEVER tool-internal paths): 1Password SSH agent socket + `SSH_AUTH_SOCK` env (45-50), GPG SSH socket (52-60), `~/.gnupg` ro (62-65), YubiKey `--device` passthrough via `lsusb` vendor 1050 (67-75), `~/.zai.json` ro (77-81), per-tool `~/.config/<tool>` from extra-tools (83-107), git config XDG-or-legacy ro (109-114), `/etc/machine-id` ro (116-119), `~/.ssh` ro (141), and the container flags `--cap-add NET_ADMIN` + `--userns=keep-id` (130-131). Do NOT include the `~/.claude*` mounts here — those are mode-specific (Plan 01-03). Expose the project mount at `/home/harnessed/<relpath>` with `-w` set there (replacing `CONTAINER_HOME/$project_relpath`).</action>
  <verify>Unit-style: with fake HOST_HOME fixtures, the function emits the expected `-v`/`--device`/`-e` specs only for existing sources; every source path begins with HOST_HOME or is a host-absolute system path (/etc/machine-id, /run/user/...).</verify>
  <acceptance_criteria>
    - host_integration.py emits the project mount at `/home/harnessed/` and sets the working dir there
    - all source paths are built via mounts.py (host-absolute); none use the tool-internal view
    - `~/.claude*` mounts are NOT present in this module
  </acceptance_criteria>
  <done>The §4a layer is reproduced with host-absolute sources and conditional inclusion.</done>
</task>

<task type="auto">
  <name>Task 2: egress firewall relocation + apply</name>
  <files>lib/egress-firewall.sh, tools/harnessed/podman.py</files>
  <read_first>egress-firewall.sh, container.sh</read_first>
  <action>Move `egress-firewall.sh` to `lib/egress-firewall.sh` (verbatim; it is runtime-mounted glue, not assembler logic — keep a top-of-repo symlink or update the `container` alias path in Plan 01-03). In `podman.py`, add an `apply_firewall(instance)` that mirrors `container.sh:284-303`: mount the script at `/usr/local/sbin/egress-firewall` (done in the run spec), then `podman exec` it once per session guarded by the `/run/egress-firewall-active` flag file.</action>
  <verify>`apply_firewall` is idempotent (second call no-ops via the flag file); the script is mounted read-only at `/usr/local/sbin/egress-firewall`.</verify>
  <acceptance_criteria>
    - lib/egress-firewall.sh exists and is byte-identical to the original
    - podman.py applies the firewall guarded by /run/egress-firewall-active
  </acceptance_criteria>
  <done>Egress firewall is applied inside instances and re-applies safely per session.</done>
</task>

<task type="auto">
  <name>Task 3: harness image lineage with /home/harnessed</name>
  <files>base/Dockerfile.harnessed-base, base/Dockerfile.harnessed-claude</files>
  <read_first>Dockerfile</read_first>
  <action>Split the existing `Dockerfile` into `base/Dockerfile.harnessed-base` (the mise toolchain from `Dockerfile:1-74`, but set the user home to `/home/harnessed` instead of `/container/$USERNAME` per D-06; keep pnpm as-is — Phase 3 hardens it) and `base/Dockerfile.harnessed-claude` (`FROM harnessed-base` + the Claude install from `Dockerfile:76-88`). Ensure `$HOME=/home/harnessed` and the mise shims PATH are correct for the new home. Verify the claude binary resolves at runtime from the new home (open question #2 in RESEARCH).</action>
  <verify>`podman build -t harnessed-base -f base/Dockerfile.harnessed-base .` and `... harnessed-claude -f base/Dockerfile.harnessed-claude .` both succeed; `podman run --rm harnessed-claude bash -lc 'echo $HOME; which claude'` prints `/home/harnessed` and a claude path.</verify>
  <acceptance_criteria>
    - base images build; $HOME is /home/harnessed; claude resolves on PATH
    - harnessed-claude is FROM harnessed-base (lineage only, no union — §6)
  </acceptance_criteria>
  <done>Harness image lineage builds with the legible-slug home.</done>
</task>

</tasks>

<verification>
- [ ] host_integration.py emits host-absolute §4a mounts conditionally; no `~/.claude*` here
- [ ] `podman build` of harnessed-base and harnessed-claude succeed; $HOME=/home/harnessed, claude on PATH
- [ ] egress firewall mounts ro and applies idempotently
</verification>

<success_criteria>
- All tasks completed
- §4a layer + egress + project mount reproduced via the host-absolute helper
- Harness image home relocated to /home/harnessed without breaking the claude install
</success_criteria>

<output>
After completion, create `.planning/phases/01-containerized-engine-transparent-stack/01-02-SUMMARY.md`
</output>
