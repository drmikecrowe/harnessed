---
phase: 07-fat-base-agent-images
plan: "03"
type: execute
wave: 2
depends_on: ["01", "02"]
gap_closure: true
files_modified:
  - base/Dockerfile.harnessed-base
  - base/Dockerfile.harnessed-codex
  - base/Dockerfile.harnessed-gemini
  - lib/harnessed-common.sh
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - "IMG-01 (complete — all harness CLIs removed from base)"
must_haves:
  truths:
    - "rg '@openai/codex' base/Dockerfile.harnessed-base exits 1 (codex CLI removed from base)"
    - "rg '@google/gemini-cli' base/Dockerfile.harnessed-base exits 1 (gemini CLI removed from base)"
    - "rg '@openai/codex' base/Dockerfile.harnessed-codex exits 0 (codex CLI installed locally)"
    - "rg '@google/gemini-cli' base/Dockerfile.harnessed-gemini exits 0 (gemini CLI installed locally)"
    - "rg 'mise up' base/Dockerfile.harnessed-base exits 1 (auto-upgrade removed from .bashrc)"
    - "build_images() in lib/harnessed-common.sh does NOT build HARNESSED_OMP_IMAGE (HRN-01 contract)"
    - "ensure_images() in lib/harnessed-common.sh checks only HARNESSED_CLAUDE_IMAGE and HARNESSED_HATAGO_IMAGE"
  artifacts:
    - path: "base/Dockerfile.harnessed-base"
      provides: "Fat toolchain base with NO harness CLIs (IMG-01 complete)"
      contains: "node@24"
    - path: "base/Dockerfile.harnessed-codex"
      provides: "Codex harness image that installs its own CLI"
      contains: "npm:@openai/codex"
    - path: "base/Dockerfile.harnessed-gemini"
      provides: "Gemini harness image that installs its own CLI"
      contains: "npm:@google/gemini-cli"
    - path: "lib/harnessed-common.sh"
      provides: "build_images() with HRN-01 lazy contract restored for omp"
      contains: "HARNESSED_OMP_IMAGE"
  key_links:
    - from: "base/Dockerfile.harnessed-codex"
      to: "base/Dockerfile.harnessed-base"
      via: "FROM harnessed-base:latest"
      pattern: "FROM harnessed-base"
    - from: "base/Dockerfile.harnessed-gemini"
      to: "base/Dockerfile.harnessed-base"
      via: "FROM harnessed-base:latest"
      pattern: "FROM harnessed-base"
    - from: "lib/harnessed-common.sh ensure_omp_image()"
      to: "HARNESSED_OMP_IMAGE"
      via: "lazy build — only for omp stacks"
      pattern: "ensure_omp_image"
---

<objective>
Close the SC-1 gap from phase 07 verification: move `npm:@openai/codex` from `base/Dockerfile.harnessed-base`
into `base/Dockerfile.harnessed-codex`, and move `npm:@google/gemini-cli` from base into
`base/Dockerfile.harnessed-gemini`. This completes IMG-01 ("No harness CLIs in base").

Also fixes two code review blockers from 07-REVIEW.md:
- CR-01: Remove `mise up 2>/dev/null` from `.bashrc` in `base/Dockerfile.harnessed-base`
  (auto-upgrades tools past the scanned versions, breaking the supply-chain scan guarantee).
- CR-02: Remove `HARNESSED_OMP_IMAGE` from `build_images()` and fix `ensure_images()` to restore
  the HRN-01 lazy-build contract (omp should only be built when an omp stack is requested).

After this plan, `harnessed-base` truly has no harness CLIs. Each harness Dockerfile is fully
self-contained: it inherits runtimes from base and installs only its own CLI.
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-fat-base-agent-images/07-VERIFICATION.md
@.planning/phases/07-fat-base-agent-images/07-REVIEW.md
@base/Dockerfile.harnessed-base
@base/Dockerfile.harnessed-codex
@base/Dockerfile.harnessed-gemini
@lib/harnessed-common.sh
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install codex CLI locally in Dockerfile.harnessed-codex (SC-1)</name>
  <files>base/Dockerfile.harnessed-codex</files>
  <read_first>
    - base/Dockerfile.harnessed-codex — full file; understand the existing structure (FROM + config RUN) before editing
  </read_first>
  <action>
Edit `base/Dockerfile.harnessed-codex`:

1. Add a `RUN` block BEFORE the `RUN mkdir -p /home/harnessed/.codex ...` config block to install
   the codex CLI via mise:

```dockerfile
# Install the OpenAI Codex CLI — previously inherited from harnessed-base; now self-contained
# so harnessed-base has no harness CLIs (IMG-01). Uses the same mise + pnpm supply-chain policy
# already active in the base image (npm.package_manager=pnpm, managed pnpm config).
RUN mise use -g npm:@openai/codex && mise install
```

2. Update the file's header comment: replace the sentence "codex is ALREADY installed and working
   in the base image (npm:@openai/codex — it ships per-platform Rust binaries as
   optionalDependencies, so no blocked postinstall, unlike opencode), so this image only bakes the
   config." with "This image installs the codex CLI locally via mise (npm:@openai/codex), then
   bakes the config. The CLI ships per-platform Rust binaries as optionalDependencies, so no
   blocked postinstall."

No other changes to the file.
  </action>
  <verify>
    <automated>rg "npm:@openai/codex" /home/mcrowe/Programming/Personal/code-container/base/Dockerfile.harnessed-codex && echo "PASS: codex CLI install present in harnessed-codex"</automated>
  </verify>
  <acceptance_criteria>
    - `base/Dockerfile.harnessed-codex` contains `mise use -g npm:@openai/codex && mise install`
    - The RUN block is placed before the `mkdir -p /home/harnessed/.codex` config block
    - The header comment no longer says codex is "ALREADY installed" in the base image
  </acceptance_criteria>
  <done>base/Dockerfile.harnessed-codex installs its own codex CLI; no longer depends on the base for it</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install gemini CLI locally in Dockerfile.harnessed-gemini (SC-1)</name>
  <files>base/Dockerfile.harnessed-gemini</files>
  <read_first>
    - base/Dockerfile.harnessed-gemini — full file; understand the existing structure (FROM + config RUN) before editing
  </read_first>
  <action>
Edit `base/Dockerfile.harnessed-gemini`:

1. Add a `RUN` block BEFORE the `RUN mkdir -p /home/harnessed/.gemini ...` config block to install
   the gemini CLI via mise:

```dockerfile
# Install the Google Gemini CLI — previously inherited from harnessed-base; now self-contained
# so harnessed-base has no harness CLIs (IMG-01). Uses the same mise + pnpm supply-chain policy
# already active in the base image (npm.package_manager=pnpm, managed pnpm config).
RUN mise use -g npm:@google/gemini-cli && mise install
```

2. Update the file's header comment: replace the sentence "gemini-cli is ALREADY installed and
   working in the base image (npm:@google/gemini-cli — a pure-JS CLI with no blocked postinstall,
   unlike opencode), so this image only bakes the config." with "This image installs the gemini CLI
   locally via mise (npm:@google/gemini-cli), then bakes the config. The CLI is pure-JS with no
   blocked postinstall."

No other changes to the file.
  </action>
  <verify>
    <automated>rg "npm:@google/gemini-cli" /home/mcrowe/Programming/Personal/code-container/base/Dockerfile.harnessed-gemini && echo "PASS: gemini CLI install present in harnessed-gemini"</automated>
  </verify>
  <acceptance_criteria>
    - `base/Dockerfile.harnessed-gemini` contains `mise use -g npm:@google/gemini-cli && mise install`
    - The RUN block is placed before the `mkdir -p /home/harnessed/.gemini` config block
    - The header comment no longer says gemini-cli is "ALREADY installed" in the base image
  </acceptance_criteria>
  <done>base/Dockerfile.harnessed-gemini installs its own gemini CLI; no longer depends on the base for it</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove codex/gemini CLIs and mise up from Dockerfile.harnessed-base (SC-1 + CR-01)</name>
  <files>base/Dockerfile.harnessed-base</files>
  <read_first>
    - base/Dockerfile.harnessed-base — full file; specifically the mise use -g block (lines ~70-84) and the .bashrc block (lines ~95-99)
  </read_first>
  <action>
Edit `base/Dockerfile.harnessed-base` with two changes:

**Change 1 — Remove codex/gemini CLIs from mise use -g block (SC-1):**

Remove `npm:@openai/codex` and `npm:@google/gemini-cli` from the `mise use -g` invocation.
The resulting list should be:
```
    node@24 \
    pnpm@11 \
    python@latest \
    bun \
    rust \
    go \
    fd \
    ripgrep && \
```
(two lines removed; the `&& \` continuation on `ripgrep` becomes `ripgrep && \` to close the block)

Update the block comment (lines 64-66) to remove the reference to codex/gemini CLIs and update
the IMG-01 status from "partial" to "complete":
Replace the comment block with:
```dockerfile
# Core runtimes — no harness CLIs in base (IMG-01 complete). Harness CLIs (claude, omp, codex,
# gemini) are each installed in their own harness image FROM this base.
# Note: opencode-ai removed — non-functional in base. npm.package_manager=pnpm routes npm: tools
# through pnpm so the managed policy governs them (RESEARCH Pitfall 5); set BEFORE `mise use -g`.
# pnpm@11 (not @latest) so the v11 supply-chain defaults are in effect (Node 24 LTS required).
```

**Change 2 — Remove `mise up` from .bashrc (CR-01):**

In the `.bashrc` RUN block (lines ~96-99), remove the line:
```
    echo 'mise up 2>/dev/null' >> /home/${USERNAME}/.bashrc && \
```
The final `.bashrc` RUN block should have only three echo lines:
```dockerfile
RUN echo 'PS1="\[\033[01;32m\][harnessed]\[\033[00m\] \[\033[01;34m\]\w\[\033[00m\]\$ "' >> /home/${USERNAME}/.bashrc && \
    echo 'eval "$(mise activate bash)"' >> /home/${USERNAME}/.bashrc && \
    echo 'mise trust -a 2>/dev/null' >> /home/${USERNAME}/.bashrc
```

No other changes to the file. Do not touch the apt-get block, the 1Password block, or any other RUN layer.
  </action>
  <verify>
    <automated>
echo "--- codex absent from base ---"
rg -c "@openai/codex" /home/mcrowe/Programming/Personal/code-container/base/Dockerfile.harnessed-base && echo "FAIL: codex still in base" || echo "PASS: codex absent"
echo "--- gemini absent from base ---"
rg -c "@google/gemini-cli" /home/mcrowe/Programming/Personal/code-container/base/Dockerfile.harnessed-base && echo "FAIL: gemini still in base" || echo "PASS: gemini absent"
echo "--- mise up absent from base ---"
rg -c "mise up" /home/mcrowe/Programming/Personal/code-container/base/Dockerfile.harnessed-base && echo "FAIL: mise up still in base" || echo "PASS: mise up absent"
echo "--- core runtimes still present ---"
rg "node@24|bun|rust|go" /home/mcrowe/Programming/Personal/code-container/base/Dockerfile.harnessed-base | head -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `rg "@openai/codex" base/Dockerfile.harnessed-base` exits 1 (absent)
    - `rg "@google/gemini-cli" base/Dockerfile.harnessed-base` exits 1 (absent)
    - `rg "mise up" base/Dockerfile.harnessed-base` exits 1 (absent)
    - `rg "node@24" base/Dockerfile.harnessed-base` exits 0 (node@24 still present)
    - `rg "bun" base/Dockerfile.harnessed-base` exits 0 (bun still present)
    - Comment block updated: no longer says "IMG-01 partial", now says "IMG-01 complete"
  </acceptance_criteria>
  <done>harnessed-base has no harness CLIs (IMG-01 complete); no auto-upgrade in .bashrc (CR-01 fixed)</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Restore HRN-01 lazy-build contract — remove omp from build_images() and fix ensure_images() (CR-02)</name>
  <files>lib/harnessed-common.sh</files>
  <read_first>
    - lib/harnessed-common.sh — read build_images() (lines ~66-103) and ensure_images() (lines ~193-205) to see the exact blocks to modify
  </read_first>
  <action>
Edit `lib/harnessed-common.sh` with two changes:

**Change 1 — Remove omp build block from `build_images()`:**

Remove the entire omp build block (these 5 lines added by plan 07-02):
```bash
    if [ "$force" = "true" ] || ! image_exists "$HARNESSED_OMP_IMAGE"; then
        print_info "Building $HARNESSED_OMP_IMAGE ..."
        "$CONTAINER_RUNTIME" build -t "$HARNESSED_OMP_IMAGE" \
            -f "$HARNESSED_DIR/base/Dockerfile.harnessed-omp" "$HARNESSED_DIR"
    fi
```

Update the comment above `build_images()` from "Build the base, claude, omp, then hatago images
via host `podman build`" back to "Build the base, claude, then hatago image via host `podman build`".

The omp image continues to be built lazily by `ensure_omp_image()` which is called by the isolated
launcher only when an omp stack is requested (HRN-01 contract). `ensure_omp_image()` itself must
NOT be changed.

**Change 2 — Fix `ensure_images()` condition:**

Remove `HARNESSED_OMP_IMAGE` from the `ensure_images()` condition. Change:
```bash
if ! image_exists "$HARNESSED_CLAUDE_IMAGE" || ! image_exists "$HARNESSED_OMP_IMAGE" || ! image_exists "$HARNESSED_HATAGO_IMAGE"; then
```
Back to:
```bash
if ! image_exists "$HARNESSED_CLAUDE_IMAGE" || ! image_exists "$HARNESSED_HATAGO_IMAGE"; then
```

This restores the pre-07-02 behavior: `harnessed build` (bare) builds base, claude, hatago.
omp is lazy-built only when first launching an omp stack.

Do not change any other function in the file.
  </action>
  <verify>
    <automated>
echo "--- build_images must NOT contain omp build block ---"
rg -A3 "Building \$HARNESSED_OMP_IMAGE" /home/mcrowe/Programming/Personal/code-container/lib/harnessed-common.sh | rg "build -t" && echo "FAIL: omp in build_images" || echo "PASS: omp absent from build_images"
echo "--- ensure_images must NOT reference OMP in condition ---"
rg "ensure_images" -A5 /home/mcrowe/Programming/Personal/code-container/lib/harnessed-common.sh | rg "HARNESSED_OMP_IMAGE" && echo "FAIL: OMP in ensure_images" || echo "PASS: ensure_images clean"
echo "--- ensure_omp_image lazy fallback must still exist ---"
rg -c "ensure_omp_image\(\)" /home/mcrowe/Programming/Personal/code-container/lib/harnessed-common.sh && echo "PASS: lazy fallback present"
    </automated>
  </verify>
  <acceptance_criteria>
    - `build_images()` does NOT contain a `"$CONTAINER_RUNTIME" build -t "$HARNESSED_OMP_IMAGE"` invocation
    - `ensure_images()` condition references only `HARNESSED_CLAUDE_IMAGE` and `HARNESSED_HATAGO_IMAGE`
    - `ensure_omp_image()` function is still present and unchanged (the lazy-build path)
    - `rg -c "HARNESSED_OMP_IMAGE" lib/harnessed-common.sh` still shows omp references (in ensure_omp_image and image_exists checks within it) — only the two erroneous call sites are removed
  </acceptance_criteria>
  <done>HRN-01 lazy-build contract restored; build_images() builds base+claude+hatago only; omp is lazy via ensure_omp_image()</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Update ROADMAP.md SC-4 to reflect HRN-01 lazy-build contract for omp</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md — read Phase 7 success criteria section to find SC-4 and SC-1 exact wording before editing
  </read_first>
  <action>
In `.planning/ROADMAP.md`, find Phase 7's success criteria and make two updates:

**SC-1 update:** Change the SC-1 wording to reflect IMG-01 is now fully complete (not partial).
Find text like: "does NOT have claude, omp, codex, or gemini CLIs" (or however SC-1 is worded)
and confirm it still reads correctly after the gap closure. If SC-1 was previously marked as
"partial" or had a note about codex/gemini being deferred, remove that caveat — the gap closure
fully satisfies it.

**SC-4 update:** Find SC-4 (about `harnessed build` bare producing all four images) and update
it to reflect the HRN-01 lazy-build contract. Change it from requiring harnessed-omp in the bare
build to documenting the lazy pattern. The new wording should be:

> `harnessed build` (bare, no stack argument) produces harnessed-base, harnessed-claude, and
> harnessed-hatago; harnessed-omp is lazy-built on first omp stack launch (HRN-01 contract)

This aligns the ROADMAP contract with the intended architecture. The lazy pattern is correct
per HRN-01 — CR-02 in 07-REVIEW.md explicitly identifies the SC-4 vs HRN-01 tension and sides
with HRN-01.

Do not change any other part of ROADMAP.md.
  </action>
  <verify>
    <automated>rg "HRN-01\|lazy" /home/mcrowe/Programming/Personal/code-container/.planning/ROADMAP.md | head -5 && echo "PASS: ROADMAP updated"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 7 SC-4 no longer requires harnessed-omp in the bare build output
    - ROADMAP.md Phase 7 SC-4 references HRN-01 lazy-build contract
    - ROADMAP.md Phase 7 SC-1 no longer has "partial" or "deferred" caveats about codex/gemini
  </acceptance_criteria>
  <done>ROADMAP.md SC-4 aligned with HRN-01 lazy-build contract; SC-1 marked complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codex CLI npm install in harnessed-codex | mise use -g npm:@openai/codex — same install path as before, just in a different Dockerfile layer |
| gemini CLI npm install in harnessed-gemini | mise use -g npm:@google/gemini-cli — same install path as before, just in a different Dockerfile layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | npm:@openai/codex in harnessed-codex | accept | No change in package identity; same install path as before, just moved to the derived image. Existing osv-scanner image scan gate (BLD-02) runs over the built image |
| T-07-06 | Tampering | npm:@google/gemini-cli in harnessed-gemini | accept | Same as above; existing gate covers it |
| T-07-07 | Elevation of Privilege | `mise up` removal from .bashrc | mitigate | Removing auto-upgrade is the fix — users who want upgrades must do `harnessed build` explicitly, which goes through the scan gate |
</threat_model>

<verification>
After plan execution:

1. `rg "@openai/codex" base/Dockerfile.harnessed-base` exits 1 (absent from base)
2. `rg "@google/gemini-cli" base/Dockerfile.harnessed-base` exits 1 (absent from base)
3. `rg "npm:@openai/codex" base/Dockerfile.harnessed-codex` exits 0 (present in codex image)
4. `rg "npm:@google/gemini-cli" base/Dockerfile.harnessed-gemini` exits 0 (present in gemini image)
5. `rg "mise up" base/Dockerfile.harnessed-base` exits 1 (removed from .bashrc)
6. `rg "build_images" -A30 lib/harnessed-common.sh | rg "HARNESSED_OMP_IMAGE"` exits 1 (omp absent from build_images)
7. `rg "ensure_images" -A5 lib/harnessed-common.sh | rg "HARNESSED_OMP_IMAGE"` exits 1 (omp absent from ensure_images condition)
8. `rg -c "ensure_omp_image" lib/harnessed-common.sh` exits 0 (lazy function still exists)
</verification>

<success_criteria>
- IMG-01 fully satisfied: `harnessed-base` has NO harness CLIs (claude, omp, codex, gemini all absent)
- Each harness Dockerfile is self-contained: codex installs its own CLI, gemini installs its own CLI
- Supply-chain scan guarantee restored: no auto-upgrade in .bashrc (CR-01 closed)
- HRN-01 lazy-build contract restored: `harnessed build` (bare) builds only base+claude+hatago; omp is lazy (CR-02 closed)
- ROADMAP SC-1 now fully satisfied (can be verified by re-running gsd-verifier)
</success_criteria>

<output>
Create `.planning/phases/07-fat-base-agent-images/07-03-SUMMARY.md` when done
</output>
