---
phase: 07-fat-base-agent-images
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/claude/agent.yaml
  - agents/omp/agent.yaml
  - lib/harnessed-common.sh
autonomous: true
requirements:
  - IMG-02
must_haves:
  truths:
    - "agents/claude/agent.yaml and agents/omp/agent.yaml exist with type: agent"
    - "build_images() in lib/harnessed-common.sh builds harnessed-omp in addition to base, claude, hatago"
    - "harnessed build (bare, no stack arg) exits 0 and all four images exist afterward"
  artifacts:
    - path: "agents/claude/agent.yaml"
      provides: "Agent descriptor for harnessed-claude (IMG-02)"
      contains: "type: agent"
    - path: "agents/omp/agent.yaml"
      provides: "Agent descriptor for harnessed-omp (IMG-02)"
      contains: "type: agent"
    - path: "lib/harnessed-common.sh"
      provides: "build_images() updated to include harnessed-omp"
      contains: "HARNESSED_OMP_IMAGE"
  key_links:
    - from: "agents/claude/agent.yaml"
      to: "base/Dockerfile.harnessed-claude"
      via: "dockerfile field reference"
      pattern: "dockerfile:.*harnessed-claude"
    - from: "agents/omp/agent.yaml"
      to: "base/Dockerfile.harnessed-omp"
      via: "dockerfile field reference"
      pattern: "dockerfile:.*harnessed-omp"
    - from: "lib/harnessed-common.sh build_images()"
      to: "HARNESSED_OMP_IMAGE"
      via: "ensure_omp_image call or inline build"
      pattern: "HARNESSED_OMP_IMAGE"
---

<objective>
Create the `agents/` directory with `type:agent` descriptor YAML files for the claude and omp harness
images, and update `build_images()` in `lib/harnessed-common.sh` so that `harnessed build` (bare, no
stack argument) produces all four images: `harnessed-base`, `harnessed-claude`, `harnessed-omp`, and
`harnessed-hatago`.

Purpose: IMG-02 formalises the agent layer of the image lineage. The `agents/` directory is the
canonical enumeration of agent images; `build_images()` reads from it implicitly by building all known
agents on a bare build invocation.

Output: `agents/claude/agent.yaml`, `agents/omp/agent.yaml`, updated `lib/harnessed-common.sh`.
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/harnessed-common.sh
@base/Dockerfile.harnessed-claude
@base/Dockerfile.harnessed-omp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create agents/ directory with claude and omp agent.yaml descriptors (IMG-02)</name>
  <files>agents/claude/agent.yaml, agents/omp/agent.yaml</files>
  <read_first>
    - base/Dockerfile.harnessed-claude — verify it is FROM harnessed-base:latest (no changes needed)
    - base/Dockerfile.harnessed-omp — verify it is FROM harnessed-base:latest + omp + bridge (no changes needed)
    - lib/harnessed-common.sh — check HARNESSED_CLAUDE_IMAGE and HARNESSED_OMP_IMAGE constants (lines 16–37)
  </read_first>
  <action>
Create two new files:

`agents/claude/agent.yaml`:
```
type: agent
harness: claude
image: harnessed-claude
dockerfile: base/Dockerfile.harnessed-claude
description: >
  Claude Code harness. FROM harnessed-base + official Claude Code CLI installer.
  Builds without re-downloading runtimes (all in harnessed-base).
```

`agents/omp/agent.yaml`:
```
type: agent
harness: omp
image: harnessed-omp
dockerfile: base/Dockerfile.harnessed-omp
description: >
  Oh My Pi harness. FROM harnessed-base + omp (via mise, pinned v16) +
  pre-installed claude-hooks-bridge extension. Consumes Claude-canonical profiles
  via the bridge at runtime.
```

Do NOT write fenced code blocks in the YAML files — write plain YAML directly. These are static
descriptor files; no processing logic is needed in this task.

Do not create any README or additional documentation files.
  </action>
  <verify>
    <automated>rg "type: agent" /home/mcrowe/Programming/Personal/code-container/agents/claude/agent.yaml && rg "type: agent" /home/mcrowe/Programming/Personal/code-container/agents/omp/agent.yaml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `agents/claude/agent.yaml` exists and contains `type: agent`
    - `agents/claude/agent.yaml` contains `harness: claude`
    - `agents/claude/agent.yaml` contains `dockerfile: base/Dockerfile.harnessed-claude`
    - `agents/omp/agent.yaml` exists and contains `type: agent`
    - `agents/omp/agent.yaml` contains `harness: omp`
    - `agents/omp/agent.yaml` contains `dockerfile: base/Dockerfile.harnessed-omp`
  </acceptance_criteria>
  <done>agents/ directory exists with valid agent.yaml descriptors for claude and omp</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update build_images() to include harnessed-omp in bare build (IMG-02)</name>
  <files>lib/harnessed-common.sh</files>
  <read_first>
    - lib/harnessed-common.sh — read the full build_images() function (lines 66–97) and ensure_images() (lines 193–200) and ensure_omp_image() (lines 203–216) to understand the current lazy-build pattern before editing
  </read_first>
  <action>
In `lib/harnessed-common.sh`, make two edits:

**Edit 1 — `build_images()` function**: After the block that builds `HARNESSED_CLAUDE_IMAGE` (and before the block that builds `HARNESSED_HATAGO_IMAGE`), add a matching block that builds `HARNESSED_OMP_IMAGE`:

```
if [ "$force" = "true" ] || ! image_exists "$HARNESSED_OMP_IMAGE"; then
    print_info "Building $HARNESSED_OMP_IMAGE ..."
    "$CONTAINER_RUNTIME" build -t "$HARNESSED_OMP_IMAGE" \
        -f "$HARNESSED_DIR/base/Dockerfile.harnessed-omp" "$HARNESSED_DIR"
fi
```

Place this block between the claude build block and the hatago build block so the build order is:
base → claude → omp → hatago.

Update the comment above `build_images()` from "Build the base, claude, then hatago image via host
`podman build`" to "Build the base, claude, omp, then hatago images via host `podman build`".

**Edit 2 — `ensure_images()` function**: Update the condition from checking only
`HARNESSED_CLAUDE_IMAGE || HARNESSED_HATAGO_IMAGE` to also check `HARNESSED_OMP_IMAGE`:

Change: `if ! image_exists "$HARNESSED_CLAUDE_IMAGE" || ! image_exists "$HARNESSED_HATAGO_IMAGE"; then`
To:     `if ! image_exists "$HARNESSED_CLAUDE_IMAGE" || ! image_exists "$HARNESSED_OMP_IMAGE" || ! image_exists "$HARNESSED_HATAGO_IMAGE"; then`

This ensures first-run auto-build produces all four required images.

Do not change `ensure_omp_image()` — it remains as the lazy-build fallback for omp stacks when
the image was removed or skipped. The two paths are complementary: `build_images` ensures it on
bare build; `ensure_omp_image` handles the case where someone deleted it between builds.

Do not modify any other functions in the file.
  </action>
  <verify>
    <automated>rg -A5 "Building \$HARNESSED_OMP_IMAGE" /home/mcrowe/Programming/Personal/code-container/lib/harnessed-common.sh | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `lib/harnessed-common.sh` contains a `build_images`-internal block that calls `"$CONTAINER_RUNTIME" build -t "$HARNESSED_OMP_IMAGE"` with `-f "$HARNESSED_DIR/base/Dockerfile.harnessed-omp"`
    - The omp build block appears between the claude build block and the hatago build block
    - `ensure_images()` condition references `HARNESSED_OMP_IMAGE` (so first-run auto-build triggers if omp is missing)
    - `rg -c "ensure_omp_image" lib/harnessed-common.sh` still shows the lazy function is present (unchanged)
    - After running `harnessed build` with both harnessed-base and harnessed-claude already present but harnessed-omp absent: `podman image inspect harnessed-omp:latest` exits 0
  </acceptance_criteria>
  <done>build_images() builds base, claude, omp, hatago in order; harnessed build (bare) produces all four images</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| omp CLI install | Dockerfile.harnessed-omp pulls omp from GitHub via mise at build time (already existing) |
| agent.yaml parsing | Static YAML descriptors; read by humans and future tooling; no code execution path in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | omp download in Dockerfile.harnessed-omp build | accept | Pinned to OMP_VERSION=16.0.1 ARG in Dockerfile.harnessed-omp (established in Phase 4); existing supply-chain gate (BLD-02) scans the built image |
| T-07-04 | Information Disclosure | agent.yaml descriptor files | accept | No secrets; purely structural metadata (image name, dockerfile path, description) |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; Dockerfile.harnessed-omp already exists and was scanned in Phase 4 |
</threat_model>

<verification>
After plan execution:

1. `fd agent.yaml agents/` — returns `agents/claude/agent.yaml` and `agents/omp/agent.yaml`
2. `rg "type: agent" agents/claude/agent.yaml agents/omp/agent.yaml` — exits 0
3. `rg "HARNESSED_OMP_IMAGE" lib/harnessed-common.sh | rg "build -t"` — shows the build invocation inside build_images()
4. Full integration (after plan 07-01 also completes): `harnessed build` exits 0; `podman image inspect harnessed-base:latest harnessed-claude:latest harnessed-omp:latest harnessed-hatago:latest` exits 0
</verification>

<success_criteria>
- `agents/claude/agent.yaml` and `agents/omp/agent.yaml` exist with correct descriptors
- `build_images()` builds omp between claude and hatago
- `harnessed build` (bare invocation) produces harnessed-base, harnessed-claude, harnessed-omp, harnessed-hatago (IMG-02 success criterion 4)
- `podman run --rm harnessed-omp omp --version` exits 0 (IMG-02 success criterion 3; omp was already in the Dockerfile, now reachable via the fat base)
</success_criteria>

<output>
Create `.planning/phases/07-fat-base-agent-images/07-02-SUMMARY.md` when done
</output>
