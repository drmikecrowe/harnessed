---
phase: 06-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-containerized-engine-transparent-stack/01-01-SUMMARY.md
  - .planning/phases/01-containerized-engine-transparent-stack/01-02-SUMMARY.md
  - .planning/phases/01-containerized-engine-transparent-stack/01-03-SUMMARY.md
  - .planning/phases/04-shared-services-recipe-breadth-full-cli/04-02-SUMMARY.md
  - .planning/phases/04-shared-services-recipe-breadth-full-cli/04-03-SUMMARY.md
  - .planning/phases/04-shared-services-recipe-breadth-full-cli/04-04-SUMMARY.md
autonomous: true
requirements: [SC-3]

must_haves:
  truths:
    - "Every `0*-SUMMARY.md` under `.planning/phases/0[1-5]-*/` begins with `---` (well-formed YAML frontmatter) (D-08, D-09, SC-3)."
    - "`01-01-SUMMARY.md`, `01-02-SUMMARY.md`, `01-03-SUMMARY.md` carry the de-facto 02–05 frontmatter schema: `phase` / `plan` / `subsystem` / `tags` + a `# Dependency graph` block (`requires:` / `provides:` / `affects:`) + tech-tracking / key-files / key-decisions / patterns-established / requirements-completed / metrics (D-08, SC-3)."
    - "`04-02-SUMMARY.md`, `04-03-SUMMARY.md`, `04-04-SUMMARY.md` carry the `# Dependency graph` comment header immediately before `requires:` (D-09, SC-3)."
    - "Existing SUMMARY prose body is preserved VERBATIM — only the missing structural pieces (full frontmatter for D-08, the one header line for D-09) are added; no prose, history, or existing frontmatter content is rewritten (D-09, Pitfall 4)."
    - "`.planning/STATE.md` is NOT touched (D-10 — different schema, tool-managed)."
  artifacts:
    - path: ".planning/phases/01-containerized-engine-transparent-stack/01-01-SUMMARY.md"
      provides: "Backfilled YAML frontmatter + # Dependency graph block (D-08) — opens with ---"
      contains: "phase: 01-containerized-engine-transparent-stack"
    - path: ".planning/phases/01-containerized-engine-transparent-stack/01-02-SUMMARY.md"
      provides: "Backfilled YAML frontmatter + # Dependency graph block (D-08) — opens with ---"
      contains: "phase: 01-containerized-engine-transparent-stack"
    - path: ".planning/phases/01-containerized-engine-transparent-stack/01-03-SUMMARY.md"
      provides: "Backfilled YAML frontmatter + # Dependency graph block (D-08) — opens with ---"
      contains: "phase: 01-containerized-engine-transparent-stack"
    - path: ".planning/phases/04-shared-services-recipe-breadth-full-cli/04-02-SUMMARY.md"
      provides: "The missing # Dependency graph header before requires: (D-09)"
      contains: "# Dependency graph"
    - path: ".planning/phases/04-shared-services-recipe-breadth-full-cli/04-03-SUMMARY.md"
      provides: "The missing # Dependency graph header before requires: (D-09)"
      contains: "# Dependency graph"
    - path: ".planning/phases/04-shared-services-recipe-breadth-full-cli/04-04-SUMMARY.md"
      provides: "The missing # Dependency graph header before requires: (D-09)"
      contains: "# Dependency graph"
  key_links:
    - from: "01-02-SUMMARY.md requires:"
      to: "01-01-SUMMARY.md provides:"
      via: "the dependency-graph block wires 01-02 (transparent launcher mounts) to 01-01 (bootstrap + common helpers)"
      pattern: "requires:"
    - from: "01-03-SUMMARY.md requires:"
      to: "01-01 + 01-02 provides:"
      via: "the dependency-graph block wires 01-03 (transparent stack + container alias) to 01-01/01-02"
      pattern: "requires:"
---

<objective>
Normalize the `*-SUMMARY.md` frontmatter to the de-facto 02–05 schema. This implements CONTEXT decisions D-08 (backfill full frontmatter into the three Phase 01 SUMMARYs that currently carry only prose `**Completed:**`/`**Requirements:**` headers), D-09 (insert the missing `# Dependency graph` comment header before `requires:` in the three Phase 04 SUMMARYs that drop it), and D-10 (STATE.md is OUT of scope — different schema, tool-managed).

Purpose: close ROADMAP success criterion SC-3 ("Every phase `*-SUMMARY.md` carries consistent, well-formed frontmatter"). The audit (06-RESEARCH.md C.1) confirms exactly 3 Phase-01 SUMMARYs lack all frontmatter and exactly 3 Phase-04 SUMMARYs lack only the `# Dependency graph` header; all 13 Phase 02–05 SUMMARYs otherwise conform.

Output: 3 backfilled Phase 01 SUMMARYs (full frontmatter prepended, prose body unchanged) + 3 Phase 04 SUMMARYs with the one-line `# Dependency graph` header inserted. No SUMMARY prose or history is rewritten (D-09; Pitfall 4). STATE.md is not touched (D-10).
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tech-debt-cleanup/06-CONTEXT.md
@.planning/phases/06-tech-debt-cleanup/06-RESEARCH.md
@.planning/phases/06-tech-debt-cleanup/06-PATTERNS.md
@.planning/phases/06-tech-debt-cleanup/06-VALIDATION.md
@.planning/phases/02-isolated-tracer-bullet-stack/02-01-SUMMARY.md
@.planning/phases/01-containerized-engine-transparent-stack/01-01-SUMMARY.md
@.planning/phases/01-containerized-engine-transparent-stack/01-02-SUMMARY.md
@.planning/phases/01-containerized-engine-transparent-stack/01-03-SUMMARY.md
@.planning/phases/04-shared-services-recipe-breadth-full-cli/04-02-SUMMARY.md
@.planning/phases/04-shared-services-recipe-breadth-full-cli/04-03-SUMMARY.md
@.planning/phases/04-shared-services-recipe-breadth-full-cli/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill full frontmatter into the three Phase 01 SUMMARYs (D-08)</name>
  <read_first>
    Read the CURRENT version of each file before editing (line numbers are illustrative; anchor on content):
    - `.planning/phases/02-isolated-tracer-bullet-stack/02-01-SUMMARY.md` (lines 1–63) — the CANONICAL template. Copy-adapt its shape VERBATIM; do not invent a new frontmatter form (06-RESEARCH.md "Don't Hand-Roll" row 1). Note its structure: YAML `phase`/`plan`/`subsystem`/`tags`, a `# Dependency graph` block (`requires:`/`provides:`/`affects:`), `# Tech tracking` (`tech-stack:`), `key-files:`, `key-decisions:`, `patterns-established:`, `requirements-completed:`, `# Metrics` (`duration`/`completed`), then a closing `---`, then the existing prose title + body.
    - `.planning/phases/01-containerized-engine-transparent-stack/01-01-SUMMARY.md` — currently opens with an `# Plan 01-01 Summary: ...` H1 and prose `**Completed:**` / `**Requirements:**` lines (no YAML frontmatter). Mine its existing prose body (`## What was built`, `## Key decisions honored`, `## Files`, `## Commits`) to populate `provides:`/`key-files`/`key-decisions`/`requirements-completed`.
    - `01-02-SUMMARY.md` and `01-03-SUMMARY.md` — same prose-only form; mine their prose for the backfill fields.
    - `06-PATTERNS.md` "Shared Pattern 1" — the verbatim YAML template + the Phase-01 backfill specifics table (`requires.phase`, `requirements-completed`, key `provides` per plan).
    - `06-RESEARCH.md` Item C.2 (the backfill schema + the Phase-01 specifics table) and C.1 (confirms all 3 Phase-01 SUMMARYs lack frontmatter).
    - `.planning/REQUIREMENTS.md` traceability table — the `requirements-completed` IDs per Phase-01 plan (ENG-01/ENG-02 for 01-01; MNT-01/MNT-02 for 01-02; ENG-03/MODE--01/MODE-02/AUTH-01/MNT-03 for 01-03).
  </read_first>
  <action>
    PREPEND the full YAML frontmatter block to each of `01-01-SUMMARY.md`, `01-02-SUMMARY.md`, `01-03-SUMMARY.md`, copying the shape from `02-01-SUMMARY.md` (06-PATTERNS.md Shared Pattern 1 verbatim template). PRESERVE the existing prose body VERBATIM below the closing `---` — only prepend frontmatter + the `# Dependency graph` block; do NOT rewrite any prose, history, or decision text (D-08; Pitfall 4). Populate per-plan values from the existing prose (do not invent content):

    Common to all three: `phase: 01-containerized-engine-transparent-stack`, `subsystem: infra` (Phase 01 plans are all infra — bootstrap/images/transparent). Per-plan:
    - `01-01-SUMMARY.md`: `plan: 01`; `tags:` from the plan content (bootstrap, podman, bash, base-image, claude-code, lineage); `requires:` → `(none)` (Phase 01 is the greenfield foundation); `provides:` the `harnessed` bootstrap + `lib/harnessed-common.sh` (build_images/ensure_images/lifecycle) + `base/Dockerfile.harnessed-{base,claude}`; `affects: [01-02-transparent-mounts, 01-03-transparent-stack]`; `requirements-completed: [ENG-01, ENG-02]`; `completed: 2026-06-14` (and `duration`, `key-decisions`, `patterns-established`, `tech-stack`, `key-files` mined from the existing prose — do not fabricate; if the prose does not state a value, omit that field rather than inventing one).
    - `01-02-SUMMARY.md`: `plan: 02`; `requires:` from `01-01` (provides the bootstrap + common helpers); `provides:` `lib/harnessed-mounts.sh` + relocated `lib/egress-firewall.sh` (the §4a host-integration mount layer); `requirements-completed: [MNT-01, MNT-02]`.
    - `01-03-SUMMARY.md`: `plan: 03`; `requires:` from `01-01` + `01-02` (provides the bootstrap + §4a mount layer); `provides:` `lib/harnessed-transparent.sh` + `lib/harnessed-claude-config.sh` + `stacks/transparent/stack.yaml` + the `container` alias; `requirements-completed: [ENG-03, MODE-01, MODE-02, AUTH-01, MNT-03]`.

    Keep the frontmatter shape byte-for-byte consistent with `02-01-SUMMARY.md` (same comment headers `# Dependency graph` / `# Tech tracking` / `# Metrics`, same key ordering). The existing H1 title and all prose below the closing `---` stay UNCHANGED.

    DO NOT touch `.planning/STATE.md` (D-10 — different schema, tool-managed). DO NOT rewrite any prose below the frontmatter (Pitfall 4). DO NOT pad `04-04-SUMMARY.md` or the Phase 01 files with blocks their prose never supported — backfill only the structural pieces the 02–05 schema mandates and that the existing prose supports (D-09 "add only the missing structural pieces").
  </action>
  <verify>
    <automated>For each of the three Phase 01 SUMMARYs: (1) `head -1 01-0N-SUMMARY.md` returns exactly `---` (YAML open). (2) The frontmatter parses as YAML and closes with a matching `---` before the existing H1: `awk 'NR==1{next} /^---$/{print "closed at line " NR; exit}' 01-0N-SUMMARY.md` prints exactly one "closed" line. (3) `grep -c '^phase: 01-containerized-engine-transparent-stack$' 01-0N-SUMMARY.md` == 1. (4) `grep -c '^# Dependency graph$' 01-0N-SUMMARY.md` == 1. (5) `grep -c '^requires:' 01-0N-SUMMARY.md` == 1 AND `grep -c '^provides:' 01-0N-SUMMARY.md` == 1. (6) `requirements-completed` sanity: `grep '^requirements-completed:' 01-01-SUMMARY.md` contains ENG-01 and ENG-02; 01-02 contains MNT-01 and MNT-02; 01-03 contains ENG-03/MODE-01/MODE-02/AUTH-01/MNT-03. (7) Prose-preservation check (Pitfall 4): `git diff 01-0N-SUMMARY.md` shows changes ONLY above the closing `---` (the prepended frontmatter); the existing H1 title and every prose line below it are byte-identical — `git show HEAD:01-0N-SUMMARY.md | tail -n +5` matches `tail -n +<post-frontmatter-line> 01-0N-SUMMARY.md` for the prose body. (8) `grep -c '^plan: 0[1-3]$' 01-0N-SUMMARY.md` == 1.</automated>
  </verify>
  <done>
    Each of `01-01`/`01-02`/`01-03-SUMMARY.md` opens with `---`, carries `phase: 01-containerized-engine-transparent-stack` + the correct `plan:` + `subsystem: infra` + `tags` + a `# Dependency graph` block (`requires:`/`provides:`/`affects:`) + `requirements-completed` matching the REQUIREMENTS traceability, and the existing prose body is byte-identical below the closing `---`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert the missing # Dependency graph header in the three Phase 04 SUMMARYs (D-09)</name>
  <read_first>
    Read the CURRENT version of each file before editing (line numbers are illustrative; anchor on content):
    - `.planning/phases/02-isolated-tracer-bullet-stack/02-01-SUMMARY.md` lines ~5–8 — the CORRECT shape: `tags:` → blank line → `# Dependency graph` comment → `requires:`. This is the one-line-comment class of edit to replicate.
    - `04-02-SUMMARY.md` (gap: `tags:` → blank line → `requires:`, the `# Dependency graph` header is MISSING between the blank and `requires:`), `04-03-SUMMARY.md` (same gap), `04-04-SUMMARY.md` (gap: `gap_closure: true` → blank line → `requires:`, header missing). Confirm the gap in each current file before editing.
    - `06-PATTERNS.md` "Shared Pattern 1" D-09 gap comparison (the exact before/after) and the Item C D-09 row.
    - `06-RESEARCH.md` Item C.1 (confirms 04-02/04-03/04-04 are the FULL D-09 set; 04-01 already HAS the header) and the note on 04-04 (a lighter gap-closure SUMMARY — D-09 scope is narrowly "add the missing `# Dependency graph` header", do NOT pad it with blocks it never had).
    - `04-04-SUMMARY.md` note (06-RESEARCH.md C.1): it only carries `requires`/`provides`/`affects`/`key-files`/`requirements-completed`/`duration`/`completed` — D-09 adds ONLY the missing `# Dependency graph` header, nothing else.
  </read_first>
  <action>
    Insert exactly ONE comment line — `# Dependency graph` — immediately before the `requires:` key in each of `04-02-SUMMARY.md`, `04-03-SUMMARY.md`, `04-04-SUMMARY.md`. This is the identical one-line-comment edit class that makes the three Phase 04 files structurally identical to `02-*/03-*` (06-PATTERNS.md D-09 gap comparison). PRESERVE all existing frontmatter content VERBATIM (D-09: "add only the missing structural pieces, do not rewrite history") — the three files already have correct `phase`/`plan`/`subsystem`/`tags`/`requires`/`provides`/`affects`/etc.; only the single `# Dependency graph` comment line is missing.

    For `04-04-SUMMARY.md`, add ONLY the `# Dependency graph` header — do NOT add `# Tech tracking`/`key-decisions`/`patterns-established` blocks it never had (06-RESEARCH.md C.1 note; D-09 narrowly scopes the edit). (The PATTERNS.md "Observation for the planner" about also normalizing `# Tech tracking` in the same three files is at Claude's discretion; the conservative, D-09-literal choice is to add only `# Dependency graph`. This task does the D-09-literal edit.)

    DO NOT touch `.planning/STATE.md` (D-10). DO NOT rewrite any prose or existing frontmatter content (Pitfall 4). DO NOT touch `04-01-SUMMARY.md` — it already has the header (06-RESEARCH.md C.1).
  </action>
  <verify>
    <automated>For each of `04-02`/`04-03`/`04-04-SUMMARY.md`: (1) `grep -c '^# Dependency graph$' 04-0N-SUMMARY.md` == 1 (header now present). (2) The header sits immediately above `requires:` — `grep -n -A1 '^# Dependency graph$' 04-0N-SUMMARY.md` shows `requires:` as the next non-blank line. (3) `head -1 04-0N-SUMMARY.md` still returns `---` (frontmatter still well-formed). (4) Existing-content preservation (Pitfall 4): `git diff 04-0N-SUMMARY.md` adds exactly the one `# Dependency graph` line and nothing else — `git diff --stat 04-0N-SUMMARY.md` shows 1 insertion, 0 deletions; and `grep -c '^phase: 04-shared-services-recipe-breadth-full-cli$' 04-0N-SUMMARY.md` == 1 (existing phase key intact). (5) `04-01-SUMMARY.md` was NOT touched (already had the header): `git diff --name-only` does not list it.</automated>
  </verify>
  <done>
    `04-02-SUMMARY.md`, `04-03-SUMMARY.md`, `04-04-SUMMARY.md` each carry exactly one `# Dependency graph` header immediately before `requires:`; all other frontmatter content and all prose is byte-identical; `04-01-SUMMARY.md` and STATE.md are untouched. Combined with Task 1, every `0*-SUMMARY.md` under `.planning/phases/0[1-5]-*/` now opens with `---` and has a `# Dependency graph` block (SC-3).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits only `.planning/phases/0[14]-*/*-SUMMARY.md` metadata files (YAML frontmatter + comment headers). It touches NO runtime code, NO config consumed by the `harnessed` tool, NO network path, NO auth/secret flow, NO dependency. There is no trust boundary implicated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering / repudiation | SUMMARY frontmatter (planning metadata) | accept | These files are human/tool-authored planning records, not runtime inputs. No parser consumes them as a trust source. Editing them carries no runtime risk. Accept residual risk: the edits are purely structural (prepend a YAML block / insert a comment line) and preserve existing content verbatim (D-09). |
| T-06-06 | Tampering | Inadvertent prose/history rewrite during backfill | mitigate | Pitfall 4. Mitigation: the verify step asserts `git diff` changes are confined to the added frontmatter (Task 1) / the single inserted header line (Task 2); the prose body below the closing `---` is byte-identical. |
| T-06-07 | Information disclosure | STATE.md (different schema, tool-managed) | mitigate | D-10 excludes STATE.md. The verify step (Task 1) asserts STATE.md is not in `git diff --name-only`. |

This plan is security-neutral — zero new attack surface, no ASVS L1 category newly implicated, no `security_block_on: "high"` trigger (no behavior change at all). These are planning-metadata edits only.
</threat_model>

<verification>
Plan-level gate (per 06-VALIDATION.md / D-11) — run after BOTH tasks complete:
- Static frontmatter hygiene check (the SC-3 observable signal): `for f in .planning/phases/0*-*/0*-SUMMARY.md; do head -1 "$f"; done` — EVERY file prints `---` (all 16 SUMMARYs now open with YAML frontmatter).
- `# Dependency graph` header present in every SUMMARY: `for f in .planning/phases/0*-*/0*-SUMMARY.md; do printf '%s: ' "$f"; grep -c '^# Dependency graph$' "$f"; done` — every file prints a count >= 1.
- D-10 exclusion: `git diff --name-only | grep -c 'STATE.md'` == 0 (STATE.md untouched).
- Prose-preservation (Pitfall 4): the diffs for the 6 touched files show only frontmatter additions (Task 1) / one-line header insertions (Task 2); no prose line below a closing `---` changed.
- No runtime regression check needed — these are planning-metadata files with zero runtime effect. (The plan-level integration gate `harnessed test ping-time && harnessed test tracer-time && bash tools/uat/run-uat.sh` is shared with Plan 06-01 and is the phase-wide regression gate; it is NOT affected by SUMMARY edits. Run it once at the phase boundary per D-11.)
</verification>

<success_criteria>
- SC-3 (every phase `*-SUMMARY.md` carries consistent, well-formed frontmatter): all 16 `0*-SUMMARY.md` files under `.planning/phases/0[1-5]-*/` open with `---` and contain a `# Dependency graph` block (`requires:`/`provides:`/`affects:`) + `phase`/`plan`/`subsystem`/`tags`. The 3 Phase 01 SUMMARYs are backfilled (D-08); the 3 Phase 04 SUMMARYs have the `# Dependency graph` header (D-09); STATE.md is untouched (D-10); existing prose/history is preserved verbatim (Pitfall 4).
</success_criteria>

<output>
Create `.planning/phases/06-tech-debt-cleanup/06-02-SUMMARY.md` when done. Use the de-facto 02–05 SUMMARY frontmatter schema (copy the shape from `02-01-SUMMARY.md`) — including `phase: 06-tech-debt-cleanup`, `plan: 02`, a `# Dependency graph` block, and `requirements-completed: [SC-3]` (this phase carries no formal REQ IDs — use the ROADMAP success criteria).
</output>
