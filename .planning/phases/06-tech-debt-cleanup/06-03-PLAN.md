---
phase: 06-tech-debt-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/codebase/INTEGRATIONS.md
  - services/ping/server.py
  - tools/harnessed/schema.py
  - CLAUDE.md
  - stacks/ping-time/stack.yaml
  - harnessed
autonomous: true
requirements: [SC-1]

must_haves:
  truths:
    - "A repo-wide search for `harnessed-net` (excluding `.planning/` and `.claude/`) returns ONLY live references (`ensure_named_net`/`ensure_harnessed_net`, the `HARNESSED_NET:-` opt-in path, the live ping service code), the `HARNESSED_NET` opt-in escape hatch, or now-accurate docs/comments — NO comment, docstring, doc section, heading, or ASCII-diagram edge asserts the rootless bridge as the current/default service network (SC-1, D-01, D-05)."
    - "The stale `http://ping:8080/mcp` / `http://<name>:<port>/mcp` URL assertions in `services/ping/server.py` (docstring), `tools/harnessed/schema.py` (ServiceDef docstring), and `docs/codebase/INTEGRATIONS.md` are corrected to `http://host.containers.internal:<port>/mcp`, with the old DNS-name form labeled as the `HARNESSED_NET` opt-in bridge form (mirrors the B4 correction shipped in `recipes/ping/recipe.yaml:4`)."
    - "The 6 files the original 06-01 audit missed — `docs/codebase/INTEGRATIONS.md` (4 locations), `services/ping/server.py:6-7` (docstring), `tools/harnessed/schema.py:143-146` (ServiceDef docstring), `CLAUDE.md:153` (Engine bullet), `stacks/ping-time/stack.yaml:5`, `harnessed:51/253` (help string + comment) — now state the publish-to-`0.0.0.0` + `host.containers.internal:<port>` model with `HARNESSED_NET` as the opt-in, using vocabulary already shipped in `lib/harnessed-services.sh:4-6` and `recipes/ping/recipe.yaml:4-7` verbatim."
    - "NO executable code changed (D-12): the `server.py` edit is the module docstring (lines ~6-7) ONLY — the `allowed_hosts` body at `:19-25` and every function stays; `schema.py` edit is the `ServiceDef` docstring ONLY — no dataclass field or type changes; `harnessed` edits are the `svc up` help string and a section comment ONLY — no control flow / dispatch / function changes; `stack.yaml` edit is a comment ONLY — no YAML key/value changes; `CLAUDE.md`/`INTEGRATIONS.md` edits are prose ONLY."
    - "OPEN `[INFERENCE]` markers are untouched (D-06); the 4 D-07 replacement-documenting comments (`tools/harnessed/assemble.py:63-67`, `lib/harnessed-services.sh:98-102`, `lib/harnessed-isolated.sh:121-126`, `docs/guides/service-authoring.md:163-166`) are untouched; the publish+host-gateway pivot is NOT reversed (D-04); `ensure_harnessed_net`/`ensure_named_net`/the `HARNESSED_NET:-harnessed-net` default remain (D-03)."
    - "`bash -n harnessed` exits 0 (the help-string + comment edits did not break shell parsing); `python3 -c 'import ast; ast.parse(...)'` on `services/ping/server.py` and `tools/harnessed/schema.py` exits 0 (the docstring/comment edits did not break Python parse)."
  artifacts:
    - path: "docs/codebase/INTEGRATIONS.md"
      provides: "Corrected §'HTTP service sidecars' (bridge-as-network + resolved URL), §'Shared Services' heading + body, and the §'Summary' ASCII diagram edge to the host-gateway + HARNESSED_NET opt-in model"
      contains: "host.containers.internal"
    - path: "services/ping/server.py"
      provides: "Corrected module docstring (lines ~6-7) so the docstring matches the file's own accurate host.containers.internal + allowed_hosts body (lines ~19-25)"
      contains: "host.containers.internal"
    - path: "tools/harnessed/schema.py"
      provides: "Corrected ServiceDef docstring (lines ~143-146) — bridge-as-network + old DNS-name URL → host-published + host.containers.internal URL with HARNESSED_NET opt-in"
      contains: "host.containers.internal"
    - path: "CLAUDE.md"
      provides: "Corrected Engine bullet (~line 153) — pod on rootless (pasta) by default, host-gateway reachability, HARNESSED_NET opt-in"
      contains: "rootless (pasta)"
    - path: "stacks/ping-time/stack.yaml"
      provides: "Corrected sidecar comment (~line 5) — host-gateway proxy, HARNESSED_NET opt-in"
      contains: "host.containers.internal"
    - path: "harnessed"
      provides: "Corrected `svc up` help string (~line 51) + service-lifecycle section comment (~line 253) — host-published sidecar framing"
      contains: "host.containers.internal"
  key_links:
    - from: "services/ping/server.py module docstring"
      to: "lib/harnessed-services.sh:4-6 (B1-corrected file header) + recipes/ping/recipe.yaml:4-7 (B4-corrected service-resolution comment)"
      via: "docstring rewritten to MIRROR the shipped publish+host-gateway vocabulary verbatim, resolving the internal inconsistency with its own body at server.py:19-25"
      pattern: "host\\.containers\\.internal"
    - from: "tools/harnessed/schema.py ServiceDef docstring"
      to: "lib/harnessed-services.sh:4-6 (B1 wording) + recipes/ping/recipe.yaml:4-7 (B4 wording)"
      via: "identical B1+B4 correction class — 'own image/container/volume on harnessed-net' → host-published; old DNS-name URL → host.containers.internal URL"
      pattern: "host\\.containers\\.internal"
    - from: "docs/codebase/INTEGRATIONS.md (§HTTP service sidecars + §Shared Services + §Summary diagram)"
      to: "lib/harnessed-services.sh:4-6 + recipes/ping/recipe.yaml:4-7 + docs/harnessed-design.md §9 (PRIMARY-model framing)"
      via: "the four stale assertions mirror the same B1/B4 wording already corrected in the scoped files; corrected to match the reconciled design source of truth"
      pattern: "host\\.containers\\.internal"
---

<objective>
Close ROADMAP success criterion **SC-1** at the repo-wide level by extending the B1+B4 correction (already shipped in plan 06-01 for `lib/harnessed-services.sh`, `services/ping/service.yaml`, and `recipes/ping/recipe.yaml`) to the 6 files the original `06-RESEARCH.md` Item B audit missed. `06-VERIFICATION.md` returned `status: gaps_found`: SC-2 and SC-3 PASS, but **SC-1 FAIL (gaps)** — a repo-wide grep still returns stale `harnessed-net`-bridge-as-default assertions and the old DNS-name URL `http://<name>:<port>/mcp` in un-audited files. This plan (06-03) closes that scope-completeness gap.

Every edit is a **comment / docstring / doc / help-string edit** with zero runtime risk (D-12). Each correction MIRRORS vocabulary already shipped in the reconciled `lib/harnessed-services.sh:4-6` (B1) and `recipes/ping/recipe.yaml:4-7` (B4) verbatim — the planner authors NO new networking prose. This honors CONTEXT decisions D-01 (reconcile to shipped reality), D-05 (scope = any comment/doc contradicting shipped behavior — "**any** code comment still asserting DNS-by-name over the bridge"), D-06 (EXCLUDE OPEN `[INFERENCE]` markers), D-07 (KEEP replacement-documenting comments), D-11/D-12 (no behavior change).

Purpose: a repo-wide `git grep -n 'harnessed-net'` (excluding `.planning/`/`.claude/`) returns ONLY live / opt-in / now-accurate references; the stale bridge-as-default assertions and the old DNS-name-URL comments in the 6 files are corrected to the shipped publish-to-`0.0.0.0` + `host.containers.internal:<port>` PRIMARY model with `HARNESSED_NET` as the opt-in; no executable code changed.

Output: corrected comments/docstrings/doc in `docs/codebase/INTEGRATIONS.md`, `services/ping/server.py` (docstring), `tools/harnessed/schema.py` (docstring), `CLAUDE.md`, `stacks/ping-time/stack.yaml`, `harnessed`. This plan does NOT redo 06-01's design.md reconciliation or the B1–B7 scoped sweep — it ONLY extends the same correction to the files 06-01 missed.
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-tech-debt-cleanup/06-VERIFICATION.md
@.planning/phases/06-tech-debt-cleanup/06-CONTEXT.md
@.planning/phases/06-tech-debt-cleanup/06-01-PLAN.md
@.planning/phases/06-tech-debt-cleanup/06-01-SUMMARY.md
@lib/harnessed-services.sh
@recipes/ping/recipe.yaml
@docs/harnessed-design.md
@docs/codebase/INTEGRATIONS.md
@services/ping/server.py
@tools/harnessed/schema.py
@CLAUDE.md
@stacks/ping-time/stack.yaml
@harnessed
</context>

<tasks>

<task type="auto">
  <name>Task 1: HIGH-confidence corrections — extend the B1+B4 correction to the 4 un-audited files asserting bridge-as-network + the old DNS-name URL (D-01, D-05, D-07, SC-1)</name>
  <read_first>
    Read the CURRENT versions of each file — the working tree has drifted ~7 lines from committed HEAD, so anchor on the CONTENT (the exact stale phrase) below, NOT on line numbers. Re-read each range immediately before editing it. THEN read the wording source to MIRROR verbatim (do NOT paraphrase or author new prose):
    - `lib/harnessed-services.sh:1-12` — the B1-corrected file header (the authoritative wording for "own image/container/volume on a host-published port").
    - `recipes/ping/recipe.yaml:1-10` — the B4-corrected service-resolution comment (the authoritative wording for the `host.containers.internal:8080/mcp` URL + the `HARNESSED_NET` opt-in bridge form).
    - `docs/harnessed-design.md` §9 (the `## 9. State & lifecycle` "Shared instance, concurrent" bullet ~line 256-269) — the PRIMARY-model framing ("publishes its port to `0.0.0.0` ... `host.containers.internal:<port>` (the primary reachability model); the `harnessed-net` bridge + DNS-by-name is the `HARNESSED_NET` opt-in").
    - `.planning/phases/06-tech-debt-cleanup/06-VERIFICATION.md` — the SC-1 gap spec (the authoritative list of stale assertions per file).
    - For each target file, ALSO read the accurate shipped code the comment/doc should now match:
      - `services/ping/server.py` — read lines ~6-7 (the STALE docstring) AND lines ~19-25 (the ACCURATE `host.containers.internal` + `allowed_hosts` body that design.md §9 cites as canonical). The docstring must converge to its own body.
      - `tools/harnessed/schema.py` — read lines ~143-146 (the STALE ServiceDef docstring). The shipped URL rewrite lives in `tools/harnessed/assemble.py:63-67` (the D-07 replacement-doc — leave it; it confirms the host-gateway URL is `http://host.containers.internal:{port}/mcp`).
      - `docs/codebase/INTEGRATIONS.md` — read §"HTTP service sidecars" (~104-110), §"Shared Services" (~117-127), and §"Summary" ASCII diagram (~311-331). ALSO read `tools/harnessed/assemble.py:51-67` to confirm the shipped resolved URL form.
      - `CLAUDE.md` — read line ~153 (the STALE Engine bullet). ALSO read `lib/harnessed-isolated.sh:121-135` to confirm the pod is on rootless (pasta) by default and `harnessed-net` is the `HARNESSED_NET` opt-in (gated on `[ -n "${HARNESSED_NET:-}" ]`).
  </read_first>
  <action>
    Edit the 4 HIGH-confidence files. Mirror the shipped vocabulary verbatim — do NOT invent networking prose. Each edit changes ONLY the comment/docstring/doc prose; change NO executable code, NO YAML key/value, NO dataclass field.

    **G1 — `docs/codebase/INTEGRATIONS.md`** (4 stale locations, mirror B1/B4/design §9):
    - §"HTTP service sidecars" body (~107): the clause `service runs as its **own** container on \`harnessed-net\`.` → rewrite to `service runs as its **own** container on a host-published port (reachable via \`host.containers.internal:<port>\`; or by DNS name over the \`HARNESSED_NET\` bridge on bridge-capable hosts).` (mirrors `lib/harnessed-services.sh:4-6` B1 verbatim).
    - §"HTTP service sidecars" canonical example (~110): the resolved URL `{url: http://ping:8080/mcp, type: http}` → rewrite to `{url: http://host.containers.internal:8080/mcp, type: http}` (the host-gateway form; \`http://ping:8080/mcp\` is the \`HARNESSED_NET\` opt-in bridge form).` (mirrors `recipe.yaml:4-7` B4 verbatim).
    - §"Shared Services" heading (~117): `## Shared Services (sidecars over harnessed-net)` → `## Shared Services (host-published sidecars)`.
    - §"Shared Services" body (~119): `A shared service is its **own image/container/volume** on \`harnessed-net\`, with a lifecycle **independent of any instance** (design §3/§9).` → `A shared service is its **own image/container/volume** on a host-published port (reachable via \`host.containers.internal:<port>\`; or by DNS name over the \`HARNESSED_NET\` bridge on bridge-capable hosts), with a lifecycle **independent of any instance** (design §3/§9).` (mirrors B1 verbatim; keep the rest of the section — lifecycle bullets, service-scoped-volume note, ping description — unchanged).
    - §"Summary" ASCII diagram edge (~326): the line `└── HTTP proxies ──▶ shared services on harnessed-net` → rewrite to `└── HTTP proxies ──▶ shared services (host.containers.internal:<port>)`. Keep the following `(ping FastMCP :8080; ...)` line and the rest of the diagram unchanged.

    **G2 — `services/ping/server.py`** (module docstring ~6-7 ONLY; leave the body ~19-25 and all functions unchanged):
    - The two docstring lines `The service runs standalone on harnessed-net; hatago proxies it as a network-native` / `MCP server ({url: http://ping:8080/mcp, type: http}). The /health route is what` → rewrite to `The service publishes its port to \`0.0.0.0\`; hatago proxies it via the podman host` / `gateway \`host.containers.internal:8080\` as a network-native MCP server` and continue `({url: http://host.containers.internal:8080/mcp, type: http}; \`http://ping:8080/mcp\` is the \`HARNESSED_NET\` opt-in bridge form). The /health route is what`. This makes the docstring match the file's own accurate body at ~19-25 (the `host.containers.internal` + `allowed_hosts` impl design.md §9 cites). PRESERVE the docstring's `"""` delimiters and the surrounding sentences (`/health` readiness clause, design §9 lifecycle reference) — rewrite ONLY the stale reachability clause.

    **G3 — `tools/harnessed/schema.py`** (the `ServiceDef` docstring ~143-146 ONLY; change no dataclass field/type):
    - The docstring lines `A service is its OWN image/container/volume on \`harnessed-net\`, with a lifecycle` / `independent of any instance. A recipe references it via \`mcp.servers[].service\`;` / `the assembler resolves the service name → a hatago URL-proxy entry pointing at` / `\`http://<name>:<port>/mcp\` (plan 04-01 Task 4).` → rewrite to `A service is its OWN image/container/volume on a host-published port` / `(reachable via \`host.containers.internal:<port>\`; or by DNS name over the \`HARNESSED_NET\`` / `bridge on bridge-capable hosts), with a lifecycle independent of any instance. A recipe` / `references it via \`mcp.servers[].service\`; the assembler resolves the service name →` / `a hatago URL-proxy entry pointing at \`http://host.containers.internal:<port>/mcp\`` / `(the host-gateway form; \`http://<name>:<port>/mcp\` is the \`HARNESSED_NET\` opt-in bridge form` / `— plan 04-01 Task 4).` (mirrors B1 + B4 verbatim). PRESERVE the `"""` delimiters and the `@dataclass`/`class ServiceDef:` lines.

    **G4 — `CLAUDE.md`** (Engine bullet ~153, prose ONLY):
    - The line `- **Engine:** podman **pod** \`harnessed-<stack>-<projhash>\` on \`harnessed-net\`: harness container + **hatago** + shared services by reference.` → rewrite to `- **Engine:** podman **pod** \`harnessed-<stack>-<projhash>\` on rootless (pasta) networking by default (shared services are host-published, reached via \`host.containers.internal:<port>\`; \`HARNESSED_NET\` is the opt-in bridge for bridge-capable hosts): harness container + **hatago** + shared services by reference.` (mirrors `lib/harnessed-isolated.sh:121-135` — pod on pasta by default, `harnessed-net` gated on `${HARNESSED_NET:-}`).

    DO NOT touch the D-07 replacement-documenting comments (`assemble.py:63-67`, `harnessed-services.sh:98-102`, `harnessed-isolated.sh:121-126`, `service-authoring.md:163-166`). DO NOT touch any OPEN `[INFERENCE]` marker (D-06). DO NOT reverse the publish+host-gateway pivot (D-04). DO NOT remove `ensure_harnessed_net`/`ensure_named_net`/the `HARNESSED_NET:-harnessed-net` default (D-03). DO NOT edit any file outside `files_modified`.
  </action>
  <verify>
    <automated>(1) Python parse: `python3 -c 'import ast; ast.parse(open("services/ping/server.py").read()); ast.parse(open("tools/harnessed/schema.py").read())'` exits 0 (the docstring edits did not break Python parse). (2) Old-URL-is-opt-in-only: G1/G2/G3 RETAIN the old DNS-name URL ONLY as the labeled `HARNESSED_NET` opt-in bridge form (mirroring `recipe.yaml` B4), so a naive 0-match grep is WRONG. Assert no old-URL occurrence lacks the opt-in qualifier: `grep 'http://ping:8080/mcp\|http://<name>:<port>/mcp' docs/codebase/INTEGRATIONS.md services/ping/server.py tools/harnessed/schema.py | grep -v 'HARNESSED_NET'` returns 0 (every surviving old-URL line carries the `HARNESSED_NET` opt-in label); the PRIMARY/resolved URL is now `host.containers.internal` (asserted in check 3). `recipes/ping/recipe.yaml` (B4 source) and `tools/harnessed/assemble.py` (D-07 replacement-doc) remain intentionally out of scope. (3) Correct-host-gateway-present: `grep -rn 'host.containers.internal' docs/codebase/INTEGRATIONS.md services/ping/server.py tools/harnessed/schema.py` returns ≥1 match per file. (4) Stale-bridge-absent on the HIGH set: `grep -n 'on harnessed-net' docs/codebase/INTEGRATIONS.md services/ping/server.py tools/harnessed/schema.py CLAUDE.md` returns 0 matches asserting the bridge as the default service network (the only remaining `harnessed-net` refs are the `HARNESSED_NET` opt-in form or the `harnessed-<stack>-<projhash>` pod NAME, which is unrelated). (5) Diff review: `git diff services/ping/server.py tools/harnessed/schema.py` shows ONLY docstring lines changed — the `allowed_hosts` body (`server.py:19-25`) and the `ServiceDef` dataclass fields are byte-identical.
    </automated>
  </verify>
  <done>
    The 4 HIGH-confidence files assert the publish+host-gateway PRIMARY model with `HARNESSED_NET` as the opt-in: `INTEGRATIONS.md` §HTTP service sidecars (body + resolved URL), §Shared Services (heading + body), §Summary diagram edge; `server.py` docstring matches its own accurate body; `schema.py` ServiceDef docstring mirrors B1+B4; `CLAUDE.md` Engine bullet states pasta-by-default + host-gateway + opt-in. The stale "own ... on `harnessed-net`" bridge-as-default assertions are gone; the old DNS-name URL survives ONLY as the labeled `HARNESSED_NET` opt-in bridge form (mirroring B4), NOT as the resolved/primary URL; `ast.parse` passes on both Python files; no executable code, no dataclass field, no YAML key changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: MEDIUM-confidence corrections — drop "over/on harnessed-net" transport phrasing in the stack manifest comment + the CLI launcher help/comment (SC-1 repo-wide)</name>
  <read_first>
    Anchor on CONTENT, not line numbers (WIP has drifted ~7 lines from committed HEAD). Re-read each range immediately before editing. THEN read the wording source:
    - `lib/harnessed-services.sh:1-12` (B1 wording) and `recipes/ping/recipe.yaml:1-10` (B4 wording) — the shipped vocabulary to MIRROR.
    - `.planning/phases/06-tech-debt-cleanup/06-VERIFICATION.md` — the SC-1 MEDIUM list.
    - `stacks/ping-time/stack.yaml` — read the full file (12 lines); the stale comment is at ~line 5, the YAML keys (`name`/`config`/`harness`/`recipes`/`services`) are the oracle and MUST stay unchanged.
    - `harnessed` — read ~lines 44-59 (the usage block; the stale `svc up` help string is at ~51) AND ~lines 252-256 (the service-lifecycle section comment; the stale line is ~253). Confirm the `svc up` dispatch at ~260 (`svc_up "$SVC_TARGET"`) and the `if [ -n "$SVC_ACTION" ]; then` gate are the live control flow — DO NOT touch them.
  </read_first>
  <action>
    Edit the 2 MEDIUM-confidence locations. Keep them concise — rephrase to drop the bridge-as-transport assertion in favor of host-gateway reachability, mentioning `HARNESSED_NET` as the opt-in where natural. Each edit is a comment / help-string edit ONLY.

    **G5 — `stacks/ping-time/stack.yaml`** (comment ~line 5 ONLY; change no YAML key/value):
    - The comment clause `server proxied by hatago over harnessed-net. The \`services: [ping]\` field auto-starts the` → rewrite to `server proxied by hatago via the host gateway \`host.containers.internal\` (the \`HARNESSED_NET\` bridge is the opt-in for bridge-capable hosts). The \`services: [ping]\` field auto-starts the`. Keep the preceding line (`#\`time\` is a stdio MCP server ...`) and the following line (`# ping sidecar on launch (ensure_service_up) — a standalone container, not a pod member.`) unchanged.

    **G6 — `harnessed`** (CLI launcher — the `svc up` help string ~line 51 AND the service-lifecycle section comment ~line 253; change NO control flow / dispatch / function):
    - Help string (~51): `  harnessed svc up <service>      Start a shared service sidecar on harnessed-net (own image + volume)` → rewrite to `  harnessed svc up <service>      Start a shared service sidecar (own image + volume; host-published,` and continue on the wrapped line to align under the `Start` column: `                                  reached via host.containers.internal — HARNESSED_NET bridge is opt-in)`. PRESERVE the column alignment and indentation of the usage block so the help render stays tidy.
    - Section comment (~253): the line `# \`harnessed svc up|down|list <service>\` manages shared service sidecars on harnessed-net` → rewrite to `# \`harnessed svc up|down|list <service>\` manages shared service sidecars (host-published,` and continue the next comment line to drop the stale clause: `# reached via host.containers.internal; HARNESSED_NET bridge is the opt-in for bridge-capable hosts)`. Keep the following comment lines (`# (own image/container/volume, labelled harnessed-service=<name>). Dispatch exits ...`) and the `if [ -n "$SVC_ACTION" ]; then` gate UNCHANGED.

    DO NOT change any executable bash (no dispatch, no `case` branch, no function). DO NOT touch any file outside `files_modified`.
  </action>
  <verify>
    <automated>(1) Shell parse: `bash -n harnessed` exits 0 (the help-string + comment edits did not break shell parsing). (2) YAML stays valid (comment-only edit): `python3 -c 'import yaml; yaml.safe_load(open("stacks/ping-time/stack.yaml"))'` exits 0 and returns a dict with `name: ping-time` (the edit touched a comment only). (3) Stale-transport-absent on the MEDIUM set: `grep -n 'over harnessed-net\|on harnessed-net' stacks/ping-time/stack.yaml harnessed` returns 0 matches asserting the bridge as the service transport (the remaining `harnessed-net` refs, if any, are the `HARNESSED_NET` opt-in form). (4) Correct-wording-present: `grep -n 'host.containers.internal' stacks/ping-time/stack.yaml harnessed` returns ≥1 match per file. (5) Diff review: `git diff stacks/ping-time/stack.yaml harnessed` shows ONLY comment / help-string lines changed — no YAML key and no control-flow line is in the diff.
    </automated>
  </verify>
  <done>
    The 2 MEDIUM-confidence locations no longer assert `harnessed-net` as the service transport: `stacks/ping-time/stack.yaml:5` states host-gateway proxy with `HARNESSED_NET` opt-in; the `harnessed` CLI `svc up` help string (~51) and the service-lifecycle section comment (~253) state host-published sidecar framing. `bash -n harnessed` passes; the stack YAML still parses with `name: ping-time`; no YAML key or control-flow line changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is comment / docstring / doc / help-string edits ONLY. It introduces NO new network path, input handler, secret flow, dependency, or trust boundary. The existing boundaries (host↔pod egress firewall, hatago MCP proxy, service-scoped volumes, FastMCP `allowed_hosts`) are unchanged — this plan only corrects prose to describe the controls that already ship. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | `harnessed` help-string + comment edits under `set -euo pipefail`; `server.py`/`schema.py` docstring edits | mitigate | A malformed comment/edit (unclosed quote, broken docstring `"""`, column-shift breaking the usage block) turns into a launch-time or import-time failure. Mitigation: `bash -n harnessed` (Task 2 verify) + `python3 ast.parse` on both Python files (Task 1 verify) after each edit. Static gates are the load-bearing check (D-12: zero runtime risk). |
| T-06-03-02 | Repudiation / info disclosure | Accidentally editing an OPEN `[INFERENCE]` marker (D-06) or a D-07 replacement-doc, or reversing the publish+host-gateway pivot (D-04) | mitigate | The `<action>` forbids all three explicitly; no target file is `docs/harnessed-design.md` (the `[INFERENCE]` host) or `tools/harnessed/assemble.py` (D-07 replacement-doc host) or `lib/harnessed-isolated.sh` (the `$net`/live networking block). The plan-level verification greps the diff to confirm `[INFERENCE]` markers and the 4 replacement-docs are untouched. |

This plan is security-neutral (ASVS L1 categories are NOT newly implicated; no new input handler, auth path, secret flow, or trust boundary is added). `security_block_on: "high"` is not triggered — there is no behavior change. Accept residual risk on the documentation-only change: the correction makes docs/comments match the controls that already ship (publish-to-`0.0.0.0`, egress-firewall `169.254.1.2` allow rule, FastMCP `allowed_hosts`); it adds/removes/widens no rule.
</threat_model>

<verification>
Plan-level integration gate (per 06-VALIDATION.md / D-11 / D-12) — run after BOTH tasks complete. Static checks are load-bearing for this comment/doc-only plan (zero runtime risk per D-12):

- `bash -n harnessed` exits 0 (the `svc up` help-string + the service-lifecycle comment edits did not break shell parsing).
- `python3 -c 'import ast; ast.parse(open("services/ping/server.py").read()); ast.parse(open("tools/harnessed/schema.py").read())'` exits 0 (the docstring edits did not break Python parse).
- `python3 -c 'import yaml; yaml.safe_load(open("stacks/ping-time/stack.yaml"))'` exits 0 and returns `{'name': 'ping-time', ...}` (the comment edit did not break YAML; the keys/values are unchanged).
- Repo-wide stale-bridge grep (SC-1): `git grep -n 'harnessed-net' -- ':!.planning' ':!.claude'` returns ONLY live references (`ensure_named_net`/`ensure_harnessed_net`, the `HARNESSED_NET:-` opt-in path, the live ping service code, the now-accurate `HARNESSED_NET` opt-in phrasing) — assert the 6 HIGH/MEDIUM files (`docs/codebase/INTEGRATIONS.md`, `services/ping/server.py`, `tools/harnessed/schema.py`, `CLAUDE.md`, `stacks/ping-time/stack.yaml`, `harnessed`) no longer assert the bridge as the default/current service network. (If a ref to the literal name `harnessed-net` remains in these files, it MUST be in the `HARNESSED_NET` opt-in form, not as the default model.)
- Old-URL-is-opt-in-only (G1/G2/G3 RETAIN the old DNS-name URL only as the labeled `HARNESSED_NET` opt-in bridge form, mirroring `recipe.yaml` B4 — so a naive 0-match grep is wrong): `git grep -n 'http://ping:8080/mcp\|http://<name>:<port>/mcp' -- docs/codebase/INTEGRATIONS.md services/ping/server.py tools/harnessed/schema.py | grep -v 'HARNESSED_NET'` returns 0 — every surviving old-URL line carries the `HARNESSED_NET` opt-in label; the PRIMARY/resolved URL is `http://host.containers.internal:<port>/mcp`. `recipes/ping/recipe.yaml` (B4 source) and `tools/harnessed/assemble.py` (D-07 replacement-doc) remain intentionally out of scope.
- D-06/D-07 integrity: `git grep -n '\[INFERENCE' -- docs/harnessed-design.md` returns the same markers as committed HEAD (no OPEN `[INFERENCE]` marker edited); `git diff -- tools/harnessed/assemble.py lib/harnessed-services.sh lib/harnessed-isolated.sh docs/guides/service-authoring.md` is empty (the 4 D-07 replacement-docs and their host files are untouched by THIS plan — 06-01 already shipped those, and 06-03 does not re-touch 06-01's files).

**Deferred to `/gsd-verify-work`** (same rootless-podman + clean-tree constraint as 06-01/06-02 — do NOT gate this comment-only plan on them):
- **status: deferred** — `harnessed test ping-time` (asserts the `ping` MCP connects via `host.containers.internal` — SC-1 live regression gate). Rootless `podman.socket` is inactive on the verification host; additionally the working tree carries uncommitted multi-harness WIP in files under test, so a live run would not isolate this phase's comment-only edits.
- **status: deferred** — `harnessed test tracer-time` (isolated pod + `time` MCP). Same podman-socket constraint.
- **status: deferred** — `bash tools/uat/run-uat.sh` (the UAT suite — D-12 no-behavior-change gate). Same constraint.

Re-run all three before `/gsd-verify-work` once the tree is committable and rootless podman is available. The doc/comment edits still land (zero-runtime-risk per D-12); only the live legs defer.
</verification>

<success_criteria>
- **SC-1 (repo-wide — the criterion this plan closes):** a repo-wide `git grep -n 'harnessed-net' -- ':!.planning' ':!.claude'` returns ONLY live / opt-in / now-accurate references. The stale bridge-as-default assertions in the 6 files (`docs/codebase/INTEGRATIONS.md` ×4 locations, `services/ping/server.py` docstring, `tools/harnessed/schema.py` ServiceDef docstring, `CLAUDE.md` Engine bullet, `stacks/ping-time/stack.yaml:5`, `harnessed:51`/`:253`) are corrected to the shipped publish-to-`0.0.0.0` + `host.containers.internal:<port>` PRIMARY model with `HARNESSED_NET` as the opt-in; the stale `http://ping:8080/mcp` / `http://<name>:<port>/mcp` URL comments are corrected to `http://host.containers.internal:<port>/mcp`. No executable code, no YAML key/value, no dataclass field, no control flow changed (D-12). OPEN `[INFERENCE]` markers untouched (D-06); the 4 D-07 replacement-documenting comments untouched. `bash -n harnessed` passes; `ast.parse` passes on both Python files; the stack YAML still parses.

*(SC-2 and SC-3 were PASS in `06-VERIFICATION.md` and are NOT in this plan's `requirements` — this plan does not revisit them.)*
</success_criteria>

<output>
Create `.planning/phases/06-tech-debt-cleanup/06-03-SUMMARY.md` when done. Use the SAME SUMMARY frontmatter schema as `06-01-SUMMARY.md` (`phase` / `plan` / `subsystem` / `tags` + `# Dependency graph` block with `requires:` / `provides:` / `affects:` + `# Tech tracking` `tech-stack` + `key-files` created/modified + `key-decisions` / `requirements-completed` / `metrics`) — copy the shape from `.planning/phases/06-tech-debt-cleanup/06-01-SUMMARY.md`. Set:
- `phase: 06-tech-debt-cleanup`, `plan: 03`
- `tags: [harnessed-net, podman, host-gateway, host.containers.internal, docs, comments, tech-debt, gap-closure]`
- `requires:` → phase 04-isolated-shared-services (the shipped publish+host-gateway model this plan reconciles docs/comments to) AND phase 06-01 (the B1–B7 scoped sweep whose correction class this plan extends to the missed files).
- `provides:` → the 6 reconciled files; SC-1 closed at the repo-wide level.
- `affects:` → `/gsd-verify-work` live-test gate (deferred), future doc passes.
- `requirements-completed: [SC-1]`
</output>
