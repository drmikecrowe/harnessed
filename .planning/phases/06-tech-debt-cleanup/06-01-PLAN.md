---
phase: 06-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/harnessed-design.md
  - lib/harnessed-isolated.sh
  - lib/harnessed-services.sh
  - services/ping/service.yaml
  - recipes/ping/recipe.yaml
autonomous: true
requirements: [SC-1, SC-2]

must_haves:
  truths:
    - "A repo-wide search for `harnessed-net` returns only LIVE references, the `HARNESSED_NET` opt-in escape hatch, or now-accurate docs — no comment or doc asserts the rootless bridge as the current/default model (D-01, SC-1, SC-2)."
    - "docs/harnessed-design.md §3/§9/§13 record publish-to-0.0.0.0 + the podman host gateway `host.containers.internal:<port>` as the PRIMARY reachability model, with `HARNESSED_NET` documented as the opt-in bridge for bridge-capable hosts (D-02, SC-1, SC-2)."
    - "docs/harnessed-design.md §9 documents the two operator prerequisites for the shipped model: the egress-firewall `host.containers.internal` (169.254.1.2) allow rule and the FastMCP `allowed_hosts` requirement (D-02)."
    - "The 6 stale bridge-model statements (B1 lib/harnessed-services.sh:4, B2 :63-67, B3 services/ping/service.yaml:3, B4 recipes/ping/recipe.yaml:4, B5 :6, B6 lib/harnessed-isolated.sh:23-26) are rewritten to the shipped publish+host-gateway model (D-05, SC-2)."
    - "ensure_harnessed_net / ensure_named_net / the `HARNESSED_NET:-harnessed-net` default remain in code untouched (D-03) — the `$net` variable at lib/harnessed-isolated.sh:70 is LEFT with a clarifying comment added (D-04, SC-1)."
    - "The 4 replacement-documenting comments (D-07: assemble.py:63-67, harnessed-services.sh:98-102, harnessed-isolated.sh:121-126, service-authoring.md:163-166) and the OPEN `[INFERENCE — verify]` markers (D-06: design.md:412, :440) are NOT edited."
    - "`harnessed test ping-time` and `harnessed test tracer-time` still pass — no behavior change to running stacks (D-11, D-12, SC-1)."
  artifacts:
    - path: "docs/harnessed-design.md"
      provides: "Reconciled design source of truth (§3 diagram edge, §9 lifecycle+reachability+operator-prereq callout, §13 CLI comment, §13 Naming/identity)"
      contains: "host.containers.internal"
    - path: "lib/harnessed-isolated.sh"
      provides: "Corrected pod-network comment (B6) + clarifying comment on the `$net` variable (D-04)"
      contains: "rootless (pasta)"
    - path: "lib/harnessed-services.sh"
      provides: "Corrected file-header (B1) + svc_up docstring (B2)"
      contains: "host.containers.internal"
    - path: "services/ping/service.yaml"
      provides: "Corrected service-role comment (B3)"
      contains: "host.containers.internal"
    - path: "recipes/ping/recipe.yaml"
      provides: "Corrected service-resolution URL + service-location comments (B4, B5)"
      contains: "host.containers.internal"
  key_links:
    - from: "docs/harnessed-design.md §9"
      to: "lib/harnessed-services.sh:98-124 (svc_up publish model)"
      via: "prose documents the shipped publish+host-gateway reachability"
      pattern: "host\\.containers\\.internal"
    - from: "docs/harnessed-design.md §9 operator-prereq callout"
      to: "lib/egress-firewall.sh:55-63 + services/ping/server.py:19-25"
      via: "documents the firewall allow rule + FastMCP allowed_hosts as operator prerequisites (D-02)"
      pattern: "169\\.254\\.1\\.2"
---

<objective>
Reconcile the harnessed-net design/code/comment surface to the shipped publish + host-gateway model, and correct the stale bridge-model statements across code and docs. This implements CONTEXT decisions D-01 (harnessed-net is NOT dead — reconcile docs to shipped reality), D-02 (design §9/§13 reconciliation + operator-prereq documentation), D-03 (KEEP the `HARNESSED_NET` opt-in in code), D-04 (leave `$net` + clarifying comment; do NOT reverse the pivot), D-05 (scope = comments/docs contradicting shipped behavior), D-06 (EXCLUDE OPEN `[INFERENCE]` markers), and D-07 (KEEP replacement-documenting comments).

Purpose: close ROADMAP success criteria SC-1 (no dead `harnessed-net` code — every reference live/opt-in/accurate) and SC-2 (stale comments corrected) by reconciling the 4 design-doc locations, documenting the two operator prerequisites, correcting the 6 stale code/manifest comments, and adding the clarifying comment on the one genuinely-dead `$net` variable — all behavior-preserving, no code removal.

Output: a reconciled `docs/harnessed-design.md` (§3/§9/§13 + new §9 operator-prereq callout), corrected comments in `lib/harnessed-isolated.sh` (B6 + `$net` comment), `lib/harnessed-services.sh` (B1/B2), `services/ping/service.yaml` (B3), `recipes/ping/recipe.yaml` (B4/B5). No executable code path changes; `ensure_harnessed_net`/`ensure_named_net`/the `HARNESSED_NET:-harnessed-net` default remain untouched.
</objective>

<execution_context>
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tech-debt-cleanup/06-CONTEXT.md
@.planning/phases/06-tech-debt-cleanup/06-RESEARCH.md
@.planning/phases/06-tech-debt-cleanup/06-PATTERNS.md
@.planning/phases/06-tech-debt-cleanup/06-VALIDATION.md
@docs/codebase/CONCERNS.md
@docs/harnessed-design.md
@lib/harnessed-isolated.sh
@lib/harnessed-services.sh
@lib/egress-firewall.sh
@tools/harnessed/assemble.py
@services/ping/service.yaml
@services/ping/server.py
@recipes/ping/recipe.yaml
@docs/guides/service-authoring.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile docs/harnessed-design.md §3/§9/§13 to the shipped publish + host-gateway model (D-01, D-02, D-06, D-07)</name>
  <read_first>
    Read the CURRENT `docs/harnessed-design.md` first — CONTEXT/RESEARCH cite line numbers that have drifted ~6 lines (e.g. §9 cited at 245-247 is actually around 251-253; §13 CLI at ~387, §13 Naming at ~396). Anchor on the CONTENT of each snapshot-tagged range below, NOT on the CONTEXT line numbers. Re-read each range immediately before editing it.
    - `docs/harnessed-design.md` §3 ASCII diagram (the edge labelled `MCP over harnessed-net` near line 54 — the harness↔shared-service edge; leave the harness↔hatago `MCP hub · HTTP` edge, that is the shared netns/localhost path), §9 "Shared instance, concurrent" (the `hindsight` on `harnessed-net` lifecycle paragraph, ~251-253), §13 CLI surface (`harnessed svc up <service>` comment, ~387), §13 Naming/identity (`shared services: global by name ... on harnessed-net`, ~396).
    - `docs/harnessed-design.md` §14 (lines ~412 and ~440) — the OPEN `[INFERENCE — verify]` markers you MUST NOT touch (D-06).
    - `docs/guides/service-authoring.md:163-166` — the accurate "Networking note" paragraph to MIRROR verbatim (the wording source, do not paraphrase).
    - `tools/harnessed/assemble.py:63-67` — the replacement-doc comment whose prose pattern §13's CLI comment should adopt (D-07: this comment stays; mirror its framing elsewhere).
    - `lib/egress-firewall.sh:55-63` — the operator-prereq firewall rule to cite (verbatim comment + `PODMAN_GW=$(getent ahosts host.containers.internal ...)` + allow rule).
    - `services/ping/server.py:19-25` (and `:90-95` for the implementation) — the FastMCP `allowed_hosts` operator-prereq to cite (commit 6f6c1b3).
    - `06-PATTERNS.md` "Shared Pattern 2 — Doc-reconciliation prose" — the verbatim vocabulary list + the operator-prereq callout text.
  </read_first>
  <action>
    Edit `docs/harnessed-design.md` ONLY — reconcile the 4 stale locations and add one operator-prereq callout. Use the vocabulary already shipped in-repo (do NOT author fresh networking prose — mirror `docs/guides/service-authoring.md:163-166` and the `assemble.py:63-67` replacement-doc comment per D-07):
    - "publish to `0.0.0.0`" / "publishes its port to `0.0.0.0`"
    - "podman host gateway `host.containers.internal:<port>`"
    - "rootless bridge is unsupported on most hosts (netavark 'Operation not supported')"
    - "`HARNESSED_NET=<name>` opt-in for bridge-capable hosts" / "DNS-by-service-name … opt-in"
    - "`host.containers.internal` (`169.254.1.2`)" — the literal address from `lib/egress-firewall.sh:62`

    The 4 location rewrites (per D-02; read each current range and anchor on content):
    1. §3 ASCII diagram edge (~line 54, `MCP over harnessed-net`): the harness↔shared-service reachability is `host.containers.internal:<port>` (primary) with `HARNESSED_NET` bridge as the opt-in. LEAVE the harness↔hatago `MCP hub · HTTP` edge — that is the shared pod netns (localhost), independent of the network model.
    2. §9 lifecycle paragraph (~251-253, `One long-lived hindsight container on harnessed-net ...`): KEEP the lifecycle/ownership prose (service-scoped, outlives instances, `harnessed svc up/down` — still true). REWRITE the reachability clause: the service publishes its port to `0.0.0.0`; peers reach it via `host.containers.internal:<port>` (primary); the `harnessed-net` bridge + DNS-by-name is the `HARNESSED_NET` opt-in for bridge-capable hosts.
    3. §13 CLI comment (~387, `harnessed svc up <service>    # start a shared service on harnessed-net`): rewrite the inline comment to "start a shared service (publishes its port; peers reach it via host.containers.internal, or by DNS name under HARNESSED_NET)".
    4. §13 Naming/identity (~396, `shared services: global by name (hindsight), on harnessed-net.`): rewrite to "shared services: global by name (`hindsight`), reached via the host gateway `host.containers.internal:<port>` (or by DNS name over the `HARNESSED_NET` bridge on bridge-capable hosts)."

    Operator-prereq callout (per D-02): add a new subsection or callout immediately after the §9 lifecycle bullets covering BOTH operator prerequisites, citing the code that already implements them (they are NOT new requirements, just currently-undocumented deps):
    (1) Egress-firewall dependency on `host.containers.internal` (`169.254.1.2`) — cite `lib/egress-firewall.sh:55-63` (the firewall must allow the podman host-gateway, not just the default-route gateway; without it the proxy path is blocked since iptables is netns-wide and gates hatago too).
    (2) FastMCP `allowed_hosts` requirement — cite `services/ping/server.py:19-25` (a Streamable-HTTP service proxied over `host.containers.internal` MUST add it to `TransportSecuritySettings.allowed_hosts` or FastMCP's DNS-rebinding protection returns `421 Misdirected Request`; commit `6f6c1b3`).

    DO NOT touch the OPEN `[INFERENCE — verify]` markers at ~412 and ~440 (D-06 — those are unresolved assumptions, M2 work). DO NOT edit `docs/guides/service-authoring.md` — it is already accurate (the wording source to mirror, not a target). DO NOT reverse the publish+host-gateway pivot (D-04).
  </action>
  <verify>
    <automated>bash -n is N/A for a markdown doc; instead: (a) grep -n 'harnessed-net' docs/harnessed-design.md shows NO remaining assertion of the bridge as the default/current model — every remaining `harnessed-net` occurrence is either the opt-in (`HARNESSED_NET`), an accurate replacement-doc context, or the now-corrected reachability clause; (b) grep -c 'host.containers.internal' docs/harnessed-design.md returns >= 4 (the 4 reconciled locations + the operator-prereq callout); (c) grep -n '169.254.1.2' docs/harnessed-design.md shows the operator-prereq firewall citation present; (d) grep -n 'allowed_hosts' docs/harnessed-design.md shows the FastMCP prereq citation present; (e) git diff docs/harnessed-design.md shows lines ~412 and ~440 (`[INFERENCE — verify]` markers) UNCHANGED (D-06). One combined check: `git diff docs/harnessed-design.md | grep -c '\[INFERENCE' ` should equal 0 (no INFERENCE line was added/removed in the diff).</automated>
  </verify>
  <done>
    docs/harnessed-design.md §3/§9/§13 describe publish + host-gateway as primary with HARNESSED_NET as the opt-in; §9 carries an operator-prereq callout citing lib/egress-firewall.sh:55-63 (169.254.1.2) and services/ping/server.py:19-25 (allowed_hosts); the §14 `[INFERENCE]` markers are untouched; the shipped networking vocabulary (host.containers.internal, 169.254.1.2, HARNESSED_NET opt-in, netavark "Operation not supported") appears verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Stale-comment sweep (B1–B6) + clarifying comment on the dead `$net` variable (D-03, D-04, D-05, D-06, D-07)</name>
  <read_first>
    Read the CURRENT versions of each file — anchor on the CONTENT of the snapshot-tagged ranges, NOT on RESEARCH line numbers (which have drifted). Re-read each range immediately before editing.
    - `lib/harnessed-services.sh` — the file-header comment near line 4 (B1: "A shared service is its OWN image/container/volume on harnessed-net ...") and the `svc_up` doc-comment near lines 63-67 (B2: "...runs -d on harnessed-net with --label ... then waits for the healthcheck"). ALSO read the replacement-doc comment at `:98-102` ("Rootless service model (plan 04-01 fix): publish the port to 0.0.0.0 — NO bridge") — that is the CORRECT target framing (D-07: leave it, mirror its wording into B1/B2), and the `svc_up` body at `:117-124` (the shipped `-p "$port:$port"` publish model, no `--network` flag).
    - `lib/harnessed-isolated.sh` — the pod-network comment block near lines 23-26 (B6: "Pod network: harnessed-net is the DEFAULT ... by DNS name ... Set HARNESSED_NET=<name> to override"). ALSO read the replacement-doc at `:121-126` ("Pod network: DEFAULT rootless (pasta) networking — NOT a bridge ... HARNESSED_NET is an explicit opt-in bridge override") — that is the CORRECT framing B6 must converge to (D-07: leave :121-126, rewrite :23-26 to match). ALSO read line ~70 (`local net="${HARNESSED_NET:-harnessed-net}"` — the genuinely-dead `$net` variable) AND the live networking block at `:127-131` (reads `${HARNESSED_NET:-}` directly, NOT `$net`). ALSO read `:27` (`HARNESSED_NET="${HARNESSED_NET:-}"`) and `:13-14` ("members share a netns ... localhost:$HATAGO_PORT") — both CORRECT, do not touch.
    - `services/ping/service.yaml` — the role comment near line 3 (B3: "its own image/container/volume on harnessed-net ..."); ALSO read the unchanged body lines `:7-11` (the actual service config).
    - `recipes/ping/recipe.yaml` — the comments near line 4 (B4: "resolves service: ping → {url: http://ping:8080/mcp, type: http}") and line 6 (B5: "the service runs as its own container on harnessed-net (design §3/§9)").
    - `06-PATTERNS.md` "Shared Pattern 3 — Bash / YAML comment house style" and the "Replacement-doc SIGNAL template" — the exact target comment form for the B1–B6 rewrites.
    - `06-RESEARCH.md` Item B.1 table (B1–B6) — the per-location correction direction.
    - `tools/harnessed/assemble.py:60-70` — confirms the shipped URL rewrite is `http://host.containers.internal:{port}/mcp` (B4's `http://ping:8080/mcp` is the OLD bridge URL that must become the host-gateway URL).
  </read_first>
  <action>
    Rewrite the CONTENT of the 6 stale comments toward the replacement-doc SIGNAL form (Shared Pattern 3); PRESERVE each comment's SHAPE (file-header summary / function docstring / YAML top-of-file header). These are comment-only edits — change NO executable code. Implementing D-05 (scope = contradict shipped behavior), D-06 (exclude `[INFERENCE]` markers), D-07 (leave replacement-docs), and the `$net` clarifying comment per D-04.

    B1 — `lib/harnessed-services.sh:4` (file header): rewrite "on harnessed-net" to "on a host-published port (reachable via `host.containers.internal:<port>`; or by DNS name over the `HARNESSED_NET` bridge on bridge-capable hosts)", keeping the "OWN image/container/volume ... lifecycle independent of any instance" wording intact.

    B2 — `lib/harnessed-services.sh:63-67` (`svc_up` doc-comment): rewrite to match the shipped body (`:117-124`) and mirror the inline replacement-doc at `:98-102`. The body publishes with `-p "$port:$port"` and uses NO `--network` flag, so the docstring must read "publishes its port to 0.0.0.0 (rootless; peers reach it via `host.containers.internal:<port>`) with `--label harnessed-service=<name>` + `--userns=keep-id`, then waits for the healthcheck." Leave the `:98-102` inline replacement-doc untouched (D-07).

    B3 — `services/ping/service.yaml:3`: rewrite "on harnessed-net" to "host-published (reachable via `host.containers.internal:8080`; or by DNS name over the `HARNESSED_NET` bridge on bridge-capable hosts)". Keep the YAML body (`:7-11`) unchanged.

    B4 — `recipes/ping/recipe.yaml:4`: rewrite the URL `http://ping:8080/mcp` to `http://host.containers.internal:8080/mcp` (the actual shipped rewrite from `assemble.py:67`) and note `http://ping:8080/mcp` as the `HARNESSED_NET` opt-in form.

    B5 — `recipes/ping/recipe.yaml:6`: rewrite "on harnessed-net (design §3/§9)" to "host-published; reachable via `host.containers.internal:<port>` (or by DNS name over the `HARNESSED_NET` bridge on bridge-capable hosts)" — consistent with the now-reconciled design §3/§9.

    B6 — `lib/harnessed-isolated.sh:23-26` (pod-network comment block): replace to match the replacement-doc at `:121-126` in the SAME file (the two currently disagree; `:23-26` is the stale one). New content: DEFAULT is rootless (pasta) — members reach shared services via `host.containers.internal:<port>`; the `HARNESSED_NET` bridge is the opt-in for bridge-capable hosts (DNS-by-name). Members share a netns either way, so the harness always reaches hatago at `localhost:$HATAGO_PORT`. Leave line `:27` (`HARNESSED_NET="${HARNESSED_NET:-}"`), the live block `:127-131`, the comment `:13-14`, and the replacement-doc `:121-126` UNCHANGED.

    `$net` clarifying comment (D-04) — `lib/harnessed-isolated.sh:~70` (`local net="${HARNESSED_NET:-harnessed-net}"`): LEAVE the line; insert a one-line clarifying comment immediately above it, in the `ensure_harnessed_net` clarifying-comment style at `lib/harnessed-services.sh:25`. The comment must state: this line is preserved as the literal default-name anchor (D-01/D-03); the live networking block at `:127-131` reads `${HARNESSED_NET:-}` directly so `$net` is assigned-but-unused on this path; KEPT per D-04 ("if unsure, leave it and add a clarifying comment"). Do NOT wire `$net` into the launcher — that reverses the pivot (D-04, Pitfall 2).

    DO NOT remove `ensure_harnessed_net` / `ensure_named_net` / the `HARNESSED_NET:-harnessed-net` default (D-03, Pitfall 1 — the v1.0 milestone audit G-1 deletion recommendation is OVERRIDDEN by D-03). DO NOT edit the 4 replacement-doc comments (D-07: assemble.py:63-67, services.sh:98-102, isolated.sh:121-126, service-authoring.md:163-166). DO NOT touch any `[INFERENCE — verify]` marker (D-06).
  </action>
  <verify>
    <automated>For every touched lib/*.sh run a syntax check then grep assertions: (1) `bash -n lib/harnessed-isolated.sh && bash -n lib/harnessed-services.sh` exits 0 (Pitfall 5 — comment edit did not break shell parsing). (2) Stale-wording-absent checks (use `grep -v '^#'`-aware gating where needed; these are comment lines so a plain `grep -c` on the exact stale phrase works): `grep -c 'on harnessed-net' lib/harnessed-services.sh lib/harnessed-isolated.sh services/ping/service.yaml recipes/ping/recipe.yaml` — each file returns 0 stale assertions of the bridge-as-default; `grep -c 'http://ping:8080/mcp' recipes/ping/recipe.yaml` returns 0 (the OLD bridge URL is gone from the comment, replaced by host.containers.internal). (3) Correct-wording-present checks: `grep -c 'host.containers.internal' lib/harnessed-services.sh lib/harnessed-isolated.sh services/ping/service.yaml recipes/ping/recipe.yaml` — each returns >= 1. (4) D-03 preservation: `grep -c 'ensure_harnessed_net' lib/harnessed-services.sh` >= 1 AND `grep -c 'HARNESSED_NET:-harnessed-net' lib/harnessed-isolated.sh` == 1 (the default expression is still there). (5) D-07 preservation: `grep -c 'publish the port to 0.0.0.0' lib/harnessed-services.sh` >= 1 (the :98-102 replacement-doc still present) AND `grep -c 'DEFAULT rootless (pasta)' lib/harnessed-isolated.sh` >= 1 (the :121-126 replacement-doc still present). (6) D-04 pivot not reversed: `grep -n 'ensure_named_net "\$net"' lib/harnessed-isolated.sh` — the live block at :127-131 is UNCHANGED (still gated on `[ -n "\${HARNESSED_NET:-}" ]`). (7) `git diff lib/harnessed-services.sh lib/harnessed-isolated.sh | grep -c '\[INFERENCE'` returns 0.</automated>
  </verify>
  <done>
    The 6 stale bridge-model comments (B1 services.sh:4, B2 services.sh:63-67, B3 service.yaml:3, B4 recipe.yaml:4, B5 recipe.yaml:6, B6 isolated.sh:23-26) now state the publish+host-gateway model with HARNESSED_NET as the opt-in; the `$net` line at isolated.sh:~70 is preserved with a clarifying comment citing D-01/D-03/D-04; ensure_harnessed_net/ensure_named_net/HARNESSED_NET:-harnessed-net remain (D-03); the 4 replacement-doc comments (D-07) and the §14 [INFERENCE] markers (D-06) are untouched; `bash -n` passes on both lib files; `harnessed test ping-time` + `harnessed test tracer-time` still pass (run at the plan verification gate).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is doc/comment edits + a clarifying comment on an unused variable. It introduces NO new network path, input handler, secret flow, dependency, or trust boundary. The existing boundaries (host↔pod egress firewall, hatago MCP proxy, service-scoped volumes) are unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information disclosure | Shared-service egress (publish + host.containers.internal) | accept | The egress firewall already whitelists `host.containers.internal` (`169.254.1.2`) as the podman host-gateway ONLY — not a blanket allow (`lib/egress-firewall.sh:55-63`); FastMCP `allowed_hosts` gates the HTTP Host header (`services/ping/server.py:19-25`). D-02 DOCUMENTS these existing controls as operator prerequisites; no rule is added, removed, or widened. Zero new attack surface; accept residual risk on the documentation-only change. |
| T-06-02 | Tampering | `lib/*.sh` comment edits under `set -euo pipefail` | mitigate | A malformed comment edit (unclosed quote/heredoc) turns into a launch-time failure. Mitigation: run `bash -n` on every touched `lib/*.sh` after each edit (Pitfall 5), and the plan-level capability-test gate (D-11) catches it too. |
| T-06-03 | Elevation of privilege / tampering | Reversing the publish+host-gateway pivot (rewiring `$net` into the launcher) | mitigate | D-04 forbids reversing the pivot. The verify step asserts the live networking block `:127-131` stays gated on `[ -n "${HARNESSED_NET:-}" ]` and `$net` is NOT wired in. Pitfall 2. |
| T-06-04 | Repudiation / info disclosure | Touching OPEN `[INFERENCE]` markers or deleting the D-07 replacement-docs | mitigate | D-06 excludes all OPEN `[INFERENCE]` markers; D-07 keeps the 4 replacement-docs. The verify step greps the diff for `[INFERENCE` (expect 0 changed lines) and asserts the replacement-doc phrases still present. |

This plan is security-neutral (ASVS L1 categories are NOT newly implicated; `security_block_on: "high"` is not triggered — there is no behavior change). The `host.containers.internal` dependency is DOCUMENTED (D-02), not changed.
</threat_model>

<verification>
Plan-level integration gate (per 06-VALIDATION.md / D-11 / D-12) — run after BOTH tasks complete:
- `bash -n lib/harnessed-isolated.sh && bash -n lib/harnessed-services.sh` exits 0 (Pitfall 5 — instant, local).
- Static diff review: the diff contains the B1–B6 corrections + the 4 design-doc reconciliations + the §9 operator-prereq callout; the D-07 replacement-doc comments (`assemble.py:63-67`, `services.sh:98-102`, `isolated.sh:121-126`, `service-authoring.md:163-166`) remain untouched; `git diff | grep '\[INFERENCE'` shows only pre-existing occurrences (no OPEN marker edited — D-06).
- `harnessed test ping-time` → asserts the `ping` MCP connects via hatago over `host.containers.internal` (the `harnessed-net`-referencing service still works — SC-1 regression gate; needs rootless podman).
- `harnessed test tracer-time` → asserts the isolated pod boots and the `time` MCP connects (tracer stack unaffected — needs rootless podman).
- `bash tools/uat/run-uat.sh` → the UAT suite is green (no behavior change — D-12; needs rootless podman).

If rootless podman is unavailable on the verification host, the doc/comment edits still land (zero-runtime-risk per D-12); only the live capability/UAT legs defer. Re-run them before `/gsd-verify-work` (D-11).
</verification>

<success_criteria>
- SC-1 (no dead `harnessed-net` code): a repo-wide grep for `harnessed-net` returns only LIVE references (`ensure_named_net`, the `:127-131` opt-in path, the `svc_up` publish model, the live ping service), the `HARNESSED_NET` opt-in escape hatch, and now-accurate docs — no stale bridge-as-default assertion. The `$net` variable is preserved with a clarifying comment; `ensure_harnessed_net`/`ensure_named_net`/the default remain (D-03).
- SC-2 (stale comments corrected): B1–B6 rewritten to the publish+host-gateway model; the 4 design-doc locations reconciled; the D-07 replacement-docs and D-06 `[INFERENCE]` markers untouched; `bash -n` passes; `harnessed test ping-time` + `harnessed test tracer-time` + the UAT suite are green.
</success_criteria>

<output>
Create `.planning/phases/06-tech-debt-cleanup/06-01-SUMMARY.md` when done. Use the de-facto 02–05 SUMMARY frontmatter schema (`phase`/`plan`/`subsystem`/`tags` + `# Dependency graph` `requires:`/`provides:`/`affects:` + tech-tracking/key-files/key-decisions/requirements-completed/metrics) — copy the shape from `.planning/phases/02-isolated-tracer-bullet-stack/02-01-SUMMARY.md`. Set `requirements-completed: [SC-1, SC-2]` (this phase carries no formal REQ IDs — use the ROADMAP success criteria).
</output>
