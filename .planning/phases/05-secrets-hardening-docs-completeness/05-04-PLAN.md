---
phase: 05-secrets-hardening-docs-completeness
plan: 04
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - README.md
  - AGENTS.md
  - docs/guides/recipe-authoring.md
  - docs/guides/stacks.md
  - docs/guides/service-authoring.md
  - docs/guides/troubleshooting.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]
user_setup: []
must_haves:
  truths:
    - "README.md exists at the repo root and documents what harnessed is, the two modes (transparent + isolated), install (podman as the only host dep), first-run build, and a quickstart that runs as-documented (copy-paste runnable)"
    - "docs/guides/recipe-authoring.md and docs/guides/stacks.md exist, each with a worked example (recipe-authoring uses `recipes/time/recipe.yaml` + `recipes/ping/recipe.yaml`; stacks uses `stacks/tracer-time/stack.yaml` + `stacks/transparent/stack.yaml` + `stacks/ping-time/stack.yaml`) that runs as-documented"
    - "docs/guides/service-authoring.md exists with a worked example using `services/ping/` (Dockerfile + server.py + service.yaml — the full minimal sidecar triple) and documents the Streamable-HTTP MCP transport (SSE is deprecated per MCP spec 2025-06-18)"
    - "docs/guides/troubleshooting.md exists covering: podman socket setup, first-run build issues, `~/.claude.json` onboarding, `--fresh` clean-room runs, host-persisted sessions, AND the SEC-04 nightly timer prerequisites (`loginctl enable-linger`, network egress to osv.dev, the `systemctl --user list-timers` + `journalctl` diagnostic)"
    - "README.md and AGENTS.md do not contradict (README is the entry point; AGENTS.md is reconciled — either folded into README or rewritten to point at it for the harnessed surface)"
    - "Every doc cross-references `docs/harnessed-design.md` for the WHY (architecture/modes/design rationale) without duplicating it — the how-tos show HOW with a worked example"
  artifacts:
    - "README.md (NEW at repo root) — what/why, two modes (transparent/isolated), install (podman-only), first-run build, quickstart (one of each mode); supersedes AGENTS.md's container-era setup"
    - "AGENTS.md (refresh) — reconciled with README (redirect or fold the container-era quickstart; keep AI-assistant-specific content if any)"
    - "docs/guides/recipe-authoring.md (NEW) — how to author a recipe (MCP layer + skills/commands), worked example from recipes/time + recipes/ping"
    - "docs/guides/stacks.md (NEW) — how to compose a stack manifest, worked example from stacks/tracer-time + stacks/transparent + stacks/ping-time"
    - "docs/guides/service-authoring.md (NEW) — how to author a shared service sidecar, worked example from services/ping (Dockerfile + server.py + service.yaml), Streamable-HTTP transport"
    - "docs/guides/troubleshooting.md (NEW) — podman socket, first-run build, ~/.claude.json onboarding, --fresh, host-persisted sessions, SEC-04 timer prerequisites + diagnostics"
  key_links:
    - "README.md → docs/harnessed-design.md (the WHY source of truth) + docs/guides/* (the how-tos) + the harnessed usage() block (the command surface)"
    - "docs/guides/secrets.md (shipped in 05-02) ← cross-referenced from README.md + troubleshooting.md (the SEC-01/SEC-04 doc surface is complete)"
---

## Phase Goal

**As a** developer new to harnessed, **I want** a README that gets me from zero to a running stack, and guides that show me how to author recipes, stacks, services, and troubleshoot issues, **so that** I can use every shipped feature without reading the source.

## Phase User Story (MVP vertical slice — DOC-01 + DOC-02 + DOC-03)

This plan delivers the gated documentation surface: README (the entry point), recipe/stack authoring guides (worked examples), and the service-authoring + troubleshooting guides. It depends on 05-01/02/03 so every doc matches shipped behavior (the cadence rule: a doc is done when its feature is done AND the worked example runs).

<objective>
Author README.md (repo root — what harnessed is, two modes, install, first-run build, quickstart) and reconcile AGENTS.md. Author four how-to guides under `docs/guides/`: recipe-authoring, stacks, service-authoring, troubleshooting. Each guide uses a verified worked example from the repo (`recipes/`, `stacks/`, `services/ping/`) and cross-references `docs/harnessed-design.md` for the WHY.

Purpose: DOC-01/02/03 close the documentation gap. `docs/harnessed-design.md` (590 lines, §1-§18) is the WHY source of truth and already exists; these docs are the HOW. The cadence rule (design §17) means the secrets doc already shipped with SEC-01 (05-02); the remaining docs land here, gated on every feature being real. Each worked example MUST run as-documented (the validation contract: copy-paste runnable).

Output: README.md, refreshed AGENTS.md, four new `docs/guides/*.md` files.
</objective>

<execution_context>
@/home/mcrowre/Programming/Personal/code-container/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowre/Programming/Personal/code-container/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-secrets-hardening-docs-completeness/05-RESEARCH.md
@.planning/phases/05-secrets-hardening-docs-completeness/05-PATTERNS.md
@docs/harnessed-design.md
@docs/guides/secrets.md
@AGENTS.md
@harnessed
@recipes/time/recipe.yaml
@recipes/ping/recipe.yaml
@stacks/tracer-time/stack.yaml
@stacks/transparent/stack.yaml
@stacks/ping-time/stack.yaml
@services/ping/service.yaml
@services/ping/Dockerfile
@services/ping/server.py
@lib/harnessed-isolated.sh
@lib/harnessed-services.sh
@install.sh
</context>

<threat_model>
**ASVS L1 — block on high. Documentation-only plan; minimal security surface.**

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → reader | Documentation content crosses to the reader; must not contain real secrets/tokens or misleading security guidance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Information Disclosure | Docs accidentally reveal real secrets/tokens | mitigate | Use placeholder values only (`op(op://Private/Snyk/credential)`, `abcd1234-ef56-…`); never real tokens; `.env.schema.example` is the template source. |
| T-05-17 | Tampering (misleading guidance) | Docs describe behavior that doesn't match shipped code | mitigate | Each worked example MUST run as-documented (the validation contract); the plan depends on 05-01/02/03 so features are real before docs describe them. |
| T-05-18 | Information Disclosure (operational) | Troubleshooting doc omits the linger prerequisite → user thinks timer is running but it isn't | mitigate | troubleshooting.md MUST state `loginctl enable-linger $USER` (SEC-04 prerequisite from 05-03) + the `systemctl --user list-timers` / `journalctl --user -u harnessed-rescan.service` diagnostic. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: README.md + reconcile AGENTS.md (DOC-01)</name>
  <files>README.md, AGENTS.md</files>
  <read_first>
    - .planning/phases/05-secrets-hardening-docs-completeness/05-RESEARCH.md (Architecture Pattern 5 [docs], the README content-source table)
    - .planning/phases/05-secrets-hardening-docs-completeness/05-PATTERNS.md (README.md assignment — AGENTS.md to supersede + design §1-§2 to lift)
    - docs/harnessed-design.md (§1 what/why, §2 two modes [transparent/isolated table], §15 podman-only host — the WHY source of truth; cross-reference, don't duplicate)
    - AGENTS.md (the OLD container-era setup prose — the quickstart README supersedes; AI-assistant-specific content to preserve or redirect)
    - harnessed (the `usage()` block at :36-68 — the command surface README documents; the top-of-file surface comment at :8-23)
    - install.sh (the install path README documents)
    - CLAUDE.md (the Constraints section — the design constraints README summarizes)
  </read_first>
  <action>
    **README.md** (NEW at repo root) — the user-facing entry point. Structure:

    - **Title + one-paragraph pitch** — what harnessed is (one executable that launches isolated, composable harness stacks — a podman pod running an AI coding harness + an MCP hub + optional shared services). Lift the core value from `CLAUDE.md:21-23` / design §1. Link to `docs/harnessed-design.md` for the full architecture.

    - **The two modes** — a table (lift from `docs/harnessed-design.md` §2 / `CLAUDE.md:148-163`):
      | Mode | What it is | When to use |
      |------|-----------|-------------|
      | `harnessed transparent [path]` | Host-mirror: your `~/.claude` + project, sandboxed | "My laptop, isolated" — your config, firewalled |
      | `harnessed <stack> [path]` | Isolated: assembled profile only (no host config), reproducible | Experiment with curated skill/MCP combos per container |

    - **Install** — podman (or docker) is the ONLY host dependency. Show the `install.sh` path (or the manual `podman build` first-run). No host Python/node/uv required.

    - **First-run build** — `harnessed build` (or `harnessed <stack>` on first run auto-builds). Explain the image lineage: base → claude → hatago + the tools assembler image. Mention the first-run latency (images build via host `podman build`).

    - **Quickstart** — two worked examples (copy-paste runnable):
      (1) Transparent: `harnessed transparent` (or just `harnessed`) in a project → interactive harness with host config mounted live.
      (2) Isolated: `harnessed build tracer-time && harnessed tracer-time` → an isolated pod with the `time` recipe's MCP server, no host config.

    - **Command surface** — a concise table mirroring `harnessed` `usage()` (`:36-68`): `build`, `transparent`, `<stack>`, `list`, `stop`, `rm`, `new`, `install`/`uninstall`, `svc up|down|list`, `test`, `auth snyk|socket`, `rescan`. One line each. Link to `harnessed --help` for the full surface.

    - **Guides** — link to `docs/guides/` (recipe-authoring, stacks, service-authoring, secrets, troubleshooting) + `docs/harnessed-design.md` (architecture).

    - **Supply chain + security** — one paragraph: pnpm-everywhere, build-time scan gate (osv-scanner + pip-audit, HIGH abort), opt-in secrets (varlock + 1Password — link to `docs/guides/secrets.md`), nightly re-scan timer (link to troubleshooting for setup). Placeholder values only in examples (T-05-16).

    **AGENTS.md** (refresh) — reconcile with README so they do not contradict (RESEARCH Pattern 5 anti-pattern: "letting the README and AGENTS.md drift"). The OLD `container`-era quickstart (`AGENTS.md:9-44`) is superseded by README's harnessed surface. Either: (a) fold the AI-assistant-specific content into README and leave AGENTS.md as a one-line redirect (`See README.md`), or (b) rewrite AGENTS.md to point at README for the harnessed surface and keep only agent-specific notes. The planner's recommendation: option (a) — README is the entry point; AGENTS.md becomes a stub redirect (keeps the file for tools that look for it, but eliminates drift).
  </action>
  <verify>
    <automated>test -f README.md && grep -qi 'harnessed' README.md && grep -qi 'transparent' README.md && grep -qi 'isolated' README.md && grep -qi 'podman' README.md && grep -qi 'quickstart\|quick start' README.md && grep -qi 'harnessed build' README.md && grep -q 'docs/guides/' README.md && grep -q 'docs/harnessed-design.md' README.md && echo '05-04 Task 1 README structural checks OK'</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at the repo root, documents what harnessed is, the two modes (transparent + isolated) in a table, install (podman as the only host dependency), first-run build, and a quickstart with one transparent + one isolated worked example
    - README.md's quickstart examples are copy-paste runnable (`harnessed transparent`, `harnessed build tracer-time && harnessed tracer-time`)
    - README.md links to docs/harnessed-design.md (the WHY) and docs/guides/* (the how-tos)
    - README.md documents the command surface (mirrors harnessed usage()) including `auth snyk|socket` and `rescan` (the Phase 5 surface)
    - AGENTS.md is reconciled with README (no contradictions; the container-era quickstart redirects to README or is folded in)
    - no real tokens/secrets in any example (placeholders only — op:// refs, dummy values)
  </acceptance_criteria>
  <done>README.md is the repo-root entry point with install + quickstart + command surface; AGENTS.md is reconciled (no drift); all examples use placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: docs/guides/recipe-authoring.md + docs/guides/stacks.md (DOC-02)</name>
  <files>docs/guides/recipe-authoring.md, docs/guides/stacks.md</files>
  <read_first>
    - .planning/phases/05-secrets-hardening-docs-completeness/05-RESEARCH.md (Architecture Pattern 5, the recipe/stack doc content-source table)
    - .planning/phases/05-secrets-hardening-docs-completeness/05-PATTERNS.md (recipe-authoring + stacks assignments — the worked-example sources)
    - recipes/time/recipe.yaml (the tracer-bullet recipe — stdio MCP + standalone skill; the primary worked example)
    - recipes/ping/recipe.yaml (the service-ref MCP recipe — a second example showing a different recipe shape)
    - stacks/tracer-time/stack.yaml (isolated + claude + one recipe; the primary stack worked example)
    - stacks/transparent/stack.yaml (transparent mode — the degenerate stack)
    - stacks/ping-time/stack.yaml (a stack with a shared service)
    - tools/harnessed/schema.py (Recipe + Stack model definitions — the schema the docs explain; Recipe.servers, Recipe.skills, Recipe.commands, Stack.harness, Stack.recipes, Stack.services)
    - docs/harnessed-design.md (§5 recipes, §11 recipe deps, §12 stacks, §2 modes — the WHY to cross-reference)
  </read_first>
  <action>
    **docs/guides/recipe-authoring.md** (NEW) — how to author a recipe. Cross-reference `docs/harnessed-design.md` §5/§11 for the WHY (don't duplicate). Structure:

    - **What a recipe is** — a `recipes/<name>/recipe.yaml` that declares an MCP layer (servers) and/or a Claude-canonical file-extension layer (skills/commands/agents/hooks). One paragraph.
    - **The recipe.yaml schema** — document the key fields from `tools/harnessed/schema.py` Recipe model: `name`, `description`, `servers` (McpServer: `name`, `command`, `args`, `transport`, `url`), `skills` (Skill: name + path), `commands`. Reference `recipes/time/recipe.yaml` as the canonical example.
    - **Worked example 1: the `time` recipe** — walk through `recipes/time/recipe.yaml` line by line: a stdio MCP server (`uvx time-mcp`) + a standalone skill. Explain each field. Show `harnessed build tracer-time` assembling it.
    - **Worked example 2: the `ping` recipe** — `recipes/ping/recipe.yaml`: a service-ref MCP (references a shared service, no local command). Contrast with the stdio shape.
    - **Transports** — stdio (hatago wraps → HTTP), streamable-http (network-native). State SSE is DEPRECATED (MCP spec 2025-06-18 — from CLAUDE.md).
    - **Supply-chain rules** — pnpm everywhere (no npm/npx — the assembler lints and points at the pnpm equivalent); `uvx` for Python MCP servers. Reference Phase 3 BLD-03.
    - **Adding a recipe to a stack** — point at `docs/guides/stacks.md`.

    **docs/guides/stacks.md** (NEW) — how to compose a stack. Cross-reference design §2/§12. Structure:

    - **What a stack is** — a `stacks/<name>/stack.yaml` that composes one harness (`claude`/`omp`) + a chosen set of recipes (+ optional services/permissions/state). One paragraph.
    - **The stack.yaml schema** — document the key fields from the Stack model: `config` (transparent|isolated), `harness` (claude|omp), `recipes` (list), `services` (list, optional). Reference `stacks/tracer-time/stack.yaml`.
    - **Worked example 1: `tracer-time`** — walk through `stacks/tracer-time/stack.yaml`: isolated + claude + the `time` recipe. Show the full lifecycle: `harnessed build tracer-time` → `harnessed tracer-time` → `harnessed test tracer-time` (capability report).
    - **Worked example 2: `transparent`** — `stacks/transparent/stack.yaml`: the degenerate transparent stack (host-mirror, no recipes/services). Contrast with isolated.
    - **Worked example 3: `ping-time`** — `stacks/ping-time/stack.yaml`: a stack with a shared service. Link to `docs/guides/service-authoring.md`.
    - **Scaffolding** — `harnessed new <stack> --harness <h> --recipes a,b,c` (CLI-02). Show the generated manifest.
    - **Build + run lifecycle** — `harnessed build <stack>` (assemble → scan → podman build → image scan), `harnessed <stack>` (launch), `harnessed <stack> --fresh` (clean-room), `harnessed list|stop|rm`. Reference the README command surface.
  </action>
  <verify>
    <automated>test -f docs/guides/recipe-authoring.md && test -f docs/guides/stacks.md && grep -qi 'recipe.yaml' docs/guides/recipe-authoring.md && grep -qi 'recipes/time' docs/guides/recipe-authoring.md && grep -qi 'recipes/ping' docs/guides/recipe-authoring.md && grep -qi 'streamable' docs/guides/recipe-authoring.md && grep -qi 'pnpm' docs/guides/recipe-authoring.md && grep -qi 'stack.yaml' docs/guides/stacks.md && grep -qi 'tracer-time' docs/guides/stacks.md && grep -qi 'transparent' docs/guides/stacks.md && grep -qi 'ping-time' docs/guides/stacks.md && grep -qi 'harnessed new' docs/guides/stacks.md && grep -q 'docs/harnessed-design.md' docs/guides/recipe-authoring.md && grep -q 'docs/harnessed-design.md' docs/guides/stacks.md && echo '05-04 Task 2 structural checks OK'</automated>
  </verify>
  <acceptance_criteria>
    - docs/guides/recipe-authoring.md documents the recipe.yaml schema (servers/skills/commands), walks through recipes/time + recipes/ping as worked examples, documents stdio + streamable-http transports (states SSE is deprecated), and covers the pnpm/uvx supply-chain rules
    - docs/guides/stacks.md documents the stack.yaml schema (config/harness/recipes/services), walks through stacks/tracer-time + stacks/transparent + stacks/ping-time, documents `harnessed new` scaffolding + the build/run/test lifecycle
    - both docs cross-reference docs/harnessed-design.md for the WHY (no duplication of design rationale)
    - all worked examples reference real repo files (recipes/time, recipes/ping, stacks/tracer-time, stacks/transparent, stacks/ping-time) that exist and are runnable
  </acceptance_criteria>
  <done>recipe-authoring.md and stacks.md exist with worked examples from the repo's real recipes/stacks, document the schemas + transports + lifecycle, and cross-reference the design doc.</done>
</task>

<task type="auto">
  <name>Task 3: docs/guides/service-authoring.md + docs/guides/troubleshooting.md (DOC-03)</name>
  <files>docs/guides/service-authoring.md, docs/guides/troubleshooting.md</files>
  <read_first>
    - .planning/phases/05-secrets-hardening-docs-completeness/05-RESEARCH.md (Architecture Pattern 5, service-authoring + troubleshooting content-source tables)
    - .planning/phases/05-secrets-hardening-docs-completeness/05-PATTERNS.md (service-authoring + troubleshooting assignments — the worked-example sources)
    - services/ping/service.yaml (the minimal service manifest — the worked example)
    - services/ping/Dockerfile (the service's own image — own layer lineage)
    - services/ping/server.py (the FastMCP streamable-http server with a /health route — the worked example)
    - lib/harnessed-services.sh (svc_up/svc_down/svc_list — the lifecycle the doc explains)
    - lib/harnessed-isolated.sh (lines 105-112 — host-persisted sessions, the state-dir layout; lines 132-143 — auto-start services)
    - .planning/phases/05-secrets-hardening-docs-completeness/05-03-PLAN.md (the SEC-04 timer prerequisites the troubleshooting doc must carry: loginctl enable-linger, OnCalendar=daily, scan-image-online, the diagnostic)
    - systemd/harnessed-rescan.timer (the unit file the troubleshooting doc references)
    - docs/harnessed-design.md (§3 pods, §9 services, §15 emit-only, §17 docs cadence — the WHY to cross-reference)
    - CLAUDE.md (the "What NOT to Use" table — SSE deprecated, npm forbidden, etc. — troubleshooting references these constraints)
  </read_first>
  <action>
    **docs/guides/service-authoring.md** (NEW) — how to author a shared service sidecar. Cross-reference design §3/§9. Structure:

    - **What a service is** — a shared sidecar (`services/<name>/`) with its OWN image (`Dockerfile`), OWN volume (service-scoped), independent lifecycle (`harnessed svc up|down|list`). Multiple instances attach concurrently over the shared network. One paragraph.
    - **The service triple** — `services/<name>/service.yaml` (the manifest: name, port, image/volume) + `Dockerfile` (the service's own image) + the server itself (e.g. `server.py`). Use `services/ping/` as the worked example.
    - **Worked example: the `ping` service** — walk through all three files:
      - `services/ping/service.yaml` — the manifest fields (name, port, the image build path, the volume name).
      - `services/ping/Dockerfile` — the service's own image lineage (FROM, the server install, the CMD/ENTRYPOINT, the healthcheck).
      - `services/ping/server.py` — a FastMCP Streamable-HTTP server with a `/health` route. Explain the transport choice: Streamable HTTP (one endpoint, POST + optional GET/SSE stream); SSE is DEPRECATED (MCP spec 2025-06-18 — from CLAUDE.md "What NOT to Use").
    - **Lifecycle** — `harnessed svc up ping` (starts the sidecar), `harnessed svc list`, `harnessed svc down ping` (stops; `--purge` removes the volume). Reference `lib/harnessed-services.sh`. Show how a stack references it (`services: [ping]` in `stacks/ping-time/stack.yaml`) and auto-starts on launch.
    - **Attaching from a recipe** — the recipe's MCP server entry references the service by host:port (e.g. `host.containers.internal:<port>` on rootless pasta networking — from `lib/harnessed-isolated.sh:114-119`).

    **docs/guides/troubleshooting.md** (NEW) — the ops/troubleshooting guide. Cross-reference design §15/§17. Cover:

    - **podman socket / rootless setup** — `systemctl --user enable --now podman.socket` (though harnessed is host-native, NOT DooD — clarify this: harnessed runs `podman` on the host directly, no socket needed for the core flow; the socket is only relevant if a tool container needs to drive podman, which harnessed-tools does NOT — it's emit-only).
    - **First-run build issues** — image build fails: check podman version (≥ 5.6), disk space, the build context. The tools image build is the longest (osv-scanner DB seed + Node + CLIs from Phase 5). If snyk's postinstall fails under strictDepBuilds, see the `allowBuilds` note.
    - **`~/.claude.json` onboarding prompt** — isolated instances use a generated stub that boots headlessly (AUTH-02). If claude prompts for onboarding, the stub is missing fields — see Phase 02 / the stub fixture. Transparent mode uses a copy-on-start per-instance copy (MNT-03).
    - **`--fresh` clean-room runs** — `harnessed <stack> --fresh` wipes the per-instance state dir and reseeds from the profile. Contrast with a normal run (reuses accumulated `.claude`). Reference `lib/harnessed-isolated.sh:108-111`.
    - **Host-persisted sessions** — session state (`projects/`, `history.jsonl`) persists at `$XDG_STATE_HOME/harnessed/<flattened-project-path>/<stack>/.claude` (STA-02, UAT gap 6 fix). The legible path is the flattened home-relative project path. Reference `lib/harnessed-isolated.sh:105-106`.
    - **Nightly re-scan timer (SEC-04)** — the critical troubleshooting section:
      - PREREQUISITE: `loginctl enable-linger $USER` (without it the timer NEVER fires while logged out — Pitfall 5). Check: `loginctl show-user $USER --property=Linger`.
      - Install: copy `systemd/harnessed-rescan.{timer,service}` to `~/.config/systemd/user/`, `systemctl --user daemon-reload`, `systemctl --user enable --now harnessed-rescan.timer`.
      - Diagnose: `systemctl --user list-timers harnessed-rescan.timer` (is it scheduled?) + `journalctl --user -u harnessed-rescan.service --no-pager` (did it run? what did it find?).
      - Network: the nightly re-scan uses the ONLINE osv.dev DB (requires network egress; the build-time offline gate does not). If the journal shows network errors, check egress to osv.dev.
      - "0 findings forever" warning sign (Pitfall 6): if the nightly always reports clean even after a widely-disclosed CVE, confirm the scan is using `scan-image-online` (online), not `scan-image` (offline).
    - **Secrets / varlock** — point at `docs/guides/secrets.md` (shipped in 05-02) for the opt-in workflow. Common issue: `op://` refs unresolved → the 1Password agent socket isn't mounted (is the desktop app running?) or the vault item path is wrong.
    - **Supply-chain scan failures** — `harnessed build` aborts on HIGH: read the finding id; the vuln fixture (`tools/test-fixtures/vuln-stack`) demonstrates the abort. snyk/socket warn-and-skip without tokens (SEC-02) — check `SNYK_TOKEN`/`SOCKET_SECURITY_API_KEY` if you expected them to run.
  </action>
  <verify>
    <automated>test -f docs/guides/service-authoring.md && test -f docs/guides/troubleshooting.md && grep -qi 'services/ping' docs/guides/service-authoring.md && grep -qi 'service.yaml' docs/guides/service-authoring.md && grep -qi 'Dockerfile' docs/guides/service-authoring.md && grep -qi 'server.py' docs/guides/service-authoring.md && grep -qi 'streamable' docs/guides/service-authoring.md && grep -qi 'svc up' docs/guides/service-authoring.md && grep -qi 'enable-linger' docs/guides/troubleshooting.md && grep -qi 'loginctl' docs/guides/troubleshooting.md && grep -qi 'list-timers' docs/guides/troubleshooting.md && grep -qi 'journalctl' docs/guides/troubleshooting.md && grep -qi 'fresh' docs/guides/troubleshooting.md && grep -qi 'claude.json' docs/guides/troubleshooting.md && grep -qi 'secrets.md' docs/guides/troubleshooting.md && grep -q 'docs/harnessed-design.md' docs/guides/service-authoring.md && grep -q 'docs/harnessed-design.md' docs/guides/troubleshooting.md && echo '05-04 Task 3 structural checks OK'</automated>
  </verify>
  <acceptance_criteria>
    - docs/guides/service-authoring.md documents the service triple (service.yaml + Dockerfile + server), walks through services/ping as the worked example, documents Streamable-HTTP transport (states SSE deprecated), and covers the svc lifecycle + recipe attachment
    - docs/guides/troubleshooting.md covers: podman/rootless setup clarification, first-run build issues, ~/.claude.json onboarding, --fresh, host-persisted sessions (the legible state-dir path), AND the SEC-04 nightly timer section (loginctl enable-linger prerequisite, install steps, systemctl/journalctl diagnostics, network requirement, the "0 findings forever" warning sign)
    - docs/guides/troubleshooting.md cross-references docs/guides/secrets.md (shipped in 05-02) for the secrets workflow
    - both docs cross-reference docs/harnessed-design.md for the WHY
    - all worked examples reference real repo files (services/ping/*) that exist and are runnable
  </acceptance_criteria>
  <done>service-authoring.md walks through the ping service triple with Streamable-HTTP transport; troubleshooting.md covers every operational scenario including the SEC-04 timer prerequisites + diagnostics; both cross-reference the design doc and the secrets doc.</done>
</task>

</tasks>

<verification>
- [ ] README.md at repo root: what/why, two modes, install, first-run build, quickstart (transparent + isolated), command surface, links to design + guides
- [ ] AGENTS.md reconciled with README (no contradictions; container-era quickstart redirected/folded)
- [ ] docs/guides/recipe-authoring.md: recipe.yaml schema, recipes/time + recipes/ping worked examples, transports (stdio/streamable-http, SSE deprecated), pnpm/uvx supply-chain rules
- [ ] docs/guides/stacks.md: stack.yaml schema, tracer-time + transparent + ping-time worked examples, harnessed new scaffolding, build/run/test lifecycle
- [ ] docs/guides/service-authoring.md: service triple, services/ping worked example, Streamable-HTTP, svc lifecycle
- [ ] docs/guides/troubleshooting.md: podman setup, first-run build, ~/.claude.json onboarding, --fresh, host-persisted sessions, SEC-04 timer prerequisites + diagnostics, secrets cross-ref
- [ ] docs/guides/secrets.md (from 05-02) is cross-referenced from README + troubleshooting
- [ ] every worked example runs as-documented (copy-paste runnable)
- [ ] no real tokens/secrets in any example (placeholders only)
</verification>

<success_criteria>
- All tasks completed
- README.md documents what harnessed is, the two modes, install, first-run build, and a quickstart (DOC-01)
- Recipe-authoring + stacks guides document writing recipes/stacks with a worked example (DOC-02)
- Service-authoring + troubleshooting guides exist and match shipped behavior (DOC-03 — secrets doc shipped in 05-02)
- AGENTS.md and README.md do not drift
- Every worked example is copy-paste runnable
</success_criteria>

<output>
After completion, create `.planning/phases/05-secrets-hardening-docs-completeness/05-04-SUMMARY.md`
</output>
